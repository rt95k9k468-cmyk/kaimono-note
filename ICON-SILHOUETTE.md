# 買うものの絵を、単色シルエットへ寄せる — 手順と、いまどこまで来たか

**決めたこと：708枚ぜんぶを単色シルエットにする。**
（32枚と87枚、二度の撮り比べを見ての判断。経緯と数字はこの文書の下のほうと、
`ICON-TYPES.md` の「買うものを32枚で撮り比べた」を見ること。）

**食材も含めます。** はじめは「食材は色つきのまま」で始めましたが、87枚を実際に
一覧へ出したら**混在が持ちませんでした**——単色は濃くくっきり、色つきは淡く
ぼんやりなので、同じ一覧の中で食材の行だけが薄れて見えます。食材のシルエット
（71型）はもとからあるので、弱かった3枚（食パン・ばなな・豆腐）を描き直して
使っています。

**いちばん大事な決めごと：絵は「何の仲間か」だけを言い、名前が「何か」を言う。**
一覧では絵のすぐ横に名前があるので、しゃんぷう と りんす を絵で見分ける必要は
ありません。25px には入る形の数に上限があるので、**細かく分けるほど全部が
近づきます**（下の「天井にぶつかりました」）。

## いまどこまで

| | 枚数 | 置き場 |
|---|---|---|
| **食材 269** | 71型 | `js/icons-food.js` ✅ できている |
| **容器 73** | 14型 | `js/icons-goods.js` の `TYPES`/`OF` ✅ できている |
| **個別 14** | 14枚 | `js/icons-goods.js` の `ICONS` ✅ できている |
| **道具まわり 71**（一束目） | **15型** | `js/icons-goods.js` ✅ できている |
| **家電・家具 65**（二束目） | **20型** | `js/icons-goods.js` ✅ できている |
| **残り 204** | 53型（設計だけ） | `icon-silhouette-types.json` ← ここから |
| 容器・食材の型へ回す 8 | — | 同ファイルの `toContainerType` / `toFoodType` |

**93型のうち40型を片づけました**（一束目＝道具まわり、二束目＝家電・家具）。
**34型がセットから／1型は描き起こし（えぷろん）／5型は畳んで消えた**。
93 → **88型**。決めたものは `icon-silhouette-types.json` の `done` /
`folded` に入っています。

**まだどこからも呼んでいません。** 全部そろってから、`store.productMark()` が
引く `KN.productIcons.byKey` を一度だけ差し替えます。

候補の内訳（`icon-silhouette-candidates.json` の点数）：

- **強い当たり（0.88以上）303枚（72%）** … 名前がそのまま合っている
- **弱い当たり（0.88未満）117枚（28%）** … 何か当たってはいるが、別物の見込み
- 当たり無し 0枚

**点数は候補であって、採否ではありません。** そして——**この点数は当てになりません。**

42枚（先頭22枚と、離れた場所の20枚）を実際に目で見て数えたら、こうでした：

| | 枚数 | |
|---|---|---|
| 採用できる | **17（40%）** | |
| 別キーと同じ絵になる | 3 | ぽんぷが しゃんぷー と重なる等。描き分けが要る |
| **描くことになる** | **22（52%）** | |

**「強い当たり72%」は、名前が合った率であって、絵が合った率ではありませんでした。**
実際に出てきた外れ：

- `bleach`（漂白剤）→ **マイク**（`mic`）
- `soapBar`（石けん）→ **ワインバー**（"bar" に当たった）
- `grater`（おろし金）→ **@マーク**
- `rollingPin`（めん棒）→ **画鋲**（"pin"）
- `corkscrew`（栓抜き）→ **C#** / **RSファイル**（"cs" / "rs"）
- `donburi`（どんぶり）→ **バス**
- `dishRack`（水切りかご）→ **HDDラック**
- `cottonSwab`（綿棒）→ **USBメモリ**
- `kitchenRoll`・`foil` → **ペンキのローラー**

420枚に当てはめると、**採用 約170枚／描く 約250枚**です。

外れる理由は二つあります。**①短い英単語は、UIの語彙と衝突する**（bar・pin・
cs・rs・roll）。**②日用品と和の台所道具は、そもそもUIのアイコンセットに無い**
（おろし金・めん棒・栓抜き・どんぶり・水切りかご・急須）。

**必ず目で見て、そのあと測ること。**

## やり方（確立したもの。順番が要点）

### ① セットを探す。描くのは最後の手段

想像で描くと遠回りになります。過去に、徒歩・温泉・掃除機・車を描き直しては
別物にして、探したら全部よそにありました。32枚のときも、まな板・ティッシュ・
らっぷ・歯ぶらしで**三度**描き直しています。

### ①' 探して無かったら、描く前に**参考画像をもらう**

**束の終わりに、「描くことになったもの」を一覧にして訊くこと。**
セットに無いと分かった時点で描き始めない——20型を見終わってから、
「この◯個は描くことになりました。参考画像をください」とまとめて出し、
**画像をもらってから描く**。

理由は実測です。えぷろんを**想像で三度こわしました**（腰の帯＝軒のある家 →
帯を下げたら人の胴 → 肩ひもで つなぎ の胸当て）。参考画像を一枚もらったら、
**一発で決まりました**。そこにあったのは「肩ひもが上へ広がり、裾が下へ広がり、
胸にポケットがひとつ」という形で、想像では出てこなかったものです。

もらった画像は**割合を写して使う**（丈に対する幅・ひもの長さ・ポケットの位置）。
そのまま写せないのは**線の太さ**だけです——参考画像の輪郭線は、うちの升目に
直すと 3（0.3px）しかなく、25px では靄になります。**線は抜きに変える**か、
16 以上に太らせること。

### ② 索引の組み直し（スクラッチは消えるので、毎回ここから）

`@iconify-json/*` を npm から取ります。**`api.iconify.design` は proxy が 403
ですが、npm パッケージ版は通ります**（`registry.npmjs.org` は noProxy に入って
いる）。jsdelivr・unpkg・codeload も 403。`raw.githubusercontent.com` は通ります。

```
npm pack @iconify-json/{ph,material-symbols,tabler,ri,bi,fluent,mingcute,ion,bxs}
```

面（fill）だけを抜いて一本の索引にすると **15,908枚**。セットごとの見分け方：

| prefix | 面の見分け | 枚数 | licence |
|---|---|---|---|
| material-symbols | `-rounded` かつ `-outline` を含まない | 4053 | Apache 2.0 |
| fluent | `-<数字>-filled`（24を優先、次に20） | 3168 | MIT |
| mingcute | `-fill` | 1682 | Apache 2.0 |
| ri | `-fill` | 1541 | Apache 2.0 |
| ph | `-fill` | 1533 | MIT |
| ion | `-outline` / `-sharp` を含まない | 1504 | MIT |
| tabler | `-filled` | 1091 | MIT |
| bi | `-fill` | 671 | MIT |
| bxs | 全部 solid | 665 | MIT |

（`ion` が前回の 557 から増えているのは、`md-` / `ios-` の古い言い方も
数えているからです。中身は素の名前と同じ絵なので、上位に二重で出てきます
——引くときに気になるだけで、選ぶのは目のほうです。）

**別名（`aliases`）を解くこと。** `icons.json` には `icons` のほかに
`aliases` があり、**`material-symbols` の `-rounded` は多くが別名側**にいます。
解かないと、あるのに「無い」と数えます——前回 14,684枚だったのはこれで、
`mop-rounded`（もっぷ）がまさに落ちていました。親をたどって同じ body を写す
だけです。ただし**回転・反転を持つ別名は落とす**（素直に写せない）。

**採らないと決めたもの**：

- **CC-BY-4.0 のセット**（solar・streamline・iconamoon・lets-icons・
  emojione-monotone・Font Awesome Free）… 帰属表示の欄がアプリに要る
- **hugeicons**（6005枚・MIT）… `detergent` も `tissue-paper` も持っているが、
  **線でできている**（`fill="none" stroke`）。面の一族に混ぜられない
- **healthicons**（MIT）… 描き方が線寄り。しかも `tissue` は**生体組織**だった
- **素材サイト**（flaticon・icons8・Noun Project・UXWing）… licence が合わず、
  そもそも絵を見られない（WebFetch はページを文字にする）

### ③ 引く言葉は、キー名そのもの

`cuttingBoard` `toiletRoll` `bathScale` ——**もともと英語**なので、対訳表を
人手で作る必要がありません。camelCase を語に割って引きます。

二つだけ機械の手当てが要ります：

- **単複を揃える**（`socks`→`sock`、`wipes`→`wipe`、`groceries`→`grocery`）。
  揃えないと、ある絵を「無い」と数えます
- **1文字の語を落とす**。`t-shirt` の `t` や `r-circle` の `r` が
  「toilet」「detergent」に部分一致して、上位を雑音で埋めます

言い換えの表（`bulk.js` の `ALIAS`）は、**うちの言い方とセット側の言い方が
ずれるものだけ**。推測で足さないこと——足したぶんは目で見て確かめる。

### ④ 目で見る → ⑤ 測る

候補を **64px と「25px を3倍に引き伸ばしたもの」で並べて**撮り、目で選ぶ。
引き伸ばしは `image-rendering: pixelated`——なめらかにすると、潰れているのか
読めているのかが分からなくなります。

そのあと測る。物差しは二つ（`ICON-TYPES.md` と同じ）：

- **core** … 25px で1px 侵食したあとに残るインクの割合。0.33未満が危険域
- **形の距離** … 12×12 のアルファどうし。**0.14〜0.16 で溶け合う**

**物差しは毎回書き直しになるので、目盛りを合わせてから使うこと。**
スクリプトはスクラッチに置く決まりなので、セッションが変わると同じ式を
書き直します。ところが**書き直した式は、前と同じ数字を出しません**
——正規化のしかたひとつで、同じ組が 0.156 にも 0.124 にもなります。

合わせかたは、**もう分かっている組を当てて確かめる**こと。`icons-food.js` に
もとからある、この六組が目盛りです：

| 組 | 前の物差し | 今回の物差し |
|---|---|---|
| のり／びーる | 0.147 | 0.130 |
| 漬物／ぐらたん | 0.154 | 0.156 |
| とまと／缶 | 0.156 | 0.124 |
| のり／わさび | 0.158 | 0.152 |
| ごみ袋／かまぼこ | 0.152 | 0.125 |
| にんじん／きゅうり | 0.154 | — |

**倍率でずれているのではありません**（漬物／ぐらたんはほぼ同じ、とまと／缶は
0.03 も違う）。だから「前の 0.16」を今回の式に読み替える換算は作れません。
**その回の式で、上の組がどこに出るかを見て、そこより上を安全とする。**
今回は既知の組が 0.124〜0.156 に出たので、**0.16 以上を採用の目安**にしました。

**一束ごとに、いまある絵ぜんぶと比べること。** 候補どうしを比べても意味が
ありません——溶ける相手は、たいてい**食材や容器のほうにいます**（今回、鍋の
第一候補は豆腐と 0.110、はかりの分銅はジャーと 0.114、ごみ箱の第一候補は薬と
0.115 でした。どれも目では気づけません）。

**目と測りは、食い違います。** 32枚のときに二度ありました：

- 犬と猫は、**顔のほうが**全身の座り姿より離れた（0.30超 対 0.211）。
  四つ足の座り姿はどちらも「丸い塊＋尻尾」に収束する
- 歯ぶらしは、形としては歯ぶらしになったのに **にんじんと 0.133**（溶け合う
  目安の中）だった。どちらも「縦に細って、上が広い」形。片方は野菜で片方は
  道具なので、**意味のほうが違いを埋めて、目では気づけない**

### ⑥ 器を合わせる

`F()` が `<g transform="translate(x y) scale(s)">` をかぶせます。外枠を
`getBBox()` で実測して、**長辺が216・中心が128,128**になる数字を出す。
**手でいじらないこと**（スクリプトが出すもの）。器は `-8 -8 272 272`。

## 描くときの決めごと（探して無かったぶん）

`icons-food.js` と同じ文法：256の升目・絵は24〜232・面だけ・細部は evenodd で
抜く・いちばん細いところで16以上・色は `currentColor`。

32枚のときに学んだこと：

- **紙は、ちぎれた縁で紙だと言う。** ティッシュの一枚を、小さく→大きく→斜めに
  してもずっと「親指を立てた手」でした。原因は**縁がなめらかなこと**。丸い山は
  指の形です。らっぷも同じで、V字ひとつは**旗**の記号でした
- **向きは飾りではなく、離すための寸法。** 縦・横は既に何かが占めています
- **二段は「すくうもの」の形。** 頭と柄が直につながる形は匙・へらになります。
  歯ぶらしは**首を入れて三段**にして離れました
- **寸詰まりの出っ張りは「持ち手」ではなく「留め具」に見える。** まな板の柄を
  56×60 で描いたら、板を横長にしても鞄のままでした
- 半円＋丸い穴は**レンズ**の記号です（らっぷがカメラになりました）

## 出しかた（採ると決まったときの段取り）

- 絵は `js/icons-goods.js` に置く。クラスは **`is-todo`**——これは「やることの
  絵」ではなく**「単色のシルエット」**の印で、暦の日の下の丸の中で白抜きになる
  のがそこに掛かっています（`.cal-mark .p-icon.is-todo`）
- **キーは一文字も変えない。** 保存済みの `t.icon` はキー文字列なので、変えると
  利用者の絵が外れます。変えるのは「どの絵を出すか」だけ
- **色つきの708枚（`icons-v2.js`）は消さない。** `icons-legacy.js` /
  `icons-todo-hand.js` と同じ扱いで、一行で戻せるように残す
- 差し替えるのは `store.productMark()` が最初に引く `KN.productIcons.byKey`。
  **食材は素通しにすること**（`icons-goods.js` に食材のキーを入れない）
- 新しい `.js` を足したら**3箇所**に登録（`index.html` / `sw.js` /
  `build-standalone.js`）。**採ると決まってから**——判断前に既定を切り替えない

## 容器73枚を型に寄せた — と、そこで分かった天井

「色つきでは見分けがつかない群を先に」で、容器73枚に着手しました。
73枚を全部ちがう形にはできないので、**外形で切った型**にまとめています
（`icons-food.js` の食材71型と同じ考え方）。

- **29型で73枚を受け持つ**（17型はセットから、12型は描き起こし）
- `js/icons-goods.js` の `TYPES` と `OF`。`byKey` が
  `ICONS[key] || TYPES[OF[key]]` の順に引く

### 天井にぶつかりました

型・既存・食材をまとめて **114枚**を一つの鍋で測ると、**溶け合う組（0.16未満）が
13組**あります。しかも**直すと別の相手とぶつかります**：

| 直したもの | 直す前 | 直した後、代わりに近づいた相手 |
|---|---|---|
| 詰め替え袋 | 卵 0.115 | **のり 0.104**（肩を角にしたら、四角に寄った） |
| 哺乳瓶 | わさび 0.111 | **きゅうり 0.106**（胴を細くしたら、筒に寄った） |
| 粒の袋 | 冷凍餃子 0.131 | **よーぐると 0.109**（角底にしたら、カップに寄った） |

一回目の手当てで 39組 → 17組、二回目で 17組 → 13組。**減ってはいますが、
毎回ちがう組が出ます。**

**これが答えです。25px の単色シルエットには、入る形の数に上限があります。**
114枚でもう詰まっているので、439枚（＋食材71＝510枚）は入りません。

理由は形の作りにあります。容器は「**縦に長くて、上に何かある**」形の変奏で、
そこが混みあいます。ボトル・チューブ・水筒・哺乳瓶・袋・箱——25px（＝12×12 に
落とすと**実質144画素**）では、口の作りの違いが残りません。

`ICON-TYPES.md` の「食材以外437枚は寄せても3%しか減らない（424型）」は、
**色つきの絵から機械で抜いたシルエット**を測った数字でした。あれは中の模様が
抜きとして残るので、散らばって見えていたのです。**手で描いた素直な
シルエット**にすると、こうして団子になります。

### 答え：型を粗くする。**絵は仲間だけを言い、名前が何かを言う**

**「しゃんぷー と りんす が、わざわざ絵や色で見分けられる必要は無い。」**
——これが決めごとになりました。一覧では絵のすぐ横に名前があるので、絵が言う
べきなのは「**何の仲間か**」だけです。

29型 → **14型**にしました。ボトル／スプレー／チューブ／ジャー／繰り出し棒／
石けん／袋／箱／薬／カップ／椀／皿／注ぎ口の器／バケツ。**73枚をこの14型が
受け持ちます。**

畳んだところ：

- ぽんぷ・注ぎ口・取っ手つき・細口・小びん・香水びん → **ボトル**
- トリガー噴霧・噴射缶 → **スプレー**（「吹きつける」は使い方が違うので残す）
- 自立ぱうち・粒の袋・ジップ袋 → **袋**（25px ではぜんぶ同じ台形。実測 0.11〜0.15）
- 粉の箱・角い保存容器 → **箱**
- 薬びん・錠剤・目薬 → **薬**
- まぐ・ぐらす・水筒・哺乳瓶 → **カップ**
- 椀・ざる → **椀**
- 急須・じょうろ → **注ぎ口の器**
- ますから → **繰り出し棒**（口紅と同じ）
- こんぱくと → **ジャー**

実測（型14＋個別14＋食材71＝**99枚**）：

- core 中央値 **0.628**、危険域（0.33未満）**0枚**
- 溶け合う組（0.16未満）**9組**。うち**6組は、私が触る前から `icons-food.js` に
  あった食材どうし**（のり／びーる 0.147、とうもろこし／あいす 0.152、
  漬物／ぐらたん 0.154、にんじん／きゅうり 0.154、とまと／缶 0.156、
  のり／わさび 0.158）
- 残り3組は 0.152〜0.159 で、溶け合う目安（0.14〜0.16）の**上端**
  （ごみ袋／かまぼこ、バケツ／豆腐、袋／小麦粉）

29型のときは13組でした。**粗くしたほうが、混みあいが減ります。**

### いま分かっていること

- **5枚なら分かれます。** 32枚の撮り比べで、ぽんぷ／注ぎ口／取っ手／
  スプレー／ジャーは、色つきより読みやすくなりました
- **29型は入りません。** 食材71型がすでに空間の半分を占めているので
- 食材どうしにも、もとから溶け合う組があります（のり／びーる 0.147、
  にんじん／きゅうり 0.154、とまと／缶 0.156）

## 87枚を実際に一覧へ出して撮った（試験2）

同じ型に落ちた組を**わざと隣り合わせに**並べて撮りました——ボトル6・スプレー3・
袋3・カップ3。「絵は仲間だけを言う」が買い物リストとして成り立つかは、
そこでしか分からないので。

### 分かったこと

**① 同じ絵が縦に続いても、読めます。** しゃんぷー・りんす・ぼでぃそーぷ・
食器用洗剤・洗濯洗剤・柔軟剤——**6行が同じボトル**でも「同じものが6つ」には
見えません。名前がすぐ横（タイルでは下）にあるからです。
むしろ**色つきのほうが落ち着きません**——6つの小さなボトルが青・緑・ぴんく・
水色とばらけて、色が「何の品物か」ではなく**ただの賑やかさ**になっていました。

**② 「食材だけ色つき」は持ちませんでした。** 単色は濃くくっきり、色つきは淡く
ぼんやりなので、同じ一覧の中で食材の行だけが**薄れて見えます**。
→ **食材もシルエットにしました**（71型はもとからある）。一族が揃います。

**③ ボトルとスプレーの差は効いています。** 「洗剤」の中で、注ぐものと
吹きつけるものが 25px で見分けられるのは、実際に役に立つ切り方でした。

**④ 薄い灰色の丸の中では、単色のほうが立ちます。** 淡い色つきは地に埋もれます。

### そこで直した5枚

- **食パン** … ドームを胴より**張り出させた**。張り出しの無いドームは墓石・扉
- **ばなな** … 太いほうの端に**軸**を付けた。両端が同じにすぼまる三日月は月
- **豆腐** … **浅く広い**盛りに。深い容器にすると「入れもの」に読めて中身が消える
- **チューブ** … **二度こわしました**。横に寝かせたら棒、20度傾けても斜めの棒。
  向きではなく**上下が逆**でした。チューブをチューブにしているのは「細い口が
  上、潰した底がいちばん広い」という上下の差で、口を下にすると差が消えます
- **水筒** … カップ型からボトル型へ。持ち歩く瓶なので、マグに畳むと意味が遠い

実測（99枚）：core 中央値 **0.630**、危険域 **0枚**、溶け合う組 **9組**
（うち6組は、もとから `icons-food.js` にあった食材どうし）。

## 残り341枚の型分け（`icon-silhouette-types.json`）

**93型**に切りました。切りかたは**仲間**（何に使うか）で、同じ仲間の中でも
25px で輪郭がはっきり違うものだけ別型にしています。

大きな束（受け持ちが多い型）：

| 型 | 枚数 | 入るもの |
|---|---|---|
| 紙 `paperSheet` | 14 | こぴー用紙・書類・契約書・住民票・保険・れしーと・宅配伝票… |
| かとらりー `cutlery` | 10 | すぷーん・ふぉーく・箸・れんげ・すとろー・つまようじ… |
| 書くもの `penTool` | 10 | ぺん・鉛筆・しゃーぺん・まじっく・蛍光ぺん・くれよん・絵の具… |
| 布もの `cloth` | 9 | ふきん・たおる・らぐ・玄関まっと・かーてん・毛布・しーつ… |
| おむつ・しーと `diaper` | 9 | おむつ・ぺっとしーつ・保冷剤・かいろ・湿布・絆創膏・包帯… |
| 上衣 `topWear` | 8 | てぃーしゃつ・せーたー・ぱーかー・ぶらうす・ぱじゃま・下着… |
| かばん `carryBag` | 8 | 旅行・らんどせる・えこばっぐ・くーらーばっぐ・抱っこ紐… |
| 箱の家電 `boxAppl` | 9 | 電子れんじ・とーすたー・洗濯機・乾燥機・食洗機・ぷりんたー… |
| 出かける先 `leisure` | 12 | 公園・動物園・水族館・映画館・温泉・海水浴・登山・きゃんぷ… |
| お店 `shop` | 12 | こんびに・すーぱー・八百屋・ぱん屋・書店・百貨店・薬局… |

**93型は、たぶん多すぎます。** 全部そろえると 93＋14＋14＋71 ＝ **192枚**に
なりますが、114枚でもう9組が溶け合っていました。**測ってから畳む**ことになります。
一束目で、じっさい 20型のうち5型が畳んで消えました（この割合なら 93 → 70台）。
畳む余地は分かっていて、**22型が1キーしか受け持っていません**（はかり・たいまー・
掃除機・かめら・あいろん・時計・鏡・傘・ふぁいる・消しごむ・電卓・ねくたい・
靴下・帽子・べると・ますく・べびーかー・ぺっと・体温計・飛行機・自転車・蚊取り）。
ただし傘・自転車・飛行機のように、**畳む先が無いもの**もあります（仲間が他に無い）。

**93型すべてに候補があります**が、それは名前が当たっただけです——42枚を目で見た
ときの実績は 40%。**目で見て、そのあと測る**のは変わりません。

（一束目の実績：20型のうち**候補の一位をそのまま採れたのは 8型**でした。
残りは目か測りで入れ替わっています。表の `cand` は探す取っかかりであって、
答えではありません。）

## 一束目（1〜20型・道具まわり）— 済み

71キーが片づきました。**セットから14型、描き起こし1型、畳んで消えたのが5型。**

| 型 | 採ったもの | 距離 | core |
|---|---|---|---|
| 鍋・やかん | `mingcute:soup-pot-fill` | 0.296 | 0.358 |
| ふらいぱん | `material-symbols:skillet-rounded` | 0.322 | 0.440 |
| かとらりー | `material-symbols:fork-spoon-rounded` | 0.381 | 0.417 |
| 調理の柄もの | `mingcute:spoon-fill` | 0.474 | 0.474 |
| はさみ | `mingcute:scissors-fill` | 0.369 | 0.207 |
| 刃もの | `mingcute:knife-fill` | 0.338 | 0.534 |
| かご・水切り | `tabler:basket-filled` | 0.256 | 0.552 |
| たいまー | `bxs:timer` | 0.320 | 0.416 |
| 長い柄の掃除道具 | `material-symbols:mop-rounded` | 0.311 | 0.433 |
| ぶらし | `mingcute:brush-fill` | 0.282 | 0.380 |
| 手袋 | `mingcute:glove-fill` | 0.192 | 0.627 |
| ごみ箱 | `bi:trash-fill` | 0.246 | 0.370 |
| 掃除機 | `material-symbols:vacuum-rounded` | 0.421 | 0.213 |
| 布もの | `ph:towel-fill` | 0.192 | 0.618 |
| えぷろん | **描いた** | 0.189 | 0.593 |

（距離は「いまある114枚のうち、いちばん近い相手まで」。既知の溶け合う組が
0.124〜0.156 に出る物差しなので、0.16 以上を採用の目安にしています。）

**114枚を測り直しても、溶け合う組は増えませんでした。** 0.16 未満の組は
26組ありますが、**その全部がもとからある99枚どうし**で、今回足した15枚は
一組も作っていません。

### 目で見たら、点数の一位はよく外れました

- **鍋** … 第一候補 `ph:cooking-pot-fill` は**豆腐と 0.110**。浅く広い塊は
  同じ形です。湯気と取っ手ふたつのある `soup-pot` に替えて 0.296
- **はかり** … 分銅（`weight`）は**ジャーと 0.114**。畳みました（下）
- **ごみ箱** … `ri:delete-bin-fill` は**薬と 0.115**。縦の筋がある
  `bi:trash-fill` にして 0.246
- **たいまー** … `material-symbols:timer-rounded` は天ぷらと 0.167。走り線の
  ある `bxs:timer` にして 0.320

### core が低い二枚は、目で見て残しました

はさみ 0.207、掃除機 0.213。どちらも**もともと線でできている絵**で、
25px に焼いて3倍に引き伸ばして見ると、輪と刃・胴とホースがはっきり読めます。
`ICON-PHOSPHOR.md` の「**core は自分で描くときの物差し**」のとおりです。

### 5型は、畳んで消えました

型を二つ立てて同じ絵を置くくらいなら、**キーを絵のほうへ回す**（`SAME`）。

- **はかり → 体重計の絵**。どちらも「はかり」です
- **巻きもの → といれっとぺーぱー と らっぷ**。きっちんぺーぱーは同じ巻きもの、
  あるみほいるは同じ「箱＋刃で切った縁」——**もともと同じ物の形**でした
- **しーとの袋 → ていっしゅ**。一枚ずつ引き出す紙もの、で一つ
- **ねっと → 袋**。排水口ネットも洗濯ネットも、25px では袋です
- **開ける道具 → 調理の柄もの**。缶切り・栓抜き・こるくすくりゅーは、
  15,908枚を探して一枚もありません。描こうとしても、栓抜きは「棒＋丸い穴」＝
  匙か鍵になります。**台所の柄もの**として畳みました
- （**鍋つかみ**は 手袋 へ移しました。ミトンは手袋そのものの形なので）

### えぷろんは、想像で三度こわして、参考画像で一発で決まりました

面の9セットに、えぷろん単体はありません（`person-apron` は人が着ている絵）。
そこで描いたのですが、**想像では三度こわしました**：

1. 胸当て＋裾＋**腰の帯** … 帯が軒に見えて**家**になった
2. 帯を裾の中ほどへ下げた … 今度は**人の胴**（胸当てと裾だけの形は、
   肩から下の輪郭そのもの）
3. **肩ひもを二本立てた** … つなぎ の胸当てになった。えぷろんではある

**参考画像を一枚もらったら、一発でした。** そこにあったのは
「**肩ひもが上へ広がり、裾が下へ広がり、胸にポケットがひとつ**」という形で、
三度描いても出てこなかったものです。丈208に対して裾の幅136、上端の幅は裾の
79%、肩ひもは丈の21%——**割合をそのまま写しています**。

写せなかったのは**線の太さ**だけです。参考画像のポケットは輪郭線で描かれて
いますが、うちの升目に直すと 3（25px で 0.3px）——靄になって、ポケットでは
なく汚れに見えます。**白く抜きました**（25px でも白い帯として残ります）。

測り比べ（いちばん近い相手までの距離／core）：

| | 距離 | core | |
|---|---|---|---|
| 参考画像に忠実（ひも16・輪郭ポケット） | 0.154 | 0.478 | まよねーずと溶ける |
| ひも22・べたポケット | 0.162 | 0.449 | |
| **採った**（ひも長め・裾広め・べたポケット） | **0.167** | 0.443 | |
| 同・輪郭ポケット | 0.178 | 0.378 | 25px でポケットが靄 |

**忠実に写すほど溶けます**（細く小さくすると、輪郭が「上が細くて下が広い塊」に
戻るので）。骨格は写して、寸法だけ 25px 側へ寄せること。

## 二束目（21〜40型・家電と家具）— 済み

65キーが片づきました。**20型ぜんぶ、セットから採れました**——描くものも、
畳むものもゼロです。

| 型 | 採ったもの | 距離 | core |
|---|---|---|---|
| はんがー | `tabler:hanger-2-filled` | 0.302 | 0.324 |
| 画面のもの | `ph:laptop-fill` | 0.214 | 0.569 |
| 箱の家電 | `ri:printer-fill` | 0.334 | 0.479 |
| 風・熱の家電 | `ph:fan-fill` | 0.277 | 0.496 |
| 台所の家電 | `material-symbols:coffee-maker-rounded` | 0.310 | 0.464 |
| 音のもの | `ph:headphones-fill` | 0.443 | 0.331 |
| かめら | `mingcute:camera-fill` | 0.224 | 0.515 |
| 電源・けーぶる | `ph:plugs-fill` | 0.346 | 0.383 |
| 小さな記録もの | `ph:floppy-disk-fill` | 0.462 | 0.375 |
| りもこん | `material-symbols:gamepad-rounded` | 0.382 | 0.429 |
| きーぼーど | `mingcute:keyboard-fill` | 0.224 | 0.377 |
| どらいやー | `ph:hair-dryer-fill` | 0.332 | 0.440 |
| あいろん | `material-symbols:iron-rounded` | 0.223 | 0.490 |
| 椅子・そふぁ | `mingcute:sofa-fill` | 0.320 | 0.503 |
| 机・てーぶる | `mingcute:desk-fill` | 0.360 | 0.390 |
| 寝るもの | `ph:bed-fill` | 0.315 | 0.487 |
| 棚・引き出し | `ri:book-shelf-fill` | 0.326 | 0.423 |
| あかり | `bi:lamp-fill` | 0.286 | 0.477 |
| 時計 | `tabler:alarm-filled` | 0.207 | 0.548 |
| 鏡 | `mingcute:mirror-fill` | 0.324 | 0.573 |

**UIのアイコンセットは、家電と家具には厚い。** 一束目（台所道具・日用品）が
14/20 で、探して一枚も無いものが6つあったのに対し、こちらは 20/20 です。
セットは「PCまわりと部屋」でできている、ということです。

134枚を測り直しても、**溶け合う組は 26組のまま**（全部もとからある99枚どうし）。

### この束で、測って外したもの

どれも目では気づけません。**一位を採れたのは 20型のうち9型**でした。

- **四角＋丸は三つ巴**（洗濯機・すぴーかー・かめら）。かめらだけ残し、
  箱の家電は ぷりんたー、音のものは へっどほん にしました
- **丸い文字盤**は天ぷらと 0.144〜0.158。目ざまし（鐘と脚）にすると 0.207
- **SDかーど**は家計簿と 0.118（角を落とした小さな四角＝帳面）。
  ふろっぴー（肩の切り欠きとしゃったー）で 0.462
- **まうす**はりもこんと 0.142、バケツ・びーるとも 0.098〜0.156。
  丸い塊はどれとも寄るので、きーぼーどにしました
- **電球**はあいすと 0.115。電気すたんど（笠と台）で 0.286
- **てれび**は 0.173〜0.175。のーとPC（台が広がる）で 0.214

## 次にやること

1. 41〜60型（文房具・紙もの・衣類）を、同じ順で回す
   ——**探す → 目で見る → 測る**。測るときは、いまある `icons-goods.js` ＋
   `icons-food.js` の全部と比べること（溶ける相手は食材のほうにいる）。
   **紙・帳面・付箋・ふぁいる・かーど・額はぜんぶ四角**なので、この束は
   一束目より畳むことになるはずです
2. 全型がそろったら、439枚まとめて測り直す
3. `productArt` に通して、買うもの・価格・ダイエットで撮り比べる
4. 決まったら `store.productMark()` の引き先を差し替え、
   `index.html` / `sw.js` / `build-standalone.js` の3箇所に登録する
   （`icons-goods.js` は**まだどこからも呼んでいません**）
