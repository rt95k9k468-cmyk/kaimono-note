# くらしノート (kaimono-note)

日本語のバニラJS PWA。買うもの・やること・ダイエット・daily（日記）の4タブ。
`rt95k9k468-cmyk/kaimono-note` の GitHub Pages にデプロイされている。

## 最優先の約束事（絶対に破らない）

- **データ保全が最優先。** 過去に一度データ消失事故を起こしている。
  `backup.js` / `guard.js` / 復元処理 / 既存の localStorage データ / 既存の
  バックアップには、本当に必要な場合以外触れない。設定の追加は必ず
  後方互換に（`reconcile()` で既定値へフォールバック）。**データの移行が
  必要になった場合は、勝手に実行せず先に報告する。**
- **中継所URL（`kurashi-relay.*.workers.dev/...` 形式）はそれ自体が資格情報。**
  リポジトリ・コミット・成果物に書かない。fetchもしない——その GET は
  `env.MAIL.delete` を伴う破壊的操作で、保留中の健康データを黙って消費する。
- **ユーザーが貼り付ける日記本文は深く個人的な内容。** 形式判定のためだけに
  使い、内容を引用・言及・要約しない。
- **アプリ内UIに絵文字を使わない。**（`icon("gear")` の絵で代替）
- **daily（日記）は評価しない。** 目標・連続記録・月比較・達成率・空白日の
  警告色などを一切出さない。`js/daily-rules.js` のテスト群がこれを見張る。
- モデル識別子（Claude Opus/Sonnet等）をコミットメッセージ・PR・コード
  コメントに書かない。

## 開発ブランチとデプロイ

- 作業ブランチはセッションごとに指定される。`git branch --show-current`
  で確認し、指定されたブランチを使う。
- **`main` へ流すのに、いちいち訊かない。** 直したら、テストを通して
  コミットして、そのまま `main` まで流すところまでが一続き。ユーザーの
  明示的な許可のもとで認められている。
  ただし**先に報告するもの**は変わらない——データの移行、既存の記録の
  作り替え、後戻りできない類。それは「よっぽど」のほう。
- `main` への直接pushの手順（`<branch>` は現在の作業ブランチ）：
  ```
  git push -u origin <branch>
  git checkout main && git merge --ff-only <branch>
  git push origin main
  git checkout <branch>
  ```
- デプロイは GitHub Actions（"Deploy to GitHub Pages"）が自動実行。
  `mcp__github__actions_list`（method: list_workflow_runs, branch: main）で
  確認できるが、出力が大きいので `.txt` に保存されたものを python で
  スライスして読む。
- `stamp-build.js` は**絶対にローカルで実行してコミットしない**
  （ビルド時にCI側が使うもの）。
- PRは明示的に頼まれない限り作らない。

## アーキテクチャのクセ

- グローバル名前空間は `KN.*`。`<script>` の読み込み順は `index.html` が
  決めている。
- 新しい `.js` ファイルを追加したら、**3箇所**に登録が要る：
  `index.html`（`<script src>`）、`sw.js`（`ASSETS` 配列）、
  `build-standalone.js`（`JS` 配列）。
- store（`js/store.js`）は単一の localStorage キー `kaimono-note-v2`。
  `reconcile()` が `{...base, ...saved}` でマージする。**`let state = load()`
  はモジュール評価の途中（450行目あたり）で走る**ので、load パスが触れる
  ものは、それより上に書くか、巻き上げられる `function` 宣言にすること
  （`const` で下に書くと TDZ で落ちる——過去のデータ消失事故の原因）。
- `KN.util.today()` は **UTC ISO**（`toISOString()`）、`dayKey()` /
  `todayKey()` は**ローカル**。混ぜると JST で9時間・1日のズレが起きる。
  日をまたぐ集計は必ず `dayKey()` 系で揃える。
- `node(html\`...\`)` は**最初のルート要素だけ**を返す（複数ルートは黙って
  捨てる）。
- CSSの詳細度の罠: `.tl-row .tl-item`（0,2,0）は
  `.item.todo:not(.is-tile)`（0,3,0）に負ける。負けた側にクラスを足して
  詳細度を上げること。
- Daily Log は「写さず引く」設計（`store.dayFeed()` / `monthDigest()`）。
  todo・積み上げ・買い物の完了記録から**そのつど**組み立てる。新しい
  「記録用の入れ物」を増やしたくなったら、まず本当に必要か疑うこと
  ——二重管理・不整合の温床になる。

## テストの回し方

- Playwright を使う。テストスクリプトはリポジトリの外
  （セッションのスクラッチディレクトリ）に置き、コミットしない。
- ローカルサーバー：
  ```
  (setsid python3 -m http.server 8765 --directory /workspace/kaimono-note >/dev/null 2>&1 < /dev/null &)
  ```
  サーバーはターンをまたぐと落ちていることがあるので、テスト前に生きて
  いるか確認し、必要なら上のコマンドで再起動する。
- 実行：`NODE_PATH=/opt/node22/lib/node_modules node <test>.js`
- 既存のテスト資産（daily-rules.js / daily2-smoke.js / aimeal.js など）は
  過去のセッションのスクラッチ領域にあり、新しいセッションでは失われて
  いる。テストを再走行したい場合は、対象の挙動から新しく書き起こす。
- 変更のたびに、触った画面の主要テストと `daily-rules.js`（dailyの非評価
  原則）は必ず走らせる。
- **指の手つきは、本物のタッチで試すこと。** `new PointerEvent(...)` を
  自分で投げるやり方では、`touchstart` / `touchmove` を見ているものが
  **まるごと動きません**——`pull-refresh.js` がそれです。買うものの掴み手に
  `data-pull-own` が無い不具合（帯まで一緒に降りてくる）は、PointerEvent の
  試験を何度通しても出ませんでした。**「テストが通ったのに実機で崩れる」の
  正体がこれ**です。
  ```js
  const cdp = await ctx.newCDPSession(page);
  await cdp.send("Input.dispatchTouchEvent", { type: "touchStart",
    touchPoints: [{ x, y, radiusX: 12, radiusY: 12, force: 1 }] });
  // touchMove … / touchEnd は touchPoints: []
  ```
  対になる**当たり判定**も一緒に置くこと：掴み手ではないところ（紙の本体）を
  引いたら、`.screen` に transform が付く——付かないなら、その試験は
  そもそも「引いて更新」を動かせていない、と分かります。

### 「やること」は Structured に寄せてある

動画（Structured のタイムライン）を実測して、レイアウト・色・情報配置・
操作感を合わせた。ここを触るときの決めごと：

- **行は時間に比例しない。伸びるのは丸薬のほうだけ。** ここには「丸は
  伸びない」と書いてあったが、**測り直したら違った**——静止画（1284×2778、
  3倍）で見ると、15分の用事は真円、45分・1時間のものは縦に伸びた丸薬で、
  重なった二つは丸薬どうしがぶつかっている。あちらは「行は並び順、丸は
  時間」の二本立てだった。
  こちらもそう直した（`nodeH`）：30分までは真円50px、そこから 1分＝0.8px、
  170pxで頭打ち。**長さを決めていない用事は伸ばさない**（組み立ての既定
  30分は置き場所を決めるための仮の数で、絵にすると決めた数のように読める）。
  行そのものは伸びず、丸が入るぶんだけ背伸びする
  （`min-height: max(60px, --tl-h + 10px)`）。空きは相変わらず一定
  ——610分の空きと325分の空きが同じ高さ、という実測はいまも本当。
- **余白は詰めた**（行 64→60px、空き 56→36px）。**破線がすでに「ここは
  離れている」と言っている**ので、同じことを高さでも言う必要が無い。
  二つで言っていたぶんを一つに戻したぶんが、一日が一画面に入ることに回る。
  ここを動かしたら、重なりの育てる量（下記）も直すこと。
- **過ぎたぶんは色、まだのぶんは灰色**（`--pass`、`markPass`）。行ごとに
  0〜1 で、その行の始まり〜終わりを時計がどこまで通ったか。丸薬の塗り・
  背骨・破線の色が、すべてこの一つの数から出る。一日が進むと、時間割が
  上から染まっていく。進行中の一件だけ、塗りの境目をぼかす。
  **今日を見ているときだけ**（`nowMin == null` なら全部1）——明日を開いて
  一日ぶんが灰色だと、これから組む画面として読めない。灰色が言っている
  のは「まだ来ていない」ではなく「**今日のうちで**まだ来ていない」。
  済ませたものは、時計より先でも 1（色のまま）。
  まだのぶんの灰色には棚の色を6%混ぜてある（`--tl-wait`）——夜の用事は
  18時を過ぎるまでずっとこちら側なので、まっさらな灰色にすると夜の青が
  「済んでから」しか出てこない。
- **薄くするのは字だけ。線は薄くしない。** `.tl-free-row.is-past` に
  `opacity: .6` が残っていて（`--pass` を入れる前の決めごと）、同じことを
  二つが別々に言っていた。実測で、丸薬をつなぐ線が (225,113,107)、すぐ下の
  点線が **(236,169,165)**——「丸薬の間の線の色が周囲と合わない」の正体。
- **`--tl-wait` は一色の灰**（棚の色を混ぜない）。混ぜていたら、昼側
  (232,225,224) と夜側 (223,225,227) が空きの行の境目でぶつかった。混ぜて
  いた理由（夜の青が済んでからしか出ない）は、丸薬をべた塗りにした時点で
  消えている。
- **線の色は、丸薬の地とまったく同じ**（`--tl-fill` ／ `--tl-wait`）。
  前は線だけ濃く（`--cat` そのまま）、破線はさらにその62%で、縦一列に
  三つの濃さが並んでいた。丸から丸へ続く一本なら、丸と同じ色が素直。
  **一つの変数から出すこと**——二か所に書くと、片方だけ直した日に割れる。
- **用事の行の線は、丸薬の上端と下端で三つに割る。割る場所は丸薬の寸法から
  出す**（`--node-top` / `--node-bot` / `--rail-p`、`--tl-node-h` と
  `--node-dy` から）。行の高さを決めるのは題とメモで、**時間ではない**。
  時間を持っているのは丸薬だけ。上は始まっていれば色（`--rail-top-c`）、
  下は終わっていれば色（`--rail-bot-c`）、中は丸薬の地とまったく同じ式。
  `0 ≤ pass ≤ 1` である限り、色の変わり目は必ず丸薬の中に落ちる
  ——**線が丸薬を追い越すことが、式として書けない。**
  「行の 50% で二値に割る」と書いてあった時期があるが、あれは「丸薬は行の
  まん中にいる」という前提に寄りかかった数で、**当たっていただけ**だった。
  空きの行のほうは、そこが時間そのものなので pass の位置で切る。
  - **丸薬のずらしは `transform` に直書きしない**（`--node-dy`）。線の境目が
    この数から出るので、位置だけ動かすと線が置き去りになる。
  - **いちばん下の行の線を切るのは `clip-path`。`height` ではない。**
    高さを縮めるとパーセントの基準がその箱に変わり、境目が丸薬からずれる
    （実測 2.5px、まだ始まっていない丸薬の**上**に色が出た）。
- **`--pass` を、時間ではない高さへ貼らないこと。** `--p`
  （＝`--pass × 100%`）が掛かる相手は、その要素自身の高さ。使ってよいのは
  高さが時間そのものである二つ——**空きの行**と**丸薬**——だけ。
  手順の段の背骨（`.tl-sub-wrap::before`）に貼っていた時期があり、あそこの
  高さは**手順の件数**で決まるので、丸薬の下端より **62px 下**まで色が
  下りた（実測：6:30〜7:30 の用事を 6:49 に、手順4件をひらいて）。
  「線が丸薬を追い越す」の正体はこれ。あの段は丸薬の下端より下＝終わった
  あとの場所なので、境目を持たず `--rail-bot-c` の一色でよい。
- **空きの行の「過ぎたぶん」は、始まりで決める**（`--cat-a`）。終わりだけで
  決めていたので、夕方まで予定の無い日は、朝に済ませた丸薬の下に夜の青が
  数px 顔を出した。過ぎたぶんは上の丸薬から続く線なので、上と同じ色。
- **30分以内の空きは実線、それより空いたら点線**（`TL_JOIN_GAP`）。十分や
  そこらの隙間は「途切れ」ではなく、ひと続きの時間のなかの余白なので。
  点は6px・間は4px（前は7pxと8pxで、間のほうが長く点々の列に見えた）。
  破線は**マスクで抜く**——地の色と点々を一つの背景で兼ねると、過ぎたぶんと
  これからで色を変えられない。
- **重なった二つは、丸薬どうしがぶつかる**（`is-clash` ／ `is-clash-above`）。
  線でつながない——重なりは「順番に並んでいない」ことなので、つなぐと
  そこだけ嘘になる。上の行の丸薬は下へ、下の行の丸薬は上へ14pxずつ育て、
  めり込み18pxの真ん中に白い抜き（`.tl-row.is-clash .tl-rail::after`、
  30×12px の楕円）。**めり込みの18pxが決めごと**で、育てる量（14px）は
  行間（5+5=10px）から逆算した数——行の余白を詰めたら、ここも直すこと。
  **行は詰めない**——行の高さは題とメモが決めるので、詰めた瞬間に字が噛む。
  実測（丸薬56px／めり込み20px／抜き34×13px）を50px幅に縮めた寸法。
  **三つ以上重なったら、まん中の一件は両方向へ**
  （`.tl-row.is-clash.is-clash-above`、＋32px・位置は動かさない）。ここは
  一度こわした：上の二つは詳細度が同じなので、両方を持つ行にはあとに
  書いたほうだけが効き、まん中の丸薬が下へしか育たない。上の継ぎ目は
  めり込むどころか2px離れて、白い抜きだけが宙に浮いていた。
- **一画面に一日ぶんだけ。** 棚を縦に積むのはやめた（`daySection`）。
  日を変えるのは、週の帯を押す／`‹ ›`／左右に払う、の三つ。払うと**紙そのもの**
  が横に流れて、隣の日の時間割が指のぶんだけ入ってくる（`js/day-swipe.js`。
  daily・ダイエットと分け合う一つ）。
  用事を**週の帯の日へ運ぶ**と日付が変わる（棚またぎドラッグの後継）。
- **立ち上げたときは daily**（`app.js` の `HOME = "archive"`）。やること・
  買うものは用があるときに開く画面だが、daily は開かなければ書かれない
  ままになるので。試験の `tl.js` の `boot()` は やること までめくる
  （`stay: true` を渡すとめくらない——`tabs2` は立ち上げの面そのものを
  見にきているので）。**daily の画面も最初から DOM にいる**ので、
  `.cal-day` / `.tl-sheet` / `.js-now` のような両方にあるものを掴むときは、
  出ている画面に絞ること。
- **運んでいる最中、画面の端で自動的に送る**（`EDGE_BAND` 84px、端に近い
  ほど速い）。指は画面から出られないので、これが無いと画面の外にあるもの
  ——下に隠れた長期タスクの欄や、朝の時間帯——へは運べない。送ったあとは
  狙いを取り直すこと（中身が指の下で動いたので）。
- **左の列は、まるごと一本の時間軸。「置き場」という考え方は捨てた。**
  縦の座標が、そのまま15分きざみの時刻になる。丸薬の上でも、丸薬と丸薬の
  あいだでも、空きでも、同じ一本の軸の上の一点として読む——**そこに何が
  置いてあるかは、置けるかどうかに一切関わらない**。
  - **目盛りは丸薬に合わせる**（`axisOf` の `pts`）。節は丸薬（`.tl-node`）の
    上端＝その用事の始まり、下端＝終わり。あいだは前の終わりから次の始まりへ
    まっすぐ。実測で、丸薬の上端・中心・下端の**ずれは 0分**。
    - **`.todo-mark` を測らないこと。** あれは丸薬の中の32pxの絵で、4時間の
      用事も15分の用事も同じ32pxになる。丸薬そのものは `.tl-node`。
    - **まっすぐ一直線（列の上端＝日の始まり、下端＝終わり）は試して外した。**
      px あたりの分はどこでも同じになるが、行の高さは題とメモが決めていて
      時間に比例しないので、**丸薬の時刻と最大 327分ずれた**——「12:30
      旅行準備」の真横で札が「18:12」と出る。
    - 代わりに px あたりの分は場所で変わる（実測 0.30〜1.76分/px、ただし
      13:00〜22:00 のような**長い空きだけ 11.7分/px**）。置けない高さは無いが、
      空いた夕方は粗い。気になるなら直すのは**休みの行の高さ**のほうで、
      運んでいる最中に広げるのではない（それが次の項）。
  - **破線の帯（`.tl-band`）は廃止。** 掴んだ瞬間に空きの行が 64〜220px へ
    広がり、その中だけが細かく狙えた＝「置ける場所」を、そこに何が置いて
    あるかが決めていた。広がるぶん（実測：帯5つで490px）行が押し下がるので、
    **狙っていた先が指の下から逃げる**。運んでいるあいだ、行は一切動かさない。
  - **狙いの線は一枚の層**（`.tl-aim`）に置く。行に仕込むと、線は必ず行の
    境目へ吸い寄せられる。実測で、線は指の高さと 0px ずれ。
  - **左右の境目は行の列の切れ目**（`grid-template-columns: 44px 56px 1fr`
    の 100px ＝ `.tl-item` の左端）。前はここが `.tl-rail` の右端＝**73px**
    だった——レールは2pxの線なので、その右端で切ると**題を掴んだ指はいつも
    外側**にいて、実測 x=170 では y 150〜780 の全点で時刻が取れなかった
    （＝毎回「並び順」に落ちていた。動画のトーストが全部「動かしました」
    だったのはこれ）。
  - **運んでいるあいだ、軸の幅にうすい地を敷く**（`.tl-aim::before`）。帯を
    外したぶん「どこが時計か」の手がかりが絵から消えた。「左＝時刻、右＝
    並び順」は位置で言っていることなので、その位置が見えていないと言えて
    いない。
  - **狙っている時刻の札は、線の上へ逃がす**（`.tl-band-time`、線から30px）。
    線と同じ高さに置くと、そこは指が乗っているところ——いちばん読みたい
    数字が指の腹でちょうど隠れる。実測で 18px では 4px かぶった（指の腹は
    触れた点からおよそ24px 外へ広がる）。列のいちばん上だけは上に場所が
    ないので下へ返す（`is-under`、`AIM_LABEL_GAP`）。
    **札は掴んだ瞬間から出ている**——`lift()` の最後の `aim()` が列の中
    （x = 行の左端+20）を渡すので、指を動かす前から行き先が読める。
  - 重なってはいけない理由は無い——15分の隙間へ1時間の用事を置いたなら次と
    重なるが、それは利用者が決めたことで、重なりは丸薬どうしがぶつかる絵で
    ちゃんと見える。題のほうは「順番で置きなおす」まま（別のことを言って
    いるので）。
- **長期タスクの欄へ運ぶと、日付と時刻が外れる。** 探すのは節（`.tl-someday-sec`）
  のほう——中の `ul.tl-someday` は一件も無いと組まれないので、いちばん要る
  「空っぽの長期タスクへ最初の一件」が落とせない。
- **長期タスク → 時間割の落とし先は `.tl-list`。`.tl` ではない。**
  `.tl` は**二つの別物**に付いている——行を並べる `<ul class="tl tl-someday">`
  と、その日の時間割を囲む `<div class="tl">`。`.tl:not(.tl-someday)` が拾うのは
  後者の**囲い**で、中の `<ol class="tl-list">` ではない。囲いの子は `.tl-sum` と
  `<ol>` だけなので行を数えると 0 件になり、**長期タスクから運ぶと時刻が一度も
  取れなかった**（実測：列の真上 x=20 でも y 160〜710 の全点で null。帯だけは
  `querySelectorAll` で深く探していたので出ていて、「箱は見えるのに使えない」
  状態だった）。
- **済ませた時刻を、題の下に出す**（`.tl-doneat`）。左の時刻の列は**予定の
  時刻**で、別のもの。`doneAt` は UTC の ISO なので読むのは `getHours()`。
- **期限切れ・もっと先・済んだものは「一覧」が受け皿。**
  `settings.todoTimeline` オフで棚の積み上げに戻る。時間割の頭には
  期限切れがあることだけ出す（`.tl-late`）。
- **「いま」は太字の時刻ひとつ**（`.tl-now`）。点（`.tl-now-dot`）と右へ
  流れる線（`.tl-now-line`）は外した——塗りの境目がすでに「いま」を
  言っているので、重ねると三度同じことを言うことになる。残したのは、
  境目が**何時なのか**だけ（それは色では言えない）。丸薬が時間ぶん伸びる
  ようになったので、置き場所も行の**中**に取れる（`nowY` は丸薬の中を
  割る）。30秒ごとに軸の層（`.tl-axis` / `paintNow`）だけ描き直す。
  組み直しはしない。
  **用事の時刻とぶつかったら、いまの時刻のほうが逃げる**（`clearOfClocks`、
  17px）。用事の時刻は丸薬の頭に付いていて、そこを離れると何を指している
  のか言えない。いまの時刻は、境目そのものが色で見えているので近くに
  居れば足りる（「14:45」と「15:11」が重なった）。
- **済ませたものは色のまま・同じ大きさ。** 題の取り消し線と、塗りつぶ
  された印だけで見分ける。灰色にして縮めない。
- 用事の終わりの時刻は、時間割には出さない。**詳細の紙**に
  「7:00 〜 7:30　30分」と範囲で出す。
- 詳細の紙の頭（`hero`）は、押した行の絵の続き。地は行の色の82%暗いもの。
- **18時から先は夜の色**（`--c-night`、`NIGHT_FROM`）。毎晩（`part:"dusk"`）は
  時刻がなくても夜。見るのは `t.time` ではなく**組み立てが置いた時刻**
  （`it.atMin`）——時刻を決めていない用事も、夜に落ちれば夜なので。空きの
  行も、その空きの**終わり**が18時を過ぎていれば夜側（点線の途中で色を
  変えると、そこに何かがあるように見える）。参考画面もここだけ青い。
- **いま進んでいる用事**には `.is-live`。丸のまわりの輪（`--live` ＝
  始まりからの割合の conic-gradient）**は外した**——丸薬の中の塗りが
  同じことを、しかも伸びた形に沿って言えるので。残したのはうすい地
  （8%）だけ。字は足さない。済ませたものには出さない。30秒ごとに
  `markPass` が置き直す（軸と同じ拍）。
- **丸薬の中の絵は、丸薬と同じところで色が変わる**（`.todo-mark.is-split`）。
  過ぎたぶん（テーマ色に染まったところ）は**白抜き**、まだのぶん（灰色）は
  **テーマ色**。進んでいる最中は絵の途中で切り替わる。真っ黒にはしない。
  - 絵は `<svg>` ではなく**マスク**で置く（`screen-todo.js` の `tlMark`／
    `maskUrl`）。塗りは CSS のグラデーション一枚で、**丸薬の背景と
    まったく同じ式**（`--pass` と `--tl-node-h`）から出す。境目を二か所で
    計算すると、伸びた丸薬や重なった行で必ずずれる。実測で、ずれは 0px。
  - 絵は丸薬のまん中にいるので、絵の中の境目は
    `--pass × 丸薬の高さ − (丸薬の高さ − 絵の高さ)÷2`。`clamp` で
    0〜絵の高さに収める（境目が絵の外なら、全部白／全部色になる）。
  - **丸薬の実際の高さは `--tl-node-h` 一か所から**（重なりで育つぶんも
    含めて）。前は `height` を三か所に書いていて、詳細度の罠も抱えていた。
  - **単色シルエットは、品物の絵もマスクへ通す**（`tlMark` が iconsTodo →
    iconsGoods → iconsFood の順に引く）。通っていないと、その行だけ
    `<svg>` のまま置かれて**丸の中で黒いまま**になる——白抜きになるのは
    マスクに乗った絵だけ。色つきに戻したときだけ `<svg>` に落ちる。
- **暦の日の下の丸（`.cal-mark`）の中も白抜き**。丸がすでに棚の色に
  染まっているので、丸薬と同じ決めごと。ここは進み具合を持たないので
  いつも染まっている側。**色つきの品物の絵は白くしない**（色そのものが
  「何の品物か」を言うので）——ただし品物の絵は 2026年9月5日に単色へ
  切り替わったので、いまは**品物も白抜きになります**（色つきに戻したときだけ
  効く決めごとです）。
- **絵の入れもの（`.todo-mark`）は中央合わせの箱**（`display:grid`）。
  中の `.p-icon` は字と並ぶための下げ幅（`vertical-align:-.26em`）を
  持っていて、ただの `<span>` に入れると行の高さぶん絵が押し下げられる。
  暦の日の下の丸（`.cal-mark`、15px）では、11pxの絵が丸の**外へ**落ちて
  いた。買うものの行だけ正しく見えていたのは、そこが前から flex
  だったから。

### 手順（サブタスク）の印は、二つとも色。分けるのは形

済ませた手順は ✓、できなかった手順は −。**どちらも基調色（`--cat`）で塗り、
印は白抜き**にします。できなかったほうを地味な灰色にしていた時期がありますが、
それは「やらなかったことは薄い」と言っているのと同じで、この画面がずっと避けて
きた採点です（済ませた用事を灰色にして縮めないのと、まったく同じ理由）。色は
「もう手をつけ終わった」ことだけを言い、どちらに終わったかは**形**が言う。

**`.check svg` は既定で `opacity: 0`** で、それを 1 に戻す決めごとは
`[aria-checked="true"]` にしかありませんでした——つまり − は、描かれてはいたが
**一度も見えていません**でした（灰色の四角だけが出ていて、「できなかった」は
色でしか言えていなかった）。`[aria-checked="mixed"]` にも同じ一行が要ります。

**手順を並べ替えるのは、詳細の紙の中**（`.sub-line` の `.sub-grip`、四本線）。
`KN.reorder.attach` に `handle` を渡すと、**押した瞬間から**持ち上がります
——長押しを待つ作りはここでは使えません。字を書く欄が並んでいる段で、欄の上で
指を止めるのは「文字の位置を決める」手つきだからです。掴むための場所を別に
置いたぶん、待たせる理由も無くなりました。三本（`list`）ではなく四本にしたのは、
メニューの三本線と紛れないため。

- 並べ替えても **store には触れません**。控えの配列（`subs`）を並べ替えて
  描き直すだけで、保存は紙の「保存」が引き受けます——**書く**のと**やる**を
  分ける、この紙の決めごとです。
- 打った字は `value` **属性**にも書き戻すこと。運んでいるあいだの控え
  （`reorder.js` の ghost）は `cloneNode` で作るので、属性を更新しないと
  控えだけが打つ前の字を出します。
- 持ち上がった行には影が付きます（`reorder.js` の決めごと）。この段の行は地を
  持っていないので、`--c-sheet` を敷いておくこと——さもないと影だけが宙に浮きます。

### 長期タスクと、期限（`deadline`）

時間割の下に、少し離して「長期タスク」の欄があります（`somedaySection`）。
**`due` を持たない用事**がここに集まります——やると決めているが、いつやるかは
決めていないもの。

- **時刻も、時刻の線も出しません。** 時刻が無いのだから、書けば嘘になります。
  時刻の列は `visibility: hidden` で**空けたまま**（詰めると丸の位置が上の
  時間割とずれて、運べることが伝わらない）。
- ここから時間割へ運べます。`wireDrag` は同じものを使い、`tlDrag.dayList` が
  落とし先を持ちます——長期タスクの欄には空きの行が無いので、空きを開くのも
  狙いを探すのも**その日の時間割のほう**です。
- **欄の中でも並べ替えられます**（`kind: "someday-order"`）。行の上で離すと
  その前後へ。欄の余白（行の無いところ）で離したら何もしません。
- 並びは**手で決めたもの**（`order`）です。期限の近い順には**しません**
  ——並べ替えられるようにした以上、並びの持ち主は一つ（`order`）にします。
  期限は字で見えているので、そちらまで並びを決めると同じことを二か所で
  言うことになります。急ぐものを上に置きたければ、運べば済みます。
- 詳細の紙は時間割の行と**同じもの**が開きます（`itemRow` を使い回して、
  時刻の列だけ空ける）。

`deadline` は `due` と**別の欄**です。`due` は「その日の時間割に居る」＝いつ
やるか、`deadline` は いつまでか。同じ欄で兼ねると、締め切りを書いたとたんに
その日の時間割へ入ってしまいます。持っていない古い記録は null に落ちるので、
`reconcile()` はそのまま通ります。詳細の紙では「時刻と長さ」の下、長期タスクの
行では題のすぐ下（過ぎたものは字で「◯日すぎています」——色だけだと、色の意味を
知っている人にしか伝わらない）。

### くり返しは、先の日にも立つ（`store.fallsOn`）

くり返しの用事は記録としては一件で、`due` は「次にやる日」です。素直に
`due === day` で数えると、毎日のはずのものが一日にしか出ません。`fallsOn` は
**出す／出さないを答えるだけ**で、記録は増やしません——日ごとの控えを作ると、
済ませた印をどちらに付けるかで二重管理になります（Daily Log を「写さず引く」
ことにしたのと同じ理由）。今日から先だけ広げます。過ぎた日には済ませた跡
（trace）がもう並んでいて、そこへ生きている一件を重ねると二行になるので。

くり返しの選択肢から**毎朝・毎晩は外してあります**（時刻が書けるので、毎日との
二通りができていた）。**すでにある記録には手を付けていません**——`part:
"dawn"/"dusk"` も、それを見ている並べ替え・夜の色・`plan.js` もそのままです。

### 外枠と、その中を流れる中身（丸角が消えないための作り）

**送る器は、画面ではなく紙です**（やること・daily・買うもの）。

前はひとつでした——`.screen` が送る器で、紙（`.tl-sheet`）はその中のふつうの
ブロック。だから下へ送ると、丸角のついた紙の上端はそのまま上へ流れて消えます
（実測：送り 270px で紙の上端が **-140px**）。手当てとして掴み手に sticky と
同じ丸角を着せていましたが、掴み手の地も紙の地も同じ白（当時どちらも実測
rgb(255,255,255)。いまは 253 ——下の「紙の地は 253」参照）なので、角を落とした
隙間から覗くのは紙自身の白
——**丸角は最初から見えていませんでした**。しかも流れてくる行がその隙間を
通る、という禁じ手そのものの構造でした。

外枠（丸角＋掴み手）と、中身を送る器を分けます：**紙が器になり**、上のバー・
探す欄・暦・カテゴリの札は紙の外に留まる。中身は紙の中だけを流れます。
実測：送り 600px でも紙の上端も掴み手も **130px から動かない**。丸角の外側の
画素は地の色（240,239,243）、内側は白。

- **ダイエット・価格・設定は、これまでどおり画面が送ります。** ダイエットの紙は
  `is-bare` で丸角を持たない（隠れる角がない）うえ、あの画面だけは引いて更新と
  キーボードの送りが画面そのものの `scrollTop` に乗っています。価格と設定は紙を
  持ちません。
- **器がどちらかは `KN.app.scrollerOf()` が `overflow` を見て答えます。**
  決め打ちにしないこと——`root.scrollTop` と書いてある古い場所が残っていると、
  そこだけ動かない画面を触りにいきます。直した先：`is-stuck` の見張り
  （todo/archive/list）、組み直しの位置を保つところ、「いま」へ寄せるところ、
  運んでいる最中の自動送り、`cal-peek` の送りの覚え書き。
- 高さの割り当ては `.stack` → `.js-body`/`.arc-body` → `.tl-sheet` と
  `flex:1; min-height:0` で通します。途中が欠けると紙は中身ぶんに伸びて、
  器になりません。
- 下の帯のぶんの余白は、**紙の中の `padding-bottom`** が持ちます（器の外に
  置いても、中身の下には空きができない）。画面から紙を引き出していた負の
  マージンは要らなくなりました。
- 掴み手の sticky は `top: 0`（紙の中で貼りつくので、上のバーや暦の厚みを
  数える必要がない）。
- `overscroll-behavior` は紙に付けていません——ブラウザ本来の跳ね返りが
  そのまま効くので、`pull-refresh` の帯（fling）が要りません。

#### 紙の地は 250、ふちは純白 255

`--c-sheet`（`#fafafa`）と `--c-sheet-edge`（`#ffffff`）。紙（`.tl-sheet`）も
下から出る紙（`.sheet`）も、地はこの一段下げた白。

**ふちの純白の一本は、前から引いてありました**（`.tl-grip` の
`box-shadow: inset 0 1px 0`）。地も純白だったので、**一度も見えていません**
でした——白の上に白を引いていた。地を下げると、その一本が「紙のいちばん上で
光が当たっている」ふちとして立ちます。

**落ち影ではなく inset**（内側の一本）なので、紙が浮いて見えるようにはなりま
せん。`border-radius` が box-shadow にも掛かるので、丸角にもそのまま沿います。
暗いほうは逆に、地より**明るい**灰（`#33333c`）でなぞる——暗い面に純白を引くと、
線だけが浮き上がって紙のふちではなく別の部品に見えます。

**掴み手の地は、紙の地とまったく同じにすること**（貼りついたまま行がその下を
通るので、少しでも違うと帯に見えます）。

**紙の上に乗る帯も、白は `--c-surface`（純白）ではなく `--c-sheet` を見る
こと。** daily の月のまとめ・Daily Log・積み上げの一覧（`.card.arc-log` /
`.card.arc-counts` / `.card.arc-list`）は `.card` の既定（純白）のままだと、
紙の地を下げた瞬間だけ**純白の段として浮いて**見える——地の白と紙の白が、
目には同じでも数字としては違うので。ダイエットの三つの帯（`.diet-block` の
`is-body` / `is-meal` / `is-weight`）も同じ理由で、混ぜる相手を
`--c-surface` ではなく `--c-sheet` にしてある（ダイエットの紙は
`.tl-sheet.is-bare` で面を持たないので、この三つが実質の紙）。
**一つの変数（`--c-sheet`）から出すこと**——紙の色を動かすたびに、これらも
ついてくる。

### 紙を横に払うと、日が動く（三画面共通・`js/day-swipe.js`）

やること・daily・ダイエットは、どれも**一日ぶんの紙**を出しています。隣の日へ
行く道は暦にもありますが、「昨日はどうだったか」を見るのに毎回上まで戻るのは
遠い。紙そのものを払えば、日が動きます。仕掛けは `js/day-swipe.js` が持ちます
——**三画面が同じ一つを分け合います**（書き写すと、片方だけを直した日に三つの
紙が違う動きをします）。画面が答えるのは四つだけ：いま見ている日は何か
（`day`）、その隣はどこか（`step`、行けないなら null）、その日の紙をどう組むか
（`slide`）、決まったら何をするか（`commit`）。

- **動くのは紙そのもの。** やること・daily は、指を離してから中身を差し替えて
  22px ぶん横から入れていました（`.tl-sheet.is-from-right`）——払っている最中は
  何も動かないので、**どこまで行けば送られるのかが絵に出ていません**でした。
  いまはダイエットと同じで、隣の日の本物の紙が指のぶんだけ連続して入ってきます。
- **隣の二枚は、横だと決まった瞬間（5px）に組んで、戻ってきたら片づける。**
  ダイエットは三枚を最初から並べていましたが、あの画面は一日ぶんが軽いので
  通っていただけです。やることの時間割を毎回三日ぶん組むと、store が動くたびに
  三倍の絵を捨てて組み直すことになります。組んだ直後に transform を書くので、
  一拍も見えません。**ふだんの DOM には、今日の紙しか居ません**——ここは大事な
  ところで、居残ると `.todo-day .tl-list` や `.tl-now` のような「一枚しか
  無いはず」を探している側が、隣の日のほうを掴みます。
- 器は `.day-car`（`overflow: hidden` ＋ `touch-action: pan-y`）、中身は
  `.day-track` に並ぶ `.day-slide`。既定の transform は**素のまま**（一枚しか
  居ないので）。行けない側は中身の無い一枚（`.is-void`）が場所だけ取ります
  ——真ん中を -100% に置くための足場なので、無いと位置がずれます。
- **daily の段の間（`gap: var(--sp-8)`）は `.day-slide` が持つ。** 中身が紙から
  一枚下へ移ったので、余白の役目もそこへ移します——移さないと六つの段が隙間なく
  積み上がります。
- 行けない向き（daily・ダイエットの「今日より先」）は 0.3倍に重くします。
  やることは先の日へも行けます（これから何をするか組む画面なので）。
- **待っているあいだの長押しの見張りは `document` で。** `wireDrag` は一覧
  （`.tl-list`）に付けていましたが、払いはじめると day-swipe が**外枠**で
  ポインタを捕まえます（`setPointerCapture`）——捕まえた先は一覧より外なので、
  以後の `pointermove` も `pointerup` もそこまで降りてきません。見張りが黙ると
  長押しの時計は止まらず、**払い終わったあとに行が持ち上がりました**。
  `reorder.js` も同じ作り（document）です。
- 用事を運んでいる最中（`tlDrag` / `KN.reorder.isActive()`）は取りません
  ——`busy()` がそれ。掴んでいるあいだは `lock()` で組み直しを止めます。

### 暦を横に払う（三画面共通・`js/cal-swipe.js`）

月めくりと週送りの手つき。**三画面が同じ一つを分け合う**（前は
`wireMonthSwipe` を三つに書き写していた）。画面が答えるのは五つだけ：
週だけ出しているか（`isWeek`）、いま見ている日は何か（`here`）、週を送った
先はどこか（`step`、行けないなら null）、その月の日のマスはどれか
（`monthGrid`）、決まったら何をするか（`go`）。

- **月と週で、動くものが違う。** 月ぜんぶのときは、これまでどおり盤ごと
  横へずらして薄くする（月は一枚の紙なので、押して送るのが素直。隣の月を
  先に組んでも、ほとんどは目に入らないまま捨てることになる）。
  **週のときは本物を先に並べる**——指のぶんだけ隣の週が連続して入ってくる。
  動くのは一行で、その行の中身は「先週の日にち」というはっきりした行き先
  なので、どこまで行けば送られるのかが絵に出る（day-swipe と同じ考えかた）。
- **真ん中は、生きている `.cal-slide` をそのまま運ぶ。** 組み直した写しを
  置くと、選んでいる日の輪（`.cal-ring`。枠ごとに描かず一つだけ置いて
  滑らせるもの）が払っているあいだだけ消える。片づけるときに元へ戻すので、
  いた場所（親と次の兄弟）を覚えておくこと。
- **隣の週のマスは、行き先の月の盤から抜く。** 週は月をまたぐので、着いた
  先の暦から見て隣の月の日は薄い字（`.is-out`）になる。いまの月の盤から
  抜くと、**払っているあいだだけ濃くて、指を離すと薄くなる**。
  盤は `monthGrid(y, m)` が返す——いま出している月なら生きている盤を
  そのまま、そうでなければ `fillCalendar(sec, {year, month})` で組む。
- **`fillCalendar` に月を渡したら、最後の二つ（週の印と輪）は置かない。**
  離れたところへ組むぶんは画面に出ないので、置くと出ていない盤のために
  生きている題まで塗り直すことになる。
- **先に見せる二枚から `data-day` を外すこと。** あの二枚は `.cal` の中に
  居るので、付けたままだと `cal.querySelector('.cal-day[data-day=…]')` で
  探している側（輪を置く `paintHere`、週の外に印を付ける `tagOffWeek`）が、
  生きている盤より先にこちらを掴む——輪が別の週へ飛び、先に見せている週が
  「週の外」と判定されて消える。絵として要るのは見た目だけ。
- 隣の二枚は clone なので押せない（`inert`）。押す相手は、着いてから本物に
  戻る。ふだんの DOM には**いまの週しか居ない**（day-swipe と同じ理由）。
- 行けない側（daily・health の「今日より先の週」）は中身の無い一枚
  （`.is-void`）で場所だけ取り、指は 0.3倍に重くする。やることは先の週へも
  行ける（これから何をするか組む画面なので）。
- 払っているあいだ（`KN.calSwipe.isActive()`）は cal-peek に渡さない
  ——同じ盤を二つが取り合うと、運んでいる最中に高さまで動く。

### 暦は、紙を引くと出てくる（やること／daily 共通）

仕掛けは `js/cal-peek.js` にある。**やることと daily が同じものを使う**
（書き写さない——片方だけ直した日に、二つの暦が違う動きをするので）。
画面側が答えるのは「何が『いま見ている日』か」「いつ引いてよいか」だけ。

暦（`.cal`）は三層：曜日の行（`.cal-wds`）／伸び縮みする窓（`.cal-clip`）／
その中のずらし（`.cal-slide`）＋日のマス（`.cal-grid`）。
**縦の transform は `.cal-slide`、横は `.cal-grid`**（月めくりの
`wireMonthSwipe` が横を持っているので、同じ要素に二つは書けない）。
`.cal-wd` は `.cal-grid` の外に出した——伸ばしても曜日が動かないように。
組むのは `KN.calPeek.mount(sec)`。**やること・daily・ダイエットの三つとも
この組み**（日付の題／三層／掴み手／引いて開く）。daily の暦だけは、日を
選んでいなければ題が「8月」まで（月ぶんを出す画面なので）。

開き具合は数**ひとつ**（`--cal-p`：0＝週、1＝月）に集約してある。窓の高さ、
ずらし、隣の週の濃さ、題の右の「›」の傾きが、すべてこれから出る。途中で
指を離しても全部が同じところにいる、というのがこの作りの理由。

- **どのスクロール位置からでも引ける。** 前は「いちばん上にいるときだけ」
  だった（`root.scrollTop > 0` で降りていた）。掴み手はどこまで送っても上に
  居る（sticky）ので、そこを掴んだのに何も起きないのは筋が通らない。
- **動かすのは暦の高さ。紙はそのぶんだけ上下する。** 数（p）から出るのは
  `visibleH` ひとつで、窓の高さ・曜日の行・上下の余白・**掴み手の貼りつく床
  （`--cal-h`）**が、すべてそこから毎フレーム出る。紙の上端（丸角＋掴み手）が
  いつも暦のすぐ下に来るのは、床が同じ数から出ているからで、実測で ±1px。
  - **紙のほうを transform で動かす作りは、試して外した。** 暦を月の姿で
    留めて紙を平行移動させれば組み直しが要らない、という話だったが、それには
    紙が暦の**上**を通らなければならない（上へ押して暦を消すのは「紙が覆う」
    ことなので）。ところが紙の箱は画面の上へはみ出している——送った先では
    紙の頭は viewport のずっと上だ。上に乗せた瞬間、**紙の地が暦を丸ごと
    隠した**（実測：ダイエットで下まで送ってから引くと、暦が一度も見えない）。
    紙のはみ出しを切るには clip-path を毎フレーム計算することになり、高さを
    書き換えるより高くつく。**暦が前、紙はその下を流れる**——この順が正。
- **引きはじめたら、送りを覚えて戻す。** 測るために隠していた週をいったん
  出すので、そこでブラウザの「読んでいた場所を保つ」が働き、伸びたぶんだけ
  画面が黙って送られる（実測 196px＝月と週の差ぶん、指の下で跳ねた）。
  `begin` で `scrollTop` を控え、正しい高さを書いたあとに戻す。収まった
  あと（`done`）も同じところへ。`.cal { overflow-anchor: none }` では
  止まらない（目印は紙の中にあるので）。
- **`fitCalH` は、引いているあいだ書き換えない。** 引くと暦の高さが毎フレーム
  変わって ResizeObserver が鳴る。床は `cal-peek` が持っているので、
  `.cal.is-peek` / `.is-settling` のあいだは画面側が黙る。
- **health（ダイエット）で貼りつかないのは、月だけ。週の一行は貼りつく**
  （`#screen-diet .cal.is-week`）。「下にあるのは一日ぶんの記録で、月とは
  関係がない」という理由は**五、六行ぶんの話**で、一行には当たらない。
  やること・daily では週の帯がずっと上に居るのに、この画面だけ送ると
  消えていた——同じ暦が画面によって違う顔をすることになるうえ、消えたあとの
  戻り道が掴み手（引いて開く）しかなく、「いつの週を見ているか」が分からない
  まま記録を読むことになる。貼りついた印（`is-stuck`）は暦にも付けること
  ——付けないと境目の線が出ないまま記録の字が下をくぐる。
  引いているあいだは、月でも貼りつく（`#screen-diet .cal.is-peek`）。
  下まで送った先で掴み手を引いたとき、暦が画面の外に居ては出てくるものがない。
  収めたあと、**開くほうへ行ったときだけ上まで戻す**——せっかく開いた月が
  指を離したとたんに見えなくなるので。畳んだときは読んでいたところに居させる。
- 引く向きは片道ずつ。閉じているとき＝**下**、開いているとき＝**上**。
  逆はスクロールに渡す。

### 送る器を変えたら、pull-refresh にも教えること

`pull-refresh.js` は `.screen.is-active` を測っていました。紙が器になった
三画面では画面が送れないので `scrollHeight === clientHeight`——**上端にも
下端にも同時に居る**ことになり、縦の指を毎回「下端の give」として取って
`preventDefault` します。**時間割が一切スクロールできなくなっていました**
（実測：touchmove 7回すべてが既定阻止、送り 0。黙らせると 275px 送れた）。
`KN.app.scrollerOf()` を通すこと。

### 「下に引いて更新」は、ダイエットだけ

あそこだけは引くことに用がある——中継所に預けたヘルスケアの記録を取りに行く
道が、これしかないので。ほかの三つには取りに行く先が無く、一覧の頭で下へ
動かすたびに画面が伸びるのは、ただの邪魔だった（`pull-refresh.js` の
`canRefresh`）。

**止めたのは「下へ引く」ほうだけ**。下端の give（`edge = "bottom"`）と、勢いの
ついた指がリストの端を越えたときの帯（`onScroll` → `fling`）はそのまま
——あちらは更新ではなく、ブラウザ本来の跳ね返りを返しているだけで、無くすと
一覧が壁に当たったように止まる。

実測（下へ140px引いて）：daily・やること・買うもの は面が **0px**・輪も出ない。
ダイエットだけ **67px** 下がって輪が出る。
- **段を替えられるのは掴み手（`.tl-grip`）からだけ。** 紙のどこを持っても
  下へ引けば暦が出る、という作りにしていた時期があるが、**戻した**
  ——読んでいて下を見ようとするたびに暦が伸びてきて使いづらい。途中で
  離すと中途半端に開いたまま残るのも、これが原因だった。掴み手は
  `touch-action: none` で、見えている棒（5px）より広く**上下12pxずつ**
  （29px）取ってある。
- **掴み手は sticky。** スクロールしても居なくならない。貼りつく高さは
  `calc(var(--topbar-h) + var(--cal-h))` ——上のバーも暦も sticky で上に
  居るので、数えないと掴み手はその裏へ潜って消える（実際そうなっていた）。
  暦は組み直しのたびに別の要素になるので、そのつど測り直して見張る相手も
  付け替える（`fitCalH`）。**ダイエットは `--topbar-h` を誰も測っていな
  かった**ので、この画面だけ床が 0 で、下まで送ると掴み手が上のバーの裏に
  潜って掴めなかった（＝段を替えられなかった）。いまは `fitTop` が測る。
- **「引いて更新」に譲る印（`data-pull-own`）は掴み手だけに付ける。** 紙
  ぜんぶに付けていた時期があり、そのあいだダイエットでは引いて更新がまるごと
  効かなかった。段を替えられるのが掴み手だけになったいま、紙の本体は
  引いて更新のもの。
- 取ると決める前でも、掴み手にいてその向きへ行けるあいだは `touchmove` を
  渡さない——送るのを止められるのは `pointermove` ではなく `touchmove` のほう。
- 指を離したら、行き先まで滑らせて**から** `setCalPref` に書く。先に書くと
  暦が組み直されて、途中の姿から跳ぶ。
- `settlePeek` の途中で選択中の字を放す。マウスで引くと字が選ばれ、次に
  その上から引いたときブラウザが「選んだ字を運ぶ」と判断して
  `pointercancel` を投げてくる。
- 週で見ているあいだ、紙は `data-pull-own` を持つ。`pull-refresh.js` は
  この印の中から始まった手つきを拾わない（同じ指を二つが取ると、暦が
  伸びながら画面ごと下がる）。**月が出ているあいだは印を外す**ので、
  下向きは「引いて更新」に戻る。

### 紙を閉じると、書きかけは**保存される**（`ui.js` の `tryClose`）

×・下へ払う・外を押す・Escape——どれも「やめる」の合図だが、**書いたものを
捨てる合図ではない**。`guard: true` の紙は、閉じようとした時点で中身が変わって
いれば、**保存のボタンをそのまま押す**。聞かない——保存のボタンがそこにあって、
押せる状態で、中身が変わっているなら、答えはもう決まっている。

聞くのは、**押しても保存が通らなかったとき**だけ（体重が空、のような検算で
止まる紙）。どの保存も最後に `handle.close()` を呼ぶので、**閉じたかどうか**が
そのまま「保存できたか」の返事になる。閉じていなければ一度だけ聞く
——「保存しない」を選ぶ道が無いと、直せない欄を抱えた紙から出られなくなる。
`confirm` は流された（外を押した・Escape）ときに `false` を返すので、
**「捨てる」ほうを ok に置く**（false ＝ 何もしない＝紙は開いたまま、が安全側）。

**見張る範囲は紙ぜんぶ。** `.sheet-body` の中しか数えていなかったが、
**やることの題の欄は頭（hero）にいる**——題を打ち替えただけだと「何も変わって
いない」と判定され、下へ払うと**黙って捨てられていた**。欄がどの段にあるかは、
書いた人にとって何の意味も持たない。数えないのは帯（footer）の中だけ
（押せば決まるもの）。

Escape も `tryClose` を通す（`handle.tryClose`）。閉じ方が四つあるなら、
四つとも同じ扱いにする。

### 設定は、目次と、その中の二段

一枚に全部を並べていた（「表示」の次にお店が10行、そのあとカテゴリが9行）。
設定そのものと、設定の中の一件一件が同じ高さで並ぶのは、層が無いのと同じ。

- **目次**（`renderIndex`）… 小さな灰色の見出し（`.set-head`）＋角の丸い
  カード（`.set-card`）。行は［色の四角（`.set-tile`）］［名前］［いまの値］
  ［›］。上の帯は出さない（下のタブが「設定」と言っている）ので、ノッチの
  ぶんは `.set-head:first-child` の余白が持つ。
- **その中**（`renderPage`）… 押した一つぶんだけ。上の帯に題（まん中）と
  戻る「‹」。中身は**これまでの group() をそのまま**置いているだけ。
  帯と同じ字の `.section-title` は JS が落とす（「お店 3件」のように帯と
  違うことを言う見出しは残す）。
- 行き先は `PAGES` の一つの配列にある。足すときはここへ一行。
- **四角の色は基調色と別の軸**（からだの輪と同じ理由）。どれも白い絵が
  3:1 以上で乗る濃さにしてある。ここの白抜きは、手描きの品物の絵ではなく
  一本線のUIの絵なので、上の「白抜きにしない」には当たらない。
- **開いたら、その画面の設定から**（`onEnter` → `PAGE_OF_TAB`）。全部を目次
  から選ばせていた時期がある。設定が帯の四つめだったころの理屈（「押した人は
  『設定へ行く』と言ったのであって、どこから押したかは何も言っていない」）
  だが、**いま設定に席は無く、開く口はその画面の右上の歯車だけ**。やることを
  見ながらそこを押した指は「この画面のことを直したい」と言っている。それを
  目次で受けて四つの中からもう一度選ばせるのは、いま言われたことを聞き返して
  いるのと同じ。目次は**戻るを一度押せば出てくる場所**になった（他のタブの
  設定へは、そこから）。価格は席を持たない画面なので、その席——買うもの——の
  中を開く。
- **「データ」の段は無くした。** 「バックアップと書き出し」の一行のために
  見出しを一枚立てていた。段の名前は、そこに二つ以上並んで初めて「まとめて
  いる」と言える。外観と同じ**「一般」**の中へ（どちらもアプリぜんぶの話で、
  画面ごとの話ではない）。
- **目次の下のイラスト・名乗りは外した**（`aboutBlock`）。かごの絵と
  「くらしノート」「データはこの端末の中だけに」「ホーム画面に追加すると…」の
  四行が、行き先の列の下に付いていた。探しものの列が名乗りで終わると、目次が
  「行き先の一覧」であることがそこで途切れる。名乗りは立ち上げの面が済ませて
  いるし、保存先の話はバックアップの中でもう一度、必要な文脈と一緒に出てくる。

### 画面は、帯と同じ向きへ流れる

daily → やること と押したなら、画面も左へ流れて次の面が右から入ってくる。
並びは**下の帯の並び**（`app.js` の `SLIDE`）。出ていく面は流れ終わるまで
残す（`is-leaving`）——消してから動かしても、動くものが無い。`.screens` に
`overflow: hidden` を足してあるのは、外へはみ出したぶんを見せないため。

**買うもの ⇄ 価格 は横へ流さない。** あれは横に並んだ二つのタブではなく、
**買うものの後ろに価格がいる**という重なりなので、動くのは紙だけ
（`--face-p`、上のバーは動かない）。横へ流すと「横から来た」に見えて、
後ろに居たことが伝わらない。

### 買うものの紙と、その後ろの地としての価格

**紙は一枚だけです。** 買うものの紙が一枚あり、その後ろにあるのは価格の
**地**——札も一覧も、面に乗らずに並んでいるだけ。掴み手を下げると紙が
**指について**下がり、後ろの地が露出します（`KN.app.wireFaceGrip`、
`role: "front"`）。

**前は二枚ありました。それが崩れの原因でした。** `screen-prices.js` も
買うものと同じ `.tl-sheet` ＋ `.tl-grip` を組んでいたので、めくっている
最中に**丸角の白い紙と掴み手が二組**そろって出ていました（実測：背面の紙も
掴み手も top 42、前面のものと同時に見えていた）。「乗っている」を、二枚の
ページを重ねて片方をずらすことで表そうとしていた、ということです。
**丸角と掴み手を持てるのは、前に居る一枚だけ**——それが「乗っている」の
意味なので。価格の側は `.prices-ground`（面なし・丸角なし・掴み手なし）。

- 価格の行は、もともと地の上で読めるように作られていた
  （`.product-wrap:not(.is-tile-wrap) .product` は `background: none`、
  区切りは上の細い線、絵の丸の地は `color-mix(--cat 16%, var(--c-bg))`
  ——**地の色を混ぜてある**）。白い紙は、そこに要らないものだった。
- **動くのは紙だけ。二つの帯——題も、カテゴリの札も——動かない。**
  札の帯も紙と一緒に下げていた（「札は紙の連れだから」）。実機で見ると
  **帯までが指について降りてくる**——紙が下がるのではなく、画面ごと崩れて
  いくように見える。この二つはどちらか一方の面の持ちものではなく、
  **器の側の帯**：位置はいつも同じで、変わるのは**中身だけ**。
- **帯の中身は、じわっと入れ替わる。順に、重ねずに。**
  前の札と題が薄れきってから（p .04→.30）、後ろのが出てくる（p .30→.62）。
  クロスフェードを外した理由（日本語は字が詰まっていて、二語が重なると
  別の字に読める。実測「買うもの」＋「価格」が一塊）は生きているので、
  **重ねない**。決まり目で切り替える階段はやめたが、同時には見せない。
  **札と題は同じ拍・同じ区間**にすること。
  - 地は**後ろの札が持つ**（`background`、薄めない）。前を薄めただけだと、
    消えきった一瞬に地も無くなって、札の帯に価格の行が透ける。
  - 離した先へ滑るあいだは、濃さにも `transition` を掛ける（`is-face-settle`
    を**二枚とも**に付ける）。指で引くあいだは `--face-p` が毎フレーム動く
    ので要らないが、離すと数が一度に跳んで、紙だけが滑って題と札はぱっと
    切り替わった。
- 数はひとつ（`--face-p`、0〜1）。紙の位置も題と札の入れ替わりも、そこから
  出る。**書くのは `#screens`**（前の面ではない）——カスタムプロパティは
  書いた要素から下へしか伝わらないので、前の面に書くと**後ろの面から読め
  ない**。後ろの札もこの数を読むので、二枚の親まで上げる。
  道のりは `--face-d`（紙の頭から、下の帯の上まで）で、**取ると決めた時に
  一度だけ**測る——途中で測り直すと指の下で速さが変わる（暦の `span` と
  同じ決めごと）。指の換算にも同じ数を使うこと。
- **測るのは掴み手**（紙の箱の上端ではない）。掴み手は sticky でいつも
  見えている「頭」そのものなので、一覧を下へ送ってあっても同じところに
  留まる。紙の箱の上端は、送った先では画面の外にいる。
- 後ろの面は**自分の上の一組——バー・探す欄・カテゴリの札——をまとめて
  伏せる**（`visibility: hidden`。`display:none` にすると、伏せたぶんだけ
  後ろの一覧が上がって、着いた瞬間に下へ跳ぶ）。そこには前の面の同じ三つが
  居るので、二枚重ねると字が重なる。
  **札が抜けていた。** 伏せていたのはバーと探す欄だけで、札はそのまま出て
  いた。前の札は紙の連れなので紙と一緒に下がり、空いた同じ場所へ後ろの札が
  居残る——実測で、前の札が top 300、後ろの札が top 42 に並んでいた
  （＝**二段の札**）。札は「その面の上の一組」の一部なので、バーと探す欄と
  同じときに伏せるのが正しい。
- 前の面は**地を透かす**（`background: none`）。そのぶん、地を持っていな
  かった**探す欄とカテゴリの札には地を敷く**——敷かないと、札のあいだから
  後ろの一覧の行が覗く（実測：札の隙間に価格側の牛乳の絵が出ていた）。
- 行き先は 0（買うものが全面）か 1（下がりきって価格が全面）。途中で離したら
  近いほうへ滑る（`FACE_DONE` = 0.26）。着いてから `show(..., "settled")`
  ——「呼んだ側がもう動かし終えた」の合図で、ここで重ねて動かすと指で置いた
  ところから跳ねます。
- **題は量ではなく名前なので、途中の姿を持たない。** 決まり目（0.26）で
  ぱっと入れ替える（`.face-t`、`clamp` を1000倍して階段にする）。薄めて
  重ねるのも縦に送るのも試したが、どちらも途中で二語が同時に見えた
  ——日本語は字が詰まっているので、重なると別の字に読める（実測：
  「買うもの」と「価格」が一塊になった）。
- **価格はタブではない。** 帯は四つ（daily／やること／買うもの／ダイエット）
  で、ふた面の点も無い。帯が言うのはその席の名前で、価格を見ているあいだも
  「買うもの」のまま——いま居るのは買うもののタブで、その紙を下げているだけ
  なので。`TABS` の `holds: ["prices"]` が「この席が受け持つ画面」。
  帯を押したら**紙を戻す**（`KN.app.faceTo(0)`）——差し替えではなく。
- **掴み手は、押しても効く**（`role="button"` / Enter / 空白）。引くのが
  本筋だが、そこにしか道が無いと、指で引ける人しか価格へ行けない。暦の段と
  違って、あちらにはこの画面でしか見られない中身（商品・値段・店）がある。
  やること・daily・ダイエットの掴み手は**引くだけのまま**（あちらは押しても
  何も起きない）。
- **価格に着いたら、買うものの紙は頭だけ残って留まる**（`is-face-parked`）。
  下げきったところで紙を画面から出しきると、**指で戻る道がどこにも無く
  なる**——価格は地なので掴み手を持てないから。のぞかせる量は掴み手ぶん
  （`--face-peek`、app.js が実測して書く。数字を二か所に書かない）。
  実測：紙 top 750／掴み手 750〜779／下の帯の上端 779。
  - **中身はそのまま持たせる。** 一度 `max-height` で頭だけに切った
    ——すりガラスの帯を通して買うものの行が透けるのが気になったので。
    **やめた**：あれは紙が下へ滑っていっただけで、下に隠れているのは本当に
    その紙の続き。切ると、頭は「紙の頭」ではなくそこに貼られた棒になる。
    （切っていたころは、`overflow: hidden` が紙そのものを「送る器」に変えて
    sticky の基準が画面から紙へ移り、掴み手が 750 のはずが 793＝下の帯の裏へ
    落ちた。切らないなら、その手当ても要らない。）
  - 触れるのは掴み手だけ（面は `pointer-events: none`）。紙の本体は帯の下に
    隠れているので、触れるようにすると帯ごしに買うものの行を押せてしまう。
- **掴み手には `data-pull-own` を付ける。** これが無いと、下へ引いた指を
  「引いて更新」も一緒に取ります。あちらは `.screen` ごと平行移動させる
  （`pull-refresh.js` の `screenEl.style.transform`）ので、**上のバーと
  カテゴリの札まで指について降りてきます**——紙が下がるのではなく、画面ごと
  崩れていくように見える。やること・daily・ダイエットの掴み手には前から
  付いていて、**買うものだけ抜けていました**。
  裏取り（実測、150px 引いて）：印あり → 題帯 -1／札帯 42／transform none。
  印を外す → 題帯 **67**／札帯 **110**／transform 67.77px。
- **掴み手は一つで往復する。** 向きは `opts.role` ではなく**いま紙がどこに
  居るか**（`faceAt()`）で決まる：上に居れば下へ、留まっていれば上へ。
  役目を固定していたころは、下ろしたあとの掴み手が「下へしか行けない」まま
  で戻り道が塞がっていた。名札も state で言い直す（`syncFaceGrips`）。
- **買うもの・価格から離れるときは、留まった紙を片づける**（`show()` の
  `faceUnpark()`）。のぞかせた頭は「この後ろに買うものがある」という札なので、
  daily やダイエットの上に残っていては嘘になる。

カテゴリの札は、**買うもの側は手持ちのぶんだけ・価格側は全部**。これは
もともとそうなっていた（`renderFilter` が数えている相手が違う）。

紙は画面の下まで届かせる（`#screen-list .tl-sheet` が下の余白を引き受ける）
——途中で切れると、その下の地が「まだ何かある」に見える。**価格の地には
この手当ては要らない**（器の `padding-bottom` がすでに帯のぶんを持っている
ので、`.prices-ground` は `flex: 1` だけ）。

### daily は、一日ぶん

Daily Log は**暦で選んでいる日の一日ぶんだけ**を出す（`settings.dailyScope`、
既定は `"day"`。設定で `"month"` にすると月ぜんぶを縦に並べる）。月ぜんぶを
並べていたが、書いた日が増えるほど今日の一行は下へ流れていく——毎日開くのは
今日を書くためなのに、今日がだんだん遠くなる作りだった。日を移すのは暦の役目。

- まだ何も無い日は、**空のまま一行出す**。無いことを言うためではなく、押せば
  書けるところがそこに要るから。「押して、この日のことを書く」（`is-blank`）。
  その日は起床・就寝も帳簿（作成・更新）も出さない——書いていないので。
- **月のまとめは、日を絞っていても出す。** Daily Log が一日ぶんになったいま、
  この一段だけが月の話をする。
- **線は引かない。** 一段ごとに上下へ線を入れていて、一画面に六本並んでいた。
  分かれていることは**余白のほうが言う**（`.tl-sheet.is-daily` の gap、16→32px）。
- **紙を左右に払うと、月ではなく前日／翌日**（`goDayTo` ／ `js/day-swipe.js`）。紙に出ているのは
  「その日」なので。月は上の題を押せば選べる。先の日へは行かない。
- **本文は 16px の両端揃え**（`text-align: justify`）。日本語は字幅が揃って
  いるので、右端がそろうと一枚の紙の面として落ち着く。右の余白は左より広く
  ——本文が縁に触れると、読み終わりが窮屈になる。
- **字は一段ずつ大きい**（見出し 14→16px、日の数字 14→19px）。
  小さい字はそれ自体が静かな画面の作りかたではあるが、毎日読むものとしては
  小さすぎた。静けさは字の小ささではなく、**空きの多さ**で持たせる。
- 「今日を書く」ボタンは外した——すぐ下の行が押せば開くもので、＋の中にも
  同じ口がある。同じことへ三つ目の入口を作ると見出しが重くなるだけ。
- 積み上げの札は**あるものだけ**。五種類ぜんぶを always 出していたので、
  一件も書いていない月でも六つの丸が並び、その下は空だった（中身より操作の
  ほうが多い画面）。並べ替えとお気に入りも、並べる相手がいるときだけ。

### 帯は四つ、右上は二つ

- **下の帯**（`app.js` の `TABS`）… daily／やること／買うもの／ダイエット
  の**四つだけ**。価格は席を持たない（買うものの紙の後ろに敷いた一枚で、
  そこへは掴み手で行く）。`holds` が「その席が受け持つ画面」で、価格を
  見ているあいだも帯は「買うもの」のまま。
- **設定はタブではない**（`OFF_BAR = ["settings"]`）。場所ではなく、直したい
  ときに開くもの。どのタブの右上の歯車からも開き、`KN.app.backScreen()` で
  呼んだ画面へ帰る。設定の帯の「‹」は二段ぶん働く（中→目次→呼んだ画面）。
- **右上は二つだけ**——さがす と 設定。**どの画面でも同じ二つ**。
  外したものの行き先：
  - 並べ方（タイル／リスト）→ 設定の **外観**（`settings.layout` は三画面が
    分け合う一つの札なので、画面ごとではなくここ）
  - 暦の出し入れ → 設定の やること／daily／ダイエット（`calRow()`）
  - daily の月の書き出し・ダイエットの取り込み → もともと設定にあった
  - 価格の「◯商品・◯店舗」→ 設定の 買うもの（`countsRow()`）
- **上の帯は 43px**（前は55px）。上下の詰めは `--sp-1`（4px）、帯の中の
  `.icon-btn` だけ34px（**指の的は44pxのまま**——`.icon-btn::after` が持つ）。

### 暦は三段（暦なし／週／月）

`--cal-p` は 0〜1 のまま（週→月）だが、`cal-peek.js` の中の `p` は
**-1〜1** に広がった。-1＝暦なし、0＝週、1＝月。

- 週から**上へ押せば暦が消え**、そこから下へ引けば戻る。月への上げ下げは
  これまでどおり。上へ押すのは掴み手（`.tl-grip`）からだけ。
- 畳むのは日のマスだけでは足りない。**曜日の行（`.cal-wds`）と暦の上下の
  余白も**一緒に畳む（`weekSpan`）——さもないと消したはずの場所に
  「日 月 火…」と余白だけが残る。
- しまってある暦も、引いているあいだは場所を取る
  （`.cal.is-hidden.is-peek { display: block }`）。ここが無いと三段目から
  戻る道が始められない。
- `commit` は `{shown, open}` を受ける（`store.setCalPref` の二つの札）。
- 段のあいだの道のりは、**取ると決めた時に一度だけ**決める（`span`）。
  途中で測り直すと、境目をまたぐたびに指の下で速さが変わる。

### やること・daily・ダイエットは、上が同じ

**題はひとつの部品**（`KN.util.dayTitleBar` / `paintDayTitleInto`）。三画面が
これを借りる——書式を三か所に書き写すと、片方だけ直した日に三つの題が違う顔を
する。画面が答えるのは「ここでの『いま見ている日』は何か」だけ。

書式は「2026年9月8日(火)」。**曜日まで書く**——日付だけだと、その日が
何曜だったかを数えないと分からない。予定を組むときに要るのは、たいてい曜日の
ほう。

**括弧は半角。** 全角の `（火）` は前後に見えない余白を抱えていて実測 3文字ぶん、
半角なら 1.6文字ぶん。浮いた 1.4文字ぶんが右の「今日へ戻る」に回る。
**実機で括弧が上下にずれて見えたら、`dayTitleParts` の一行を全角へ戻すこと**
——ASCIIの括弧はラテン字の中心に合わせて描かれているので、漢字と並べたときの
座りは書体しだい（iOS では括弧が SF Pro、漢字が Hiragino と**別の書体から**
来るので、ここは実機でしか確かめられない。コンテナには Hiragino も SF も無い）。

色を当てる相手は二つ。**年（`.day-y`）はいつも差し色**、**日にちの数
（`.day-d`）は当日のときだけ差し色**（`.is-today`）。参考画面が暦の帯で今日の
数だけ色を変えているのと同じ理屈で、「開いているのが今日かどうか」を字の色
そのものが言う。残り（`.day-pre` / `.day-post`）は黒。

**大きさは、どの日を見ていても同じ。** 字はいつも一つの `clamp` だけ。
実測（49日 × 320/375/390/428px）で、字の大きさ・題の高さ・帯の高さ・題の
左端が**どれも一通りしか無い**こと。

**題の右は「今日へ戻る」**（`.topbar-today`、`icon("back")`）。**`›` は外した**
——`--cal-p` で回して暦の開き具合を言っていたが、daily では題を押すと月を選ぶ
紙が開く（暦の開閉ではない）ので、あそこでは回る意味が無かった。やること・
health では効いていたが、開いているかどうかは暦そのものの高さがもう言っている。

- **今日を見ているあいだは隠す**（`hidden`）。前に外した同名の札は当日でない
  ときだけ出て、そのぶん**題の字**を一段落としていた——今日とそれ以外で題の
  大きさが変わって見えた原因はそれ。ここが違うのは、**題の幅は vw基準の
  clamp で、この矢の有無とは無関係**なこと。矢を隠しても、動くのは矢の右に
  ある探す・設定の位置だけで、題の字も帯の高さも動かない
  （実測 49日 × 320/375/390/428px で確認済み）。
- **向きが二つある。** 未来の日から戻るのは時間を遡る＝左向き
  （`arrow-u-up-left`）、過去の日から戻るのは時間を進める＝右向き
  （`arrow-u-up-right`）。**絵は左向き一つだけ持ち、右向きは CSS の鏡映し**
  （`.is-flip` → `transform: scaleX(-1)`）——`arrow-u-up-right` はこの絵の
  ちょうど鏡像なので、絵を二つ持つ必要がない。今日は向きが決まらないので
  隠す、というのが上の一行の理由でもある。
- **絵は Bold weight。** 他の UI アイコンは全部 Regular（256の升目に16＝
  24pxに直すと1.5px相当）だが、ここだけ目立たせたいという指示で Bold へ
  ——`.check` の ✓ と同じ、weight の例外。
- 絵は 17px（前の `›` と同じ）、指の的は 44px（`::after`。`.icon-btn` と
  同じ手で、幅を増やさずに的だけ広げる）。
- **絵は `arrow-u-up-left`（Bold、`back`）。** `arrow-arc-left`（弧を描く矢）
  や `undo`（＝`arrow-counter-clockwise`、ほぼ一周する矢）も検討したが、
  後者は「もう一度読み込む」に読める——health には本物の引いて更新が居る
  ので、隣に置くと紛れる。
- 置き場所は `›` の跡地、つまり**題のすぐ右**（題の幅ぶん動く）。実測 320px の
  いちばん長い日付（2026年10月22日(木)）でも、右の二つとのすき間は 27px。
- 今日へ戻る道は暦にもあるが、今日から離れる道が増えた（暦を払う・紙を払う・
  暦の日を押す）のに対して、戻る道は「暦から今日のマスを探して押す」しか
  無かった。今日が出ていない週まで行っていると、その一手が二手三手になる。

暦の見出しの行・`‹ ›`・「週／月」の札は、どの画面にも無い（ぜんぶ題に吸収した）。
暦の下は紙（`.tl-sheet`）で、掴み手つき。紙を横に払うと、やること・daily は
**日**が動く。

**daily の「日を選んでいなければ月まで」は廃止した。** 月ぜんぶを縦に並べて
いたころの決めごとで、紙が一日ぶんになったいま、題だけ月を言うと**紙と題が
違うことを言う**。日を選んでいないときに紙が出しているのは `focusDay()` で、
そこをそのまま題にする。

**幅の実測**（曜日が加わったぶん）：`clamp(16px, 5.4vw, 23px)`。もとの
`clamp(18px, 6.4vw, 26px)` のままだと「（火）」の3文字ぶんで、実測 320px の
画面で19px・375pxでも1px、字が欠ける。

**暦の下に「今日へ」「今月へ」の札を出していたのは、無くした。復活させない。**
隠れているあいだは場所を取らないボタンだったが（`.cal-now` / `.js-now`、暦の
一段いちばん下に追記）、出た瞬間だけ暦の高さがそのぶん伸びて、**下の紙が押し
下げられていた**——他の日・他の月を見に行くたびに、紙の位置が動いて見えた
正体はこれ。今日へ戻る道は、いまは**題の右の `.topbar-today`**（上を参照）。
あれが良いのは、**暦の外・上のバーの中に居る**から——出たり消えたりしても
（今日は隠れる）、動かす高さは暦にも紙にも無い。壊れていたのは「暦の中で
高さを持つもの」が出入りすることで、場所そのものが違う。

**daily の題を押すと、月を選ぶ紙が出る**（`openMonthPicker`）。前はここで
暦の開き具合を切り替えていたが、それは紙を引く手つきがすでに持っていて、
同じことを二か所でやっていた。題が言っているのは年と月なので、押したときに
応えるのは「ほかの月へ」。まだ来ていない月は選べない。やること側の題は
これまでどおり（あちらは日ぶんを出す画面なので、月を選ぶ意味がない）。

### 色

`--c-primary` と `--c-primary-fill` は**役目が違う**。字に使うのは
`--c-primary`（`#bf4f49`、4.7:1）のほうで、39か所が字の色として参照して
いるので、ここを塗りの色にすると画面が読めなくなる。

**塗り（`--c-primary-fill`）は参考画面（Structured）と同じ `#f49e98`。**
白の上で 2.06:1。濃さは 4.28 → 3.10 → 2.72 → 2.06 と下げてきた終点で、
**白抜きはそのぶん薄くなる**（丸薬の中の絵と、暦の日の下の丸）。上げるなら
この一行だけ——`--c-primary` は字の色なので、そちらを塗りに使わないこと。

（以下は経緯）**塗りは実測の `#f2938e` から `#d34c45` へ濃くした（2.25:1 → 4.28:1）。**
丸薬の中を白抜きにした以上、この色は「白が乗る地」になった——2.25:1 では
白抜きが消える。夜の色（`#55799f`、4.54:1）と釣り合う濃さに揃えてある。
丸薬の地も **42%混ぜをやめてべた塗り**（`--tl-fill`）。混ぜていたのは中の絵が
色つきの手描きだったころの話で、いまは中が白抜きなので濃いほうが読める。

設定の基調色の札の id は `"orange"` のままだが、中身はコーラル。保存済みの
値を書き換えずに色だけ入れ替えるため。

上のバーと暦は**すりガラスにしない**（下を白い紙が流れるので、透かすと
行の字が重なる）。

### 絵は三系統。混ぜない

- **UIアイコン**（＋・歯車・★…）は **Phosphor Icons の Regular**。
  `icon("gear")` で引く。名前→絵は `js/icons.js` が取り次ぎ、絵は
  `js/icons-phosphor.js` にある。**画面はどの一族かを知らない**ので、
  入れ替えは `KN.icons.use("legacy")` の一行。手描きの55個は
  `js/icons-legacy.js` に**消さずに**置いてある。
- **商品アイコン**（708個の品物の絵）は、**単色シルエットに寄せました**
  （2026年9月5日）。道具など439キーが `js/icons-goods.js` の97型＋個別14枚、
  食材269キーが `js/icons-food.js` の71型。**キーは一文字も変えていない**ので、
  保存済みの `t.icon` も、キーワードの表も動きません。
  引く順番は `store.js` の `markOf()` ＝ **iconsGoods → iconsFood → 色つき**。
  **色つきに戻すのは `markOf()` の2行を消すだけ**（`icons-v2.js` は残してある）。
  **色は黒ではなく、カテゴリの色**で塗ります（`--cat` に字の色を15%混ぜたもの。
  `--cat` のままだと丸の地との明暗比が 2.2 前後で、線でできた絵が薄れる）。
  絵の入る丸はすでにカテゴリ色（16%）なので、**地と絵で同じことを言う**ため。
  暦の日の丸だけは白抜きのまま。経緯と物差しは `ICON-SILHOUETTE.md`。
- **「こと」アイコン**（108個の**行いと場所**の絵）は `js/icons-todo.js`。
  単色のシルエット。内訳は **Phosphor Fill 90／よそのセット 14／手描き 4**
  （次の節）。手描き108枚は `js/icons-todo-hand.js` に**消さずに**置いてあり、
  `KN.iconsTodo.use("hand")` の一行で戻る（`icons-legacy.js` と同じ扱い）。
- **食材のシルエット**（71型）は `js/icons-food.js`。いまは**どの画面でも**
  品物の絵の代わりに出ます（前は やること の中だけでした）。

決めごと：
- Phosphor の絵は**面**なので、CSS の `stroke-width` は効かない。太さは
  weight が決める。**weight の例外は `.check` の ✓ だけ**（白抜き15pxで
  Regular だと消えかけるので Bold）。
- 塗りは svg ではなく **path** に書く。画面に残っている
  `svg { fill:none }` / `svg { stroke:… }` に消されたり太らされたりしないため。
- 体重計（`scale`）だけ Phosphor に適切な代替が無く、手描きのまま
  （`scales` は天びんで、買うものの「比べる」と紛れる）。
- 経緯と A/B/C/D の対応表は `ICON-PHOSPHOR.md`。

### 「こと」アイコン（`js/icons-todo.js`）— やることは品物では引けない

**やること は「こと」を先に、買うもの は「品物」を先に**引く
（`screen-todo.js` の `iconMarkHtml`）。**順番が要点**——「洗濯する」は
洗濯機であってほしいが、「洗濯洗剤」はボトルであってほしい。同じ辞書を
同じ順で引くと、どちらか一方しか立たない。測定の経緯・数字は `ICON-TODO.md`。

決めごと：

- **名前には `do-` を付ける**（`RAW` の素の名前に、`ICONS` を組むところで機械的に）。
  接頭辞なしだと品物名とぶつかる（bed・camera・tv・mug・bicycle など）。
  ぶつかると `t.icon` に保存済みの絵が黙って入れ替わる。品物が増えても
  二度と当たらないように、接頭辞で分けてある。
- **保存済みの名前は両方に聞く**（`iconsTodo.byKey(k) || productIcons.byKey(k)`）。
  昔の記録は品物の名前しか持っていないので。
- **viewBox は `-8 -8 272 272`**。絵は Phosphor Fill の文法どおり 24〜232
  （256の81%）に描くが、品物の絵は 3〜21（24の75%）。同じ器に並べると
  こちらだけ大きく見えるので、器のほうを76%に広げて合わせてある。
  **絵の座標は動かさないこと**——動かすと、これから描くものが揃わなくなる。
- **色は `currentColor`**。丸薬の上では字の色、詳細の紙の頭（濃い地）では
  白抜きになる。テーマ色で塗ることもできる、というのがシルエットの取り分。
- 一覧に出す名前は `NAMES` で言い直す。引くための言葉は語幹で切ってある
  （「本を読」「振り込」）ので、そのままだと名前として読めない。
- **絵を足したら、実際に引かせて確かめること。** 目で見て良さそうでも
  取り違える（過去の事例は `ICON-TODO.md`）。
- **言葉の割り当ても、絵の慣習に合わせる。** `KEYS` は上から順に当たるので、
  ある語をAからBへ付け替えるときは、Aのほうを先に外さないと効かない。

### 新しい絵が要るとき（順番）

**いま入っている絵のうち76型が「想像描き」**（食材71・道具4・こと1）で、これを
消していく計画が `ICON-DEIMAGINE.md` にある。作業台の表と、採否を決める二つの門
（`core ≥ 0.45` と 既存71型との距離 `≥ 0.18`）はそちら。**セットで置き換えると
25pxで崩れる型が多い**ことも実測済みなので、食材を触る前に必ず読むこと。

1. **まず確立したセットを探す。描くのは最後の手段。** 想像で描き直すと、
   直すたびに骨格がずれて別物になる。`npm pack` で取れる
   （proxyの`noProxy`にnpmがあるので通る。phosphoricons.comやjsdelivrは403）。
   手持ちの候補：Phosphor Fill（MIT）、Material Symbols rounded fill
   （Apache 2.0）、Tabler filled（MIT）、Remix Icon fill（Apache 2.0）、
   Bootstrap Icons fill（MIT）。
   - Phosphor 由来は `P(d)` を使い、**`fill-rule` を付けない**（Phosphorは
     穴を逆回りで描くnonzeroの作り。evenoddにすると重なりが打ち消されて崩れる）。
   - よそのセットは viewBox がばらばらなので `F()`（`icons-todo.js`）で
     正規化する——外枠（`getBBox`）を実測し、長辺216・中心128,128に合わせて
     `<g transform>` を被せる。数字はスクリプトが出すもので手でいじらない。
   - **崩れにくさ（core）が低くても、実物で読めれば採用する。** 線ベースの
     絵は必ずcoreが低く出るが、それは読めないことと同義ではない。丸薬32px・
     暦11px（3倍の実機画素）で実際に撮って確かめること。
2. **セットに無ければ、描く前に参考画像をもらう。** その束（20型ぐらい）を
   見終わってから、描くことになった絵を一覧にしてまとめてもらう。想像で
   描くと形が壊れる（実例は `ICON-TODO.md`）。
3. **もらった画像は、手で写さずに「なぞる」こと。** 白黒化→輪郭抽出→
   Douglas–Peuckerで簡略化→長辺208・中心128,128へ写す（scratchの
   `trace.js`、ε=1.2px）。閉じた輪はいちばん遠い点で割ってからRDPに掛ける
   （そのまま掛けると2点に潰れる）。手描きの絵は `S(...)` で従来どおり
   evenodd。
   - 手で写すのは、なぞれない場合だけ。そのときは**割合**（丈に対する幅・
     ひもの長さ・位置）を写す。**線の太さだけは写せない**——参考画像の
     輪郭線はうちの升目で0.3pxにしかならず靄になる。線は抜きに変えるか
     16以上に太らせる。忠実に写すほど溶けるので、骨格を写して寸法は
     25px側へ寄せる。

### 25pxで崩れないための描画ルール

崩れにくさ（`core`：25pxに描いて1px侵食したあとに残るインクの割合。低いほど
「線でできた絵」）を測って直した経験則。新しく描くときはここを守る。

- **枠だけ抜くと消える。** 画面・パネルは塗ったまま、蝶番・アンテナ・受話口
  のような**外の形**で言わせる。
- **棒を板・骨組みに重ねない。** 接するところまでにする（重ねるとevenodd
  相殺で1pxの帯に割れる）。
- **窓・仕切りは一枚〜数本まで。** 多いと柱・仕切りが1px未満になる
  （こと用は仕切り1枚まで、食材の房の仕切りは3本まで）。
- **小さな抜きを並べすぎない。** 数を減らして大きくする。
- **細長い抜き（麺など）は本数を絞る。** 抜きは2本まで、束にするなら3本まで。
- **茎など細い部位は42以上の太さを取る。**
- **縦の棒ものは一族が混みあう。** きゅうり・にんじん・注射器・歯ブラシの
  ように縦長の輪郭は近づきやすいので、横に寝かせて離す。
- **線は太さ2.0以上か、描かない。** 目盛り・細い脚・針金は数を減らして面にする。

実測ログ（こと用の12事例・食材用の事例）は `ICON-TODO.md` /
`ICON-SILHOUETTE.md` を参照。

### `fill-rule="evenodd"` は、重なりを打ち消す

- **union は一筆で描く。** 二つの形を重ねて一つにしない。
- **接するのは可**（皿の上辺と盛りの下辺を同じyにする）。1でも重ねると
  そこに白い線が出る。
- **抜きは、抜く相手の中に完全に収める。** はみ出すと輪郭が欠ける
  （せんべいの「ひとくち」はそれを**わざと**使っている）。
- **抜きどうしを交差させない**（チョコの格子は、横帯のあいだにだけ縦を置く）。

### かな2文字の言葉は、短い名前のときだけ当てる

`product-icons.js` の `findKey` / `icons-todo.js` の `findKey` の共通の決めごと。
かな2文字（「ぱす」等）は品物の名を成さないので、**名前が4文字までのときだけ**
当てる（品物の名は短く、用事の文は長いので、その差で切る）。漢字は2文字でも
名を成す（洗剤・牛乳）ので、そのまま当てる。かな2文字の言葉自体は残す
（「あめ」「のり」「ねぎ」等74個。「焼きのり」「長ねぎ」に当たらなくなる
ほうが失うものが大きい）。実例は `ICON-TODO.md`。

### カタカナの連なりは、後ろが主役（`KN.util.hasWord`）

`findKey` は長いあいだ「文字列に含まれるか」だけを見ていた。ひらがな・漢字の
混ざる文はそこで語の切れ目が見えるが、**カタカナ（とラテン字）を続けて書いた
ところは切れ目が見えない**——「タコス」の中に「たこ」が、「ペットボトル」の中に
「ぺっと」が、「オレンジジュース」の中に「おれんじ」が居る。実測で、蛸・犬猫・
みかんの絵がそれぞれ出ていた。

判定は `KN.util.foldRuns` ／ `KN.util.hasWord` の**一組だけ**（買うものの辞書と
「こと」の辞書が分け合う。二か所に書くと片方だけ直した日に割れる）。`foldRuns`
は `foldKana` と同じ文字列に、**同じ長さの字の種類の並び**を添えて返す
——カタカナ→ひらがなの置き換えは一字が一字なので位置がずれず、折りたたむ前の
種類を折りたたんだあとの位置で読める。

決めごとは四行：

- 連なりの**終わりまで届く**当たりは採る。日本語のカタカナ語は後ろが主役
  （オレンジ**ジュース**はジュース、ポテト**サラダ**はサラダ、ミニ**トマト**は
  トマト）。
- 途中で切れている当たりは、**残りの後ろが辞書の言葉なら譲る**
  （オレンジ｜ジュース、チーズ｜ケーキ、レトルト｜カレー）。ただし
  **譲る相手には丈が要る**（`TAIL_SLACK`、下記）。
- 後ろが辞書に無いときだけ前半を採る。ただし連なりの**頭から**始まっている
  なら4字以上（`HEAD_MIN`。「コーヒー｜フィルター」は珈琲でよいが、「タコ｜ス」の
  たこは連なりの中でたまたま揃っただけ）。
- **途中から**始まっている当たりも、**5字以上**なら採る（`MID_MIN`）。頭に
  固定していたので、「一本満足**バー｜プロテイン｜ブラック**」の ぷろていん が
  落ちて、ぶ｜**らっく** が棚として出ていた。4字にすると「ノンア｜ルコール」の
  あるこーる が消毒液として立つので、ここだけ一段高い。

**位置を返す口は `KN.util.wordAt`。`hasWord` はその薄い衣。** 同じ語が二度出る文
では最初の当たりが落ちて二つめが採られることがあり、`indexOf` で取り直すと
**落ちたほうの場所**を見る。括弧の内外を見る側（下記）にはそれが効くので、
判定した本人に場所を言わせる。**判定は一か所だけ。**

#### 譲る相手には、丈が要る（`TAIL_SLACK`）

「譲る」を無条件にしていたころ、**7文字の頭が3文字の後ろに席を譲っていた**
——「カップヌードル｜チリトマト」がトマトに、「バープロテイン｜ブラック」が
棚になっていた。後ろが主役なのは**後ろが主名詞のとき**の話で、商品名のうしろに
味や色の名前が続く書き方はそうではない。

**ここは構造では分けられない。** 実測で確かめたこと：

| 同じ形（長い頭 ＋ 3文字の後ろ） | 正解 |
|---|---|
| カップヌードル(7) ｜ チリ**トマト**(3) | **頭**（cupNoodle） |
| ノンあるこーる(5) ｜ **ビール**(3) | **後ろ**（beer） |
| カマンベール(6) ｜ **チーズ**(3) | **頭**（camembert） |
| レトルト(4) ｜ **カレー**(3) | **後ろ**（curry） |

分かれ目は語そのものの性質——**修飾語か品物の名前か**——で、それは文字列の形の
どこにも書いていない。だから `hasWord` が引けるのは**丈だけ**：1字ぶんの遊び
（`TAIL_SLACK = 1`）を置いて、**後ろが自分より2字以上短ければ譲らない**。
実測で `とまと`(3) は `かっぷぬーどる`(7) に譲らず、`どりんく`(4) は
`よーぐると`(5) に譲る（ヨーグルトドリンクは飲み物のまま）。
残りは `product-icons.js` の `MODIFIER` が名指しで持つ。

#### 一つに絞る順位は四段（`findKey`）

`findKey` は**当たったものをその場で返さない**。`FLAT` は長さの降順なので
「いちばん長い当たり」が先に来るが、長さだけでは決まらないものが二つある。
**候補を集めてから順位で選ぶ**：

1. **括弧の外 ＞ 括弧の中**（`depthsOf`）。食事メモの
   「食パン8枚切り2枚**(チーズとポテサラとジャム)**」は、括弧の中が付け合わせの
   注記で、主役ではない。以前はここで `ちーず` が勝っていた。
2. **修飾語でない ＞ 修飾語**（`MODIFIER`）。れとると・あるこーる・冷凍・冷蔵・
   業務用。どれも中身の語より**長い**ので、長さだけだと包装や形態の絵が勝つ。
   **単独で書かれたときは競争相手がいない**ので、「レトルト」「アルコール」は
   これまでどおりその絵が出る。
3. **長いほう**（これまでと同じ）。
4. **辞書に書いた順**（これまでと同じ）。

**同点の決着を「文中で先」に代えるのは、試して外した。** 日本語の複合語は
**後ろが主名詞**なので、実測で「さば水煮**缶**」が水になる（缶と水がどちらも
1文字で同点、水のほうが先に出てくる）。先頭が主役なのは**列挙**のときだけで、
列挙かどうかは括弧と読点が言う——だから見るのは位置ではなく**括弧**。

**食事メモを「と」や括弧で割る案も、外した。** `screen-diet.js` の `slotMarks`
は行・読点で割って絵を最大5つ並べるが、そこをさらに割ると**1行から複数の絵**が
出る。**1行＝絵は一つ**にそろえる決めごとなので、括弧は「割る」のではなく
「**減点する**」（上の一段目）。なお「と」割りの危険は実測で小さかった
（生の表記339件のうち割れるのは「とんかつ」「とり天」の2件、どちらも語頭）
——外した理由は危険ではなく、**1行1つ**の決めごとのほう。

`suggest()`（もしかして、の一覧）は**これまでどおり広く拾う**——選ぶのは人なので、
候補は多いほうがよい。狭めるのは、黙って決めてしまう `findKey` のほうだけ。

### 当たり外れは `icon-eval.json` で数える

入力と、そこから出てほしい絵の名前の表。`want` が**空の配列なら「何も出さないのが
正解」**（間違った絵より、絵が無いほうがよい——これがこの表のいちばん大事な列）。
`gap: true` は「その絵は要るのに語彙が無い」もので、取りこぼしとして別に数える。
`screen` が引き方を決める（やること＝こと辞書→品物辞書、食事＝量を落としてから
品物辞書、買うもの＝品物辞書）。走らせる道具はテストと同じ扱いでスクラッチに置き、
コミットしない。

実測（2026年9月11日、155件）：**誤爆 10件(6.5%) → 3件(1.9%)、命中 92.3% →
95.5%**。残る3件はどれも語彙側の話（「不用品」がゴミ箱・「わっくす」が整髪料・
「まかろにさらだ」がポテトサラダ）で、引き方では直せない。

**段階C（2026年9月11日）で、この3件と gap:true の4件（タイヤ交換／タコスを
作る／ペットボトル／カレーパン）を片づけた——命中 **100%（155/155）**。**
- 「不用品」「まかろにさらだ」は、ユーザー確認のうえ**現状維持**と決め、
  `want` をいまの引き当たりに合わせた（ゴミ箱／ポテトサラダのままでよい）。
- 「わっくす」だけは除外を足した。`findKey` に `AVOID` という小さな表を
  設け、「わっくすがけ」「わっくすをかける」の形だけ hairWax を除外する
  （`js/product-icons.js`）。ひらがなの言葉は `hasWord` のカタカナ連なり
  判定が効かない（`cls` が 0 なので即マッチ）ので、Stage Aの仕組みでは
  この形の誤爆を直せなかった。
- 4件の gap は、それぞれ足りない語を一語足しただけ（`たいや交換`→
  carFront、`たこすを作`→cooking、`ぺっとぼとる`→water、`かれーぱん`→
  sweetBun）。

**引き方の作り直し（2026年9月11日）で 202件に増えた——命中 100%（202/202）。**
食事メモのように**複数の食べ物が1行に同居する入力**を、この表は一件も持って
いなかった（1行1品ばかりだったので、カレーライスが米になっても 99.5% 通った）。
足したのは報告された4件と、**直してはいけない対**のほう
——レトルトカレー・ノンアルコールビール・さば水煮缶・オレンジジュース・
ヨーグルトドリンク。あれは「カップヌードル｜チリトマト」と同じ形で答えが逆なので、
網に載せておかないと、次に順位を触った人が黙って壊す。

**回帰は、この表だけでは足りない。** 引き方を触ったら、**辞書の全キーワード**で
新旧を突き合わせること（品物2131語＋こと684語＋食品データ177件＋この表＝実測
5892通りの引き当て）。キーワードは `KN.util.hasWord` / `wordAt` を黙らせて
`findKey` を一度走らせれば、走査した語がぜんぶ手に入る。今回の作り直しで
変わったのは**9件だけ**で、全部が意図した改善だった——この数が二桁になったら、
どこかで別のものを壊している。

**この表を回す道具（node harness）は `js/icon-system.js` → `js/icons-v2.js`
→ `js/util.js` → `js/product-icons.js` → `js/icons-todo.js` →
`js/food-data.js` の順で読み込むこと。** `icons-v2.js` を抜かすと
`KN.iconsV2Keys`（`product-icons.js` が `KEYS.push(...KN.iconsV2Keys)` で
取り込む旧語彙——mail・movie・trashOut・seaweedWakame 等）が無いまま動くので、
実際のブラウザでは当たる4件が道具の中だけ外れて見える（段階Cでこれにハマった）。

### 自分だけの言い換えと、絵の報告（`iconOverrides` / `iconReports`）

買うもの・やることは記録そのものが `icon` 欄を持てる（`product.icon` /
`t.icon`）ので、絵を選ぶ紙は行の丸を押した瞬間に直接開けます
（`screen-list.js` の `.item-emoji`、品目の紙を経由しません。品目の紙の
中の「アイコン」欄からも同じ紙が開きます——道が二つあるだけです）。

**食事メモには、その入れ物がありません。** 自由記述の一文だから
（`store.slotMemo`）です。かわりに、**入力した文字列そのもの**を鍵にした
辞書 `state.iconOverrides`（畳むのは `KN.util.foldKana`）を持ち、
`product-icons.js` の `findKey` が辞書を引くより先にここへ聞きます
（`store.js` は `product-icons.js` より後に読み込まれるので、モジュール
評価時ではなく**呼び出された瞬間**に `KN.store` を読みます——`byKey` が
`iconsGoods`/`iconsFood` を呼び出し時に読むのと同じ理由。node の harness
は store.js を読み込まないので、`KN.store` が無ければ素通りします）。

- **買うもの・やることにも同じ経路が効きますが、実害はありません。**
  そちらは `product.icon`/`t.icon` のほうが `findKey` より手前で読まれる
  ので、すでに絵が決まっている記録はここまで来ません。決まっていない
  記録にだけ、控えめに効きます。
- **タップの単位は「その絵を出した一片」**（`screen-diet.js` の
  `slotMarks`——読点・改行などで割ったあとの一つ）。一行に複数の食べ物が
  同居していても、絵は一片につき一つで、タップすればその一片だけの
  言い換えを選べます（`openMealIconPicker`）。買うもの・やることの絵選び
  紙と**同じ組み**（探す・もしかして・ぜんぶ）を、書き込み先だけ変えて
  使い回します。
- **「おまかせにする」は値を消します**（キーごと `delete`）。空文字列を
  値に持たせると、「何も出さないのが正解」だったのか「まだ決めていない」
  だったのかが区別できなくなるためです。
- 上書きの絵をタップして直しても **store には触れません**（並べ替えの
  紙と同じ理由の言い方が違うだけで、ここでは書き込む先が違うだけです）。
  書き込みは `store.setIconOverride` の一回で、それが引き金になる
  画面の組み直しは `render()` の先頭の `flushSlots()` がすでに引き受けて
  います——だから枠の書きかけを消さずに済みます（**指の手つき**の節にある
  `saving` フラグと同じ心配りの列）。

**報告は、この端末に溜まるだけです。** 外と話す口は中継所URLだけで、
それは資格情報なので触れません——だから「この絵はちがう」を送る先は
どこにもありません。`state.iconReports`（`{id, text, screen, gotIcon,
createdAt}`）は、見返す・またはコピーして次のセッションに渡すための
場所です。食事の絵選び紙の「この絵はちがう、と設定に記録する」と、
設定の「アイコンについて」ページの手書き欄、二つの入口から足せます。

- **探す欄のすぐ下に置くこと。** 格子（「ぜんぶ」は700枚超）の**下**に
  置いていた時期があり、見つけてもらう前にスクロールで力尽きる位置
  でした。まず選ぶか、まず報告するかは同じ一手の距離にします。
- 設定のページは「絵が見つからない言葉」（`iconGaps`、自動で集める
  「絵が無い」）と同じ一枚に同居させます（`iconReportsGroup`）——どちらも
  「絵の悩みごと」なので、段を増やさずに見出しを二つにしました。
  ページの題は「絵が見つからない言葉」のままだと二つ目の見出しと合わなく
  なるので、「アイコンについて」に広げてあります。
- **中の一覧は、空でも同じ入れ物を保つこと**（`.js-rep-body`）。空の
  ときだけ要素ごと差し替える作りにしたら、次の repaint がもう外れた
  要素を触っていました——空/件ありは**中身**の出し入れで言うこと。
- コピーは JSON（`{screen, text, gotIcon}` の配列）。タブ区切りの
  「絵が見つからない言葉」と違うのは、渡す先での使い道が違うからです
  ——あちらは辞書に言葉を足す作業への入力、こちらは
  「この入力にはこの絵が出た、正しいのは何か」を考える作業への入力
  なので、`icon-eval.json` と同じ形に寄せてあります。

### 食材のシルエット71型（`js/icons-food.js`）

`icon-types.json` の71型を、「こと」の絵と同じ文法で描いたもの。丸いもの
（りんご・卵など）は輪郭に出っ張りを付けて描き分けてある。いまは**どの画面
でも**（買うもの・価格・やること・ダイエットぜんぶ）品物の絵の代わりに出る
——単色と色つきが混在すると、食材の行だけ薄れて見えて画面全体が持たない
ため（2026年9月5日切り替え）。絵を選ぶ紙も同じ差し替えを通す。
実測値は `ICON-SILHOUETTE.md`。

### 商品アイコンの画風は、定数4つが決めている

708枚を描き直さずに画風を動かせる場所が `js/icon-system.js` の頭にある。
「絵が古く見える」と感じたときは、まずここを疑うこと——絵の腕ではなく
画風の語彙のほうが古い、というのが実際だった。

- `GLOSS` … 丸いものの白い光沢。**0**（置かない）。`.55` に戻せば旧画風に戻る。
- `LINE_W` … 輪郭の太さ。**0.7**（元 1.0）。
- `LINE` … 輪郭の色。**`#8a7f74`**（元 `#4a423b`）。
- `C` の彩度 … 元の **0.72倍**、明度は `(1-l)` に比例して +4%。

**輪郭を 0 にしてはいけない。** 白いもの（たまご・牛乳・トイレットペーパー）
が白いカードに溶ける。輪郭は「白いものが消えない」ための仕事をしている。
明度の持ち上げは比例にすること（一律に足すと `paper` が白飛びする）。
`ink` / `inkD` は絵の暗部の錨なので触らない。

### 光学サイズは、実測して外れ値だけ引き戻す

`js/product-icons.js` の `FIT` 表。絵は描き直さず、`ICONS` を組んだ直後に
`<g transform="translate(12,12) scale(s) …">` を被せるだけ。物差しは
**箱の長辺とインク量の相乗平均**（どちらか一方だけだと外れ値を誤って拾う）。
中央値から±15%より外だけを0.90〜1.28倍で直す。

- **軸は 12,12**（絵の重心ではない）。いまの置き場所を保つため。
- **線の太さは倍率で割り戻す**（`LINE_W / s`）。拡げた絵だけ線が太くなると
  一貫性が崩れる。
- 拡げた結果が升目からはみ出すものは、出ないところで止める。

測り直しは Playwright で `getBBox()`（viewBox単位を返す）と canvas の
アルファ数え。データURIで画像化するときは `xmlns` を足すこと（`<img>`
経由だと無いと読めない）。実測結果は `ICON-TYPES.md`。

### 型に寄せる（`ICON-TYPES.md` / `icon-types.json`）

708個を「25px で形が残る単位」にまとめる作業。**絵はまだ一枚も消していない**
——あるのは割り当ての表だけ。

- **型は外形で切る**（幅・高さ・上の作り）。中で描き分けても、その差は 25px
  まで残らない。**色は型に縛られない**——同じ形でも、とまとは赤・ぴーまんは緑。
- **キーもキーワードも変えない。** 保存済みの `t.icon` はキー文字列なので、
  キーを変えると利用者の絵が外れる。変えるのは「どの絵を出すか」だけ。
- **効くのは食材と容器だけ。** 道具・家電・家具はほとんど減らない
  ——冷蔵庫と洗濯機を同じ形にはできないので。無理に寄せると意味が壊れる。
  詳しい実測値・見込みは `ICON-TYPES.md`。

### 崩れは「core」で測る。25pxで実質いくつの絵かも測れる

**目で決めずに測ること。** アイコンを触るときの物差しは二つある。

- **① core — 崩れにくさ。** 25px に描いて、1px 侵食したあとに残るインクの
  割合。細部が2px未満だと消えるので、そこが低い絵は「線でできている」絵。
  直しかたは一つ：**数を減らして太らせる。細い線は面にする。** 前に使って
  いた「中間アルファの割合」は単色の絵には通じない（べた塗りは輪郭が全部
  中間アルファになり、読めていても数字が高く出る）。
- **② 25pxでの形の距離 — 細分化しすぎていないか。** 12×12に落とした
  アルファを比べて、距離0.085未満なら「その大きさでは同じ絵」。同じ入れもの
  （ボトル等）は同じ形でよい——中身の違いは色が言う。

測り方は Playwright で canvas に描いて画素を数えるだけ。スクリプトは
セッションのスクラッチに置き、コミットしない（テストと同じ扱い）。
実測結果・過去の数値は `ICON-TYPES.md`。
