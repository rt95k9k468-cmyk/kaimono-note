# 想像描きを無くす（ICON-DEIMAGINE）

2026年9月9日 起草。

いまアプリに入っている絵のうち、**実物も参考画像も見ずに描いたもの＝「想像描き」が76型**あります。
これをゼロにするための計画と、その根拠になった実測です。

**目的は品質ではなく、由来です。** 実測すると、この76型は既製のアイコンセットより
**明確に強い**（下の計測を参照）。それでも消したいのは、`CLAUDE.md` が書いている
とおり「想像で描き直すと、直すたびに骨格がずれて別物になる」から。**次に誰かが
この絵を直そうとしたとき、拠り所が無い**、というのが問題の中身です。

だから、この作業のゴールは「絵を良くする」ことではありません。
**一枚ごとに「これは何を見て描いたか」が言える状態にする**ことです。

---

## 何が想像描きで、何がそうでないか

`icons-food.js` / `icons-goods.js` / `icons-todo.js` の中で、
絵の出所は関数で分かれています。

| 書きかた | 意味 | 想像描きか |
|---|---|---|
| `P(d)` | Phosphor Fill からそのまま | いいえ |
| `F(d, s, x, y)` | よそのセットから、`getBBox` 実測で正規化して借りた | いいえ |
| `S(...)` | 手で描いた | **どちらもある** |

問題は `S(...)` が二種類を抱えていることです。**参考画像をなぞったもの**と、
**想像で描いたもの**が同じ関数で書かれていて、区別はコメントの日本語にしかない。
前回のセッションで数え方が食い違ったのは、これが原因です。

### なぞったもの（11型・**対象外**）

`foldClothes` `dishWash` `school`（`icons-todo.js`）、
`cuttingBoard` `tissue` `trashBag` `toothbrush` `wrap` `detergentBottle`
`apron` `tapeRoll`（`icons-goods.js`）。

参考画像を白黒化→輪郭抽出→Douglas–Peucker（ε=1.2）→長辺208・中心128,128 で
写したもので、**骨格が実物から来ています**。これは想像描きではないので触りません。

### 想像描き（76型・**対象**）

- `js/icons-food.js` … **71型すべて**
- `js/icons-goods.js` … **4型**（`tube` `bar` `bag` `plate`）
- `js/icons-todo.js` … **1型**（`cityHall`）

---

## 実測 ── 「セットから探す」は、食材では通らない

`ICON-PHOSPHOR.md` / `ICON-SILHOUETTE.md` の「まずセットを探す。描くのは最後の
手段」は、道具まわりでは効きました（100型がセットから採れている）。
**食材で同じことをやると、通りません。** 三つの数字がそれを言っています。

### ① 既製セットは 25px で崩れる

物差しは `core`（25pxに描いて1px侵食したあと、残るインクの割合。低いほど
「線でできた絵」で、`ICON-TYPES.md` の危険域は 0.33未満）。

| 出所 | core 中央値 | 危険域（<0.33） |
|---|---|---|
| **いまの手描き76型** | **0.632** | 2 / 76 |
| Fluent Emoji High Contrast（MIT・1594枚）の食材候補59枚 | **0.230** | **43 / 59** |

うち14枚は `core 0.000` ── 25pxで跡形もなく消えます
（`potato` `cucumber` `egg` `croissant` `soap` `fish-cake-with-swirl` ほか）。

理由ははっきりしています。絵文字系のセットは64〜128pxで読ませる設計で、
内側に細い抜きを持ちます。この app の「**いちばん細いところで16以上**
（＝25pxで約1.04px）」という規則を、はじめから満たしていない。
**いまの手描きは、その規則を守って描かれているぶん強い。**

### ② 探しても無いものが9型ある

MIT / Apache 2.0 / CC0 の11セット・**約69,000枚**を横断して、
候補が一枚も出ない型：

```
とうふ  のり  つけもの  ふりかけ  まよねーず
よーぐると  水  ぐらたん  干ししいたけ
```

日本の食品棚は、UIアイコンセットの語彙の外にあります。探し方の問題ではありません。

### ③ 絵文字を平らにして影にすると、団子になる

セットから採れた最良候補どうしの**形の距離**（12×12のアルファ。0.085未満＝
同じ絵、0.14〜0.16で溶け合う）：

```
0.093  とまと / せんべい     0.114  くっきー / けーき
0.108  けーき / せんべい     0.118  はんばーぐ / くっきー
0.108  かまぼこ / せんべい   0.135  りんご / けーき
```

`icons-food.js` の頭に書いてある「**丸いもの9つが団子になった**」が、
そのまま再現しました。輪郭に出っ張り（葉・へた・切り口）を付けるという
いまの設計判断は、**測ると正しい**。

いまの71型の距離は、**最小 0.182・中央 0.402・0.16未満はゼロ組**（2485組）。
これが守るべき線です。

> **平たく言うと**：既製のアイコンは大きく表示する前提で作られているので、
> 25pxの丸薬に入れると細部が消えるか、あるいは細部を持たないぶん
> どれも同じ丸に見える、ということです。

---

## 方略 ── 「置き換え」ではなく「由来を付け直す」

目的が由来である以上、道は三つあります。上から順に試す。

| 段 | 手 | 通す条件 | 前例 |
|---|---|---|---|
| 1 | **セットから採る**（`F()`） | 二つの門を通ること（下記） | goods 100型 / todo 14型 |
| 2 | **参考画像をなぞる**（`trace.js`） | 同上。通らなければ「数を減らして太らせる」で直す | えぷろん・てーぷ・school |
| 3 | **型を畳んで消す** | 25pxで区別できないなら、そもそも描かなくてよい | 既に11型がこれで消えた |
| — | ~~想像で描く~~ | **これを0にする** | |

**(B) の目的（由来）で行く以上、主戦場は段1ではなく段2です。** ①②③のとおり
セットで置き換わるのは一部で、残りは「参考画像をもらってなぞり直す」ことに
なります。**絵はいまとよく似たものになりますが、骨格の出どころが変わる**
──それがこの作業の中身です。

### 二つの門

差し替えた絵は、必ずこの二つを通してから採用する。

1. **`core ≥ 0.45`** … いまの76型の最小が 0.318（`pasta`）、中央が 0.632。
   0.45 は「いまより明らかに弱いものを入れない」ための線。
   0.33未満（危険域）は問答無用で不採用。
2. **既存71型との距離 ≥ 0.18** … いまの最小が 0.182。ここを割ると、
   一族のどこかで二つの絵が溶け合う。

**どちらか一方だけでは足りません。** core だけ見ると、細部を持たない
のっぺりした絵が高く出て（＝③の団子）、距離だけ見ると崩れる絵が通ります。

---

## 段取り

### Phase 0 ── 出所を、機械が読める形にする

絵は一枚も変えない。棚卸しだけ。

- 各型に出所の一行注記を置く：
  `/* @src imagined */` `/* @src traced 2026-08-xx */` `/* @src set phosphor-fill:carrot */`
- これで `grep '@src imagined' js/*.js | wc -l` が残数になる
- **この文書の末尾の表が、その作業台**

> なぜ最初にこれをやるか：いまは `S(` を数えても87型で、なぞり11型が混ざります。
> 「残り何枚か」が一目で出ないと、複数セッションにまたがる作業が進みません。

### Phase 1 ── goods 4型 + `cityHall`（消費：小・1セッション）── **完了、ただし1/5**

見込みは外れました。「汎用のUIアイコンの語彙の中にある」は `cityHall` だけで、
残り4型は**日用品も、食材と同じく既製セットの語彙の外**でした。

| 型 | いまの core | 結果 |
|---|---|---|
| `cityHall` 役所 | 0.479 | **採用**（下記） |
| `plate` 皿 | 0.643 | 候補なし → **Phase 3行き** |
| `tube` チューブ（化粧品・薬用） | 0.650 | 候補なし → **Phase 3行き** |
| `bag` 袋（自立パウチ・詰め替え） | 0.775 | 候補なし → **Phase 3行き** |
| `bar` 石けん（固形の塊） | 0.794 | 候補なし → **Phase 3行き** |

**`core` の門は通っても、意味が違う絵しか無い、という壁でした。**
数字だけ見て差し替えると誤読を生みます：

- `plate`（皿）… 候補は「配膳ドームカバー」（`mingcute:dish-cover-fill`、core 0.644）
  「UFO」（`flying-saucer`、0.322＝門1も落ちる）「カトラリー付き皿セット」
  （`fork-and-knife-with-plate`、0.066）。**平たい皿一枚、という形の絵が無い。**
- `tube`（潰して封じる柔らかいチューブ）… 候補は「気管チューブ」
  （`healthicons:endotracheal-tube`、0.615）「試験管」（`test-tube`、0.520）
  「ポンプ式ボトル」（`lotion-bottle`、0.179＝門1も落ちる）。
  **「口が上・潰した底が下」という上下差を持つ絵が無い。**
- `bag`（角底・上を折り返した詰め替えパウチ）… 候補は
  `fluent:shopping-bag-*-filled`（0.60前後で門は通る）だが、これは
  **持ち手付きの手提げ袋**。goods.js の `bag` とは骨格が別物
  （手提げ袋の絵を当てると、詰め替えパックの行に手提げバッグが並ぶことになる）。
- `bar`（石けんの塊）… 候補は `ph:hand-soap-fill`（0.657、門は通る）だが、これは
  **手を洗っている絵**（ポンプボトル＋泡）。`fluent-emoji-high-contrast:soap`
  は形は塊だが `core 0.000`＝25pxで消える。**「塊」を「塊」のまま描いた絵が無い。**

**`cityHall` は採用しました**（`js/icons-todo.js`）。`fluent:building-government-24-filled`
（旗の立つ議事堂型、MIT）。core 0.498（門1）、既存108枚との距離は最も近いもの
（無関係な `do-exclaim`）でも 0.231（門2）。`bank`（Phosphor）とは屋根の形と
柱の並びで分かれる。

残り4型は Phase 3（`plate` `tube` `bag` `bar` の作業台の行を参照）へ回します。

### Phase 2 ── 食材71型を仕分ける（消費：中・1〜2セッション、20型ずつ）

**自動検索だけでは決まりません。** 語幹一致は誤爆します（実測：
`butter`→蝶、`corn`→ユニコーン、`milk`→天の川、`teaBag`→チーム、
`wasabi`→気管チューブ、`springOnion`→"archive"が"chive"に部分一致）。
目で見る工程を飛ばせない。

1. 20型ずつ、11セットから候補を検索する
2. **core（門1・≥0.45）を通ったものだけをレンダリングして、意味の当たり
   はずれを人が決める。** 数字が通っても、絵の意味が別物のことが多い
   （実測：えびの候補は天ぷらえびで `tempura` と紛れる、はんばーぐの候補は
   バンズ付きハンバーガー、油の候補はシャンパンボトル）
3. 意味も一致したものだけ、既存70型との距離（門2・≥0.18）を測る
4. **通っても、出所のセットが「単色・少ないパス数」でなければ実装しない。**
   下の一般則を参照。通ったものだけ `F()` に差し替え、残りは Phase 3 の束へ

**一般則（Phase 1 で見つかった）：`F()` で借りてよいのは、
そのセットが最初から「25px用に整えられた塗りアイコン」であるときだけ。**
Phosphor Fill・Fluent filled のような UI アイコンセットは、少ないパス数・
丸い座標で作られていて、そのまま埋め込んでも `icons-food.js` の文法
（手描きの丸い数字）を壊しません。**絵文字系セット（noto-v1・
fluent-emoji-high-contrast の色つきのほう）は違います。** 1絵あたり
数KB・数百の小数点座標を持つ精密なイラストで、シルエット化した core と
距離の数字がどれだけ良くても、生データのまま埋め込むのは
「なぞる前の下絵をそのまま貼る」のと同じです（実測：`icons-food.js`
全体が24KBのところ、noto-v1のspaghetti一つで9.6KB、ear-of-cornで12.3KB）。
**このケースは「候補あり」ではなく「Phase 3 のなぞり直しに使える参考
画像が、写真を用意してもらう手間なしで既に手元にある」と読み替えて
Phase 3 へ回す。** 実際に4型（`pasta` `orange` `corn` `grape`）で
これが起きた——core・距離とも通ったが、実装は見送った。

見込み：**71型中、Phase 2 で片づくのは 5〜15型**（Phase 1 の実績と、
この一般則を踏まえて下方修正）。

**第1束（core が低い20型）の結果、2026-09-09：71型中 1型採用。**

| 判定 | 型 |
|---|---|
| **採用** | `carrot`（Fluent、単色・少パス数） |
| 意味は一致・生データ不可でPhase 3行き（参考画像あり） | `pasta` `orange` `corn` `grape` |
| 門2（距離）で不合格 | `wine` `water` |
| 意味が別物 | `driedShiitake` `shrimp` `gratin` `chocolate` `hamburgSteak` `curryRoux` `cannedFruit` `oil` `sausage` |
| セット候補ゼロ | `onion` `cabbage` `springOnion` `squid` |

Phase 1（5型中1型）と合わせて、25型を試して2型が採用。**歩留まりは1割弱。**

**第2束（次にcoreが低い20型）の結果、2026-09-09：71型中 4型採用。**
今回は Phosphor Fill / Fluent filled 由来（単色・少パス数）の候補に絞って
探したところ、歩留まりが上がった——**セットの語彙選びを絵文字系に広げるほど
候補は増えるが、埋め込める形式のものはまた別**、という Phase 1〜2 前半の
教訓が効いた。

| 判定 | 型 |
|---|---|
| **採用** | `cheese`（Phosphor Fill） `teaBag`（同） `coffee`（同） `cookie`（Fluent） |
| 意味の似た候補はあったが識別特徴が消えて不採用 | `frozenGyoza`（ひだの無いただの塊） |
| セット候補が絵文字系のみで門1未達 | `onigiri` `peas` `bacon` `bento` `watermelon` `cake` `sushi` |
| セット候補ゼロ | `mayo` `roe` `butter` `amazake` `pickles` `wasabi` `cucumber` |
| セット候補の意味が別物 | `miso` |

**歩留まりが上がった理由：候補の出所を先に絞ったから。** 第1束は
語幹検索でヒットした候補を core の高い順に全部レンダリングしていたが、
第2束は「Phosphor Fill / Fluent filled 系だけを先に選んで測る」順にした。
絵文字系（noto-v1・fluent-emoji-high-contrast）は候補としては出るが、
（a）core が低いことが多く、（b）通っても埋め込めない、の二重の壁がある
ので、**先に除いてから探すほうが早い。**

Phase 1〜2 合計（第2束まで）：45型を試して **6型採用**（歩留まり13%）。

**第3束（次にcoreが低い20型）の結果、2026-09-09：71型中 3型採用。**
検索の誤爆が増えた束——`pastaSauce`→UFO、`tin`→スキャン系UI、
`meat`→シャンパン/ハンマー（"ham"の部分一致）、`riceCracker`→花火、と
語幹検索の限界が目立った。

| 判定 | 型 |
|---|---|
| **採用** | `fish`（Fluent） `mushroom`（Tabler） `iceCream`（Phosphor Fill） |
| 意味の近い候補はあったが別の型と紛れるため見送り | `iceCream` の棒アイス型候補（`icePop` と紛れる、コーン型を採用したので実害なし） |
| セット候補が絵文字系のみ、または門1未達 | `croissant` `mochi` `milk` `beer` `banana` `curry` `broccoli` `icePop` |
| セット候補ゼロ（誤爆除く） | `pastaSauce` `flour` `furikake` `tin` `meat` `riceCracker` `turnip` `potato` `tofu` |

Phase 1〜2 合計（第3束まで）：65型を試して **9型採用**（歩留まり14%）。

**第4束（残り11型）の結果、2026-09-09：71型中 0型採用。これで食材71型
すべてを一度は探索し終えた。**

| 判定 | 型 |
|---|---|
| 意味は一致・門2で不合格 | `apple`（`tempura`と距離0.146） `bread`（`chips`と距離0.093） |
| 候補はあるが意味が別物 | `chips`（フライドポテトは「袋菓子」の意味を持たない） |
| 候補はあるが形が既存とほぼ同じ・置き換える意義が薄い | `egg`（自己距離0.059、次点も基準線ギリギリ） |
| セット候補ゼロ、または絵文字系のみ | `tomato` `nori` `yogurt` `chestnut` `kamaboko` `noodles` `tempura` |

## Phase 2 総括（2026-09-09、ここで一区切り）

**食材71型すべてを検索・実測し終えた。** 4束・合計76型を試して
**9型採用**（食材8型＋Phase 1の`cityHall`、goods 4型は Phase 1 で
候補ゼロと判明済み）。歩留まり12%。

残り**67型（食材63型＝71−採用8型 ＋ goods 4型）**は、すべて
「セットに候補がない」「候補はあるが意味が別物」「候補はあるが既存の絵と
紛れる／変わらない」のいずれかで、**これ以上セットを探しても見込みは
薄い。** ここから先は Phase 3（参考画像をなぞる）に進むのが筋で、
そちらは利用者の手（参考画像の用意）が要る。

**Phase 2 はここで打ち止めにする。** 追加の束を回しても歩留まりが
上がる理由がない——すでに「単色・少パス数のセットを先に絞る」という
最適化を第2束で導入済みで、それでも第3・4束は歩留まりが下がっている。

### Phase 3 ── 参考画像をなぞる（消費：大・数セッション）

**1束目（利用者提供の6画像）の結果、2026-09-09：6型中6型採用（1周目の
やり直し4型＋再提供の`shrimp`1型＋畳んだ`driedShiitake`1型）。**
`wine` `onion` `gratin` `chocolate` `shrimp` をなぞって差し替え、
`driedShiitake` は利用者の判断で `mushroom` へ畳んだ（型自体が消えた）。

**2束目（利用者提供の3画像）の結果、2026-09-09：3型なぞり＋1型畳み。**
`hamburgSteak` `springOnion` `oil` をなぞって差し替え、`cannedFruit` は
`tin` へ畳んだ（`tin`自体はまだ想像描き）。`curryRoux`は`curry`へ畳む案を
提示したが、実物としては別物（板 vs 盛り付け）なので保留、画像も未提供。

**3束目（利用者提供の5画像）の結果、2026-09-09：5型すべてなぞって差し替え。**
`curry` `squid` `cabbage` `water` `onigiri`。うち`curry`と`onigiri`は
「輪郭を全部見る」を徹底したうえで、さらに一段直した：
- `curry`は1枚目の参考画像にスプーンが写っていたが依頼はスプーン抜き
  だったので、利用者に再提供してもらった2枚目（スプーンなし）の輪郭
  だけを使った。
- `onigiri`は当初、のりの帯を旧手描きアイコンの座標のまま流用して
  実装したが、利用者から「借用のほうが不自然」と指摘を受け、輪郭データ
  から実際ののりの四隅座標を計算し直して差し替えた（**旧座標の流用は
  せず、実測座標だけを使う**——この教訓は今後も守る）。

**4束目（利用者提供の5画像）の結果、2026-09-09：5型すべてなぞって差し替え。**
`sausage` `mayo` `peas` `roe` `bacon`。うち2型は数値の門で懸念があり、
利用者に確認のうえで採用した：
- `mayo`は`wine`との距離が0.132（基準0.18を割り、CLAUDE.mdの「溶け合う」
  目安0.14〜0.16も下回る）。並べて実物確認し、利用者の判断で採用。
- `bacon`はcore 0.195（危険域<0.33を大きく下回り、過去の最低採用例
  springOnion 0.303をさらに下回る）。実物（32px丸薬）で縞が判読できる
  ことを確認し、利用者の判断で採用。
- `sausage` `peas` `roe`は両方の基準を通過。

**5束目の前に：noto-v1（既製の絵文字アイコン）を自分でなぞる案は撤回した。**
`pasta` `orange` `corn` `grape` を noto-v1 のアルファ値だけで抜き出すと、
透明でない部分が全部ひとつながりになり、麺・肉団子・粒々といった中の
模様が消えて輪郭だけの塊になった（実際にpasta・cornで確認、使い物に
ならないと利用者に指摘された）。**以後、なぞる対象は必ず利用者提供の
参考画像とする**——セットの生データ埋め込み不可＝自分でなぞって良い、
という判断はしない。

**5束目（利用者提供の5画像）の結果、2026-09-09：5型すべてなぞって
差し替え。** `pasta` `orange` `corn` `grape` `bento`。
- `orange`は両方の基準を通過（core 0.680／距離0.189）。
- `grape`（core 0.352）・`bento`（core 0.443）は基準にわずかに届かないが
  危険域は超えており、利用者に実寸を確認してもらったうえで採用。
- `pasta`（core 0.289）・`corn`（core 0.322）は危険域。特に`corn`は
  参考画像自体が縦横とも細い格子（粒どうしを上下左右で仕切る構造）で、
  CLAUDE.mdの「仕切りは一枚〜数本まで」の規則に反するが、数を減らすと
  参考画像に無い模様を作ることになるため削らず、利用者の判断で
  そのまま採用した。

**その後、利用者の判断で `curryRoux` を `curry` へ畳んだ**（保留を解除。
`consomme` `stockPack` `yeast` `sesame` も道連れ）。

**6束目（利用者提供の4画像）の結果、2026-09-09：4型すべてなぞって
差し替え。** `frozenGyoza` `butter` `amazake` `pickles`。

この束で「線画（塗りが無い）」の扱いを一段直した：
- **1周目の失敗**：`frozenGyoza` `butter` を、線画の外側輪郭だけを
  塗って内側の模様（光沢の弧・箱の面の境目）を「穴」として素直に抜くと、
  抜く面積が本体とほぼ同じ大きさになり、残るのは線の太さぶんの帯だけ
  ——25pxで**完全にただの輪郭線**になった（`frozenGyoza` core 0.000、
  `butter` core 0.149）。実物確認の前に数値の桁が違うと気づき、利用者に
  指摘されて撤回した。
- **直しかた**：CLAUDE.mdが指定する線画の手順（dilate→floodfillで
  内部を塗りつぶす→同量erode）を実際に通し、輪郭ではなく**実体**に
  した。ただし開いた曲線（両端が閉じていない線）はフラッドフィルでは
  塞げない——`frozenGyoza`の光沢の弧・`amazake`のリボンの結び目は、
  実体化の過程でそのまま吸収されて消えた（残す代替案＝実体の上から
  細い帯だけを別に抜く、は試していない。まず素直な実体化で数値と
  実物が両方通るかを確認する方を優先した）。
- **`butter`だけは、閉じる強さを弱めて塊とナイフを分離した**。
  dilateを強くかけると（4px以上）塊とナイフが一体化し、利用者から
  「せめて塊とナイフは分かれていないと」と指摘を受けた。2pxまで弱めると
  自然に別の連結成分のまま残り、湯気の点8個（塗りつぶし前の生の輪郭）
  も個別に保てた。

`amazake` は `mayo`（距離0.122）、`pickles` は `nori`（距離0.103）と
基準（0.18）を大きく下回るほど接近したが、いずれも実物で見分けがつく
ことを利用者に確認してもらい採用した。`pickles`の参考画像には
Shutterstockの透かしが写っていたが、抽出したのは輪郭データのみ。

**7束目（利用者提供の5画像）の結果、2026-09-09：5型すべてなぞって
差し替え＋1型畳み。** `wasabi` `sushi` `watermelon` `cucumber` `cake`。

- `wasabi`は利用者の提案どおり「単純抽出→一部だけ塗る」で解決した。
  蛇腹の3節目（大きな背景の窪み）は抜くと本体がほぼ帯だけになって
  崩れるため抜かず、上端の継ぎ目線とグリップの溝3本だけを穴として
  残した——`frozenGyoza`/`butter`の教訓（大きな穴で本体を食い潰さない）
  を、今度は「そもそも大きな穴を作らない」形で先に活かせた。
- `sushi`（core 0.190）・`cake`（core 0.231）は数値上の危険域だが、
  実物（32px丸薬）で縞模様・層といちごが判読できることを確認し、
  利用者の判断で採用した。
- `watermelon`・`cucumber`は、利用者の指示で種／いぼの粒を1.4倍に
  拡大した（実寸で見えやすくするため。位置は変えず、各粒の重心を
  中心に拡大）。
- `pastaSauce`は利用者の判断で`pasta`へ畳んだ（`ramenSoup`も道連れ）。
- `miso`は、利用者に確認のうえ「味噌汁」として`gratin`（お椀もの）へ
  畳んだ。道連れだった瓶もの6つは、そのままお椀の絵になると不自然
  なので、新しい代表`honey`を立てて引き継いだ（形は`miso`だった小びんの
  想像描きをそのまま移しただけで、新しく描いてはいない）。`honey`の瓶
  としての参考画像は利用者が後日提供予定。`blackPepper`のOF固定表も
  `honey`へ更新した。

**8束目（利用者提供の4画像）の結果、2026-09-09：4型すべてなぞって
差し替え。** `croissant` `flour` `mochi` `honey`（`miso`から代表を
引き継いだ瓶）。

- `mochi`は、いったん線画を実体化（dilate→floodfill→erode）した
  塗りつぶし版を提示したが、利用者から「全部塗りつぶすと中の輪郭が
  見えない」との指摘を受け、**単純抽出**（外形＋段の内側の抜き3つ）に
  差し替えた。core 0.126は既存全型で最低（bacon 0.195をさらに下回る）
  だが、実物では2段の輪と橙がむしろよく判読できる——「塗りつぶして
  実体化する」が常に正解とは限らない、という反例になった。
- `flour`は麦の葉18枚が細かく、`honey`は`pickles`との距離が基準を
  下回るが、どちらも実物確認のうえ利用者の判断で採用。

残り（**食材34型 ＋ goods 4型 ＝ 38型**、2026-09-09時点）。

- **20型ずつの束**にして進める（一度に見る枚数として現実的な単位）
- 束ごとに：参考画像をもらう → `trace.js` でなぞる → 二つの門で測る →
  通らなければ「数を減らして太らせる」で直す
- **束が終わるごとに `main` まで流す。** 一度に全部を抱えない

> **この工程はあなたの手が要ります。** 参考画像を用意してもらう必要があるので、
> 私の側だけでは進みません。束の順番は、上の表の core が低いほうから
> （＝いまいちばん弱い絵から）にすると、由来と品質が同時に良くなります。

#### なぞりの実装（2026-09-09、1束目の実績）

`trace.js` はスクラッチにその都度書き直す（コミットしない）。手順：

1. **画像を二値グリッド化**（Playwright + canvas。`file://` は読めないので
   base64 data URI で渡す）。アルファがあればアルファ、無ければ輝度の
   中央値で閾値を切る
2. **`marching-squares`（npm、AGPL-3.0）で輪郭抽出**。`isoLines(grid,[0.5])`
   の戻り値は「面積最大のパスがデータ全体のbboxのことが多い」が、**絶対
   ではない**——実測で、涙型（onion）は左右2つの半分に分断されて返ってきた
   （後述）。**先頭パスを機械的に除外するのではなく、グリッド周囲に
   6px程度パディングしてから渡すこと。** 形が画像データの端に接触して
   いると、`isoLines` が輪郭を分断する（実測で確認・再現）
3. **`simplify-js`（npm、mourner氏のRDP実装）で簡略化**。ε=1.2px相当は
   **最終スケール（長辺208）に正規化してから**掛ける。**閉じたループは、
   最も遠い2点で開いた弧2本に割ってから別々に simplify し、あとで繋ぐ**
   （CLAUDE.mdの警告どおり、閉じたまま渡すと歪む可能性があるため。今回の
   simplify-jsでは円で試した限り実害は出なかったが、安全側に倒した）
4. **長辺208・中心128,128に正規化**して `M x y L x y … Z`（小数点1桁）で
   出力。既存の traced 絵（`school` 等）と同じ書式
5. **線画（内部が塗られていない絵）は floodfillで内部を塗りつぶす**。
   ただし隙間（onionの根の間など）があると外へ漏れる——**先に3〜8px程度
   dilateして隙間を埋めてからfloodfillする**
6. **細い突起（触角・脚など）は簡略化では消えない**。Douglas-Peuckerは
   「不要な点」を間引くだけで、外側へ飛び出た特徴的な部分はそのまま残す。
   消したいなら **morphological opening（erode→dilate）**。ただし今回の
   `shrimp` では、触角を消せる強さまでopeningを掛けると本体（体節の
   くびれ）まで分断されて崩れた——**この参考画像では断念**が正しい判断
   だった。無理に成形しない

**ライブラリはリポジトリにコミットしない**（AGPL-3.0のmarching-squares
を含むため）。スクラッチで一度実行し、**出力される座標データだけ**を
コードに埋め込む——ツールのライセンスは生成された幾何学データそのもの
には及ばない、という扱い。`trace.js`自体がコミットされない、という
既存の decision（CLAUDE.md）とも一致する。

**外形だけで既存の絵と紛れる場合、参考画像に実在する特徴を残す。** これは
「なぞる」の延長（実在する特徴を単純化する）であって、想像で描き足すのと
は違う——ただし判断が要るので、毎回どちらか一言で言い添えること。

#### 1周目の失敗と、その直しかた（2026-09-09、同じ束の2回目）

1周目（`wine` `onion` `gratin` `chocolate`）は数値の門は通っていたが、
**利用者から「5枚のうち満足のいく質が無い」という評価を受けた。** 具体的に：

- **`chocolate`**：外形だけを採って格子を諦めた。だが Phase 2 で
  `noto-v1:chocolate-bar` を「シルエット化で格子が消えて識別不能」と
  自分で不採用にしていたのに、**同じ失敗を今回も繰り返した**——今回は
  数値（距離0.295）が通っていたので、それで良しとしてしまった
- **`gratin`**：参考画像は「皿・湯気・スプーンの全体で1つのアイコン」
  という依頼だったのに、**皿の外形だけを勝手に切り出した**——自分の
  依頼リストの書き方（「グラタン皿を斜め上から」としか書かなかった）
  が原因で、画像の一部だけを対象にしていいと思い込んだ
- **`wine`**：ラベルを「シンプルにする」という**自分の判断で無断で
  落とした**。元の手描き版にあったラベル帯より情報を減らす方向に
  変えたことになる
- **`onion`**：縦筋を、外形の曲線に合わせず**直線の角丸長方形**で足した。
  曲線のシルエットに直線を差し込む違和感を、実装時に気づけなかった

**根っこの原因は一つ：`largestContour`（面積最大のパスだけを採用する
関数）を無条件の既定値にしていたこと。** `isoLines` は実際には、
格子9マス・湯気3本・スプーン・玉ねぎの筋5本を、**最初から個別の輪郭
として検出していた**。それを見る前に「一番大きい輪郭だけ使う」と
決めて、残りを機械的に捨てていた。

**2周目の直しかた**：`largestContour` を使わず、**`isoLines` が返す
全パス（先頭のbbox除外パスだけ除く）を、面積つきで全部書き出してから
判断する。** 実際にやると：

- `chocolate`：11パス中、外形1＋くびれ1＋格子9個がそのまま個別に
  取れた。全部使って core 0.556／距離0.300——**想像の位置ではなく、
  実物の輪郭そのもの**
- `gratin`：5パス（鍋・スプーン・湯気3本）が最初から分離して取れた。
  全部使って core 0.479／距離0.308
- `onion`：塗りつぶす**前**の線画グリッドを輪郭抽出すると、内部の
  5本の筋がそれぞれ独立した帯として取れた（実物の曲線）。5本のうち
  中央+隣接2本の3本まで間引いた（5本は core 0.197 で危険域）。
  直線の近似ではなく実物の形

**教訓（次の束から適用）**：
1. **輪郭抽出の結果を、捨てる前に全部並べて見る。** 「外形だけ採用」を
   既定値にしない——細部は、想像で足さなくても、なぞる処理そのものが
   最初から検出していることが多い
2. **依頼の対象範囲は、自分の最初のリクエスト文面を疑う。** 「皿を」
   としか書いていなくても、参考画像に写っている構成要素（湯気・道具）
   ごと依頼されている可能性を確認する
3. **数値（core・距離）が通ることと、見た目が本物に見えることは別。**
   特に「細部を削った」判断は、前のフェーズで自分が「識別に必須」と
   判定した部位（chocolateの格子）と同じなら、繰り返していないか
   自己点検する
4. **抜きの形は、外形の曲線に合わせる。** 直線の primitiveをそのまま
   差し込まない——曲線のシルエットには曲線の抜きを

### Phase 4 ── 測り直しと回帰（各フェーズの最後に毎回）

- 76型の `core` 一覧と、2485組の距離行列を再測。**中央値と最小が下がっていないこと**
- `daily-rules.js`（dailyの非評価原則）と、触った画面の主要テストを走らせる
- 丸薬32px・暦11px（3倍の実機画素）で実際に撮って読めるか確認
- `t.icon` のキーは**一文字も変えない**（保存済みの利用者の選択が外れる）

---

## 測りかた（再現できるように）

物差しは二つとも Playwright で canvas に描いて画素を数えるだけ。
スクリプトはセッションのスクラッチに置き、**コミットしません**（テストと同じ扱い）。

**共通の下ごしらえ**：絵を200pxで描き、アルファの外枠（bbox）を実測して、
**長辺が19.8pxになるように25pxの升へ中央合わせ**で焼き直す。
19.8 は `216 / 272`──器（`-8 -8 272 272`）の中で絵が占める割合そのものです。
アルファ110以上を1、それ未満を0で二値化。

- **`core`** … 4近傍がすべて1である画素の数 ÷ インクの総数。
  （＝1px侵食しても残る割合）
- **距離** … 25×25 を 12×12 に平均で落として、二つのベクトルのRMS差。

**セットの絵も、この同じ下ごしらえを通してから測ること。** viewBox の大きさが
セットごとに違うので、生のまま比べると数字が意味を持ちません。

---

## 道具の覚え書き

- **`npm pack @iconify-json/<セット名>` は proxy を通ります。**
  `api.iconify.design` / jsdelivr / unpkg / codeload は 403。
  `raw.githubusercontent.com` は通る。
- 使えるセット（**MIT / Apache 2.0 / CC0 のみ**）：

  | セット | 枚数 | licence |
  |---|---|---|
  | fluent | 20146 | MIT |
  | material-symbols | 16323 | Apache-2.0 |
  | ph（Phosphor） | 9161 | MIT |
  | tabler | 6232 | MIT |
  | hugeicons | 6005 | MIT |
  | mingcute | 3336 | Apache-2.0 |
  | healthicons | 2709 | MIT |
  | noto-v1 | 2162 | Apache-2.0（**色つき**） |
  | iconoir | 1682 | MIT |
  | fluent-emoji-high-contrast | 1594 | MIT |
  | maki | 418 | CC0-1.0 |

- **除外（CC-BY・帰属表示の欄が要る）**：basil, emojione-monotone, streamline-emojis
- `fluent-emoji-high-contrast` は `fill="currentColor"` ／ stroke無し ／
  `fill-rule` 無し（nonzero）で、**書式は `P()` とそのまま互換**。
  ただし上記のとおり core が通りません。
- `noto-v1` は多色なので、そのままでは使えません。影に潰すと core は高く出ますが、
  ③のとおり**距離が潰れます**。
- 線ベースのセット（tabler・iconoir・hugeicons ほか）は、
  名前に `fill` / `solid` / `filled` を含むものだけを候補にすること。

---

## 作業台 ── 想像描き76型

`済` の欄に、片づいた手を書く（`set:<セット名>:<絵の名>` ／ `traced:<日付>` ／
`folded:<畳んだ先>`）。**空欄が残っている限り、この作業は終わっていません。**

core は 2026年9月9日の実測（上の物差し）。低い順。

### `js/icons-food.js` — 71型

| 型 | | core | 済 | 覚え書き |
|---|---|---|---|---|
| `pasta` | ぱすた | 0.318 | **traced:2026-09-09** | 参考画像（利用者提供、皿・フォーク・渦の麺）をなぞった。フォークと皿は一つの輪郭、渦の麺は輪郭抽出が検出した10本の帯をそのまま使った。core 0.289は既存69型の最低（bacon 0.195）に次ぐ危険域だが、実物（32px丸薬）で麺の渦が読めることを確認したうえで利用者の判断で採用。距離0.301 |
| `carrot` | にんじん | 0.319 | **set:fluent:food-carrot-24-filled** | core 0.624／距離0.380。2026-09-09 |
| `orange` | みかん | 0.345 | **traced:2026-09-09** | 参考画像（利用者提供、葉つき・油胞）をなぞった。実の外形・葉・葉脈のハイライト・油胞8つを輪郭抽出が検出したまま使った。core 0.680／距離0.189 |
| `corn` | とうもろこし | 0.370 | **traced:2026-09-09** | 参考画像（利用者提供、皮＋粒の格子）をなぞった。皮の輪郭と粒の格子状の白抜き17個を輪郭抽出が検出したまま使った。参考画像そのものが縦横とも細い格子で「仕切りは一枚〜数本まで」の規則を割るが、数を減らすと参考画像に無い模様になるため削らず採用。core 0.322は危険域で実物でも格子はかなり溶けるが、利用者の判断で採用。距離0.219 |
| `wine` | わいん | 0.451 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。ラベル帯も追加。core 0.432／距離0.238（coreは基準を僅かに割るが実物で確認して採用） |
| `grape` | ぶどう | 0.462 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。粒10個と軸を輪郭抽出が検出したまま使った（粒どうしの間には白い隙間が実際にある）。core 0.352は基準未達だが危険域は超えており、実物で房として読めることを確認したうえで利用者の判断で採用。距離0.244 |
| `driedShiitake` | 干ししいたけ | 0.464 | **folded:mushroom** | 利用者の判断で型を畳んだ（「干し椎茸はきのこと一緒でいい」）。`koyaTofu` `fu` `driedSeaweedSoup` も道連れ |
| `onion` | たまねぎ | 0.474 | **traced:2026-09-09** | 参考画像（利用者提供、線画）をなぞった。内側の筋も実際にトレース（近似の直線ではなく実物の曲線）。5本中3本（中央+左右隣接）まで間引いた——5本ともは core 0.197（危険域）まで崩れる。core 0.457／距離0.325 |
| `shrimp` | えび | 0.491 | **traced:2026-09-09** | 1枚目の参考画像は崩れて見送ったが、利用者が簡易な線画を再提供、再挑戦して成功。頭・体節2・尾・触角・脚の房を全部なぞった。体節同士の細い切れ目が25pxで侵食されたため、体節4パーツを重心から7px膨らませて隙間を狭めた。個別の脚3本は太らせても細いままでも崩れるため断念。core 0.474／距離0.347 |
| `gratin` | ぐらたん | 0.493 | **traced:2026-09-09** | 参考画像（利用者提供）は皿・湯気・スプーンの全体で1枚の依頼だったので、なぞりも画像全体（5つの独立した輪郭）を使った。core 0.479／距離0.308 |
| `chocolate` | ちょこ | 0.493 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。格子9マス＋中央のくびれも、輪郭抽出が最初から個別の輪郭として検出していたものをそのまま使った。core 0.556／距離0.300 |
| `hamburgSteak` | はんばーぐ | 0.494 | **traced:2026-09-09** | 参考画像（利用者提供、格子の焼き目）をなぞった。焼き目のX字の抜きも輪郭抽出が検出済みのものをそのまま使用。core 0.354（基準を僅かに割るが実物で確認して採用）／距離0.240 |
| `springOnion` | ねぎ | 0.494 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。白根の抜きが core 0.281（危険域）だったので0.7倍に縮小し0.303まで上げたが、まだ危険域内——実物（32px丸薬）で判読できることを確認して採用（`wine` `hamburgSteak`と同じ扱い）。core 0.303／距離0.354 |
| `curryRoux` | かれーるー | 0.510 | **folded:curry** | 当初は「板 vs 盛り付け」で実物として別物と保留していたが、あらためて利用者の判断で`curry`へ畳んだ。`consomme` `stockPack` `yeast` `sesame` も道連れ |
| `cannedFruit` | 果物缶 | 0.514 | **folded:tin** | 利用者の判断で`tin`（缶詰）へ畳んだ。「缶」という同じ実物の入れ物のため（`driedShiitake`→`mushroom`と同じ理屈）。`cannedCorn` `cannedFish` も道連れ。ただし`tin`自体はまだ想像描きのまま（Phase 3未着手） |
| `frozenGyoza` | ぎょうざ | 0.588 | **traced:2026-09-09** | 参考画像（利用者提供、線画・重なった3個）をなぞった。線画は塗りが無いのでdilate→floodfill→erodeで実体化した。各皮の光沢の弧線は開いた曲線でフラッドフィルが塞げず実体化に吸収されて消えた（残す案も試したが、輪郭の外側だけを塗ると帯の太さしか残らず core 0.000＝完全な輪郭線になったため断念）。core 0.726／距離0.247 |
| `oil` | 油 | 0.519 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。`wine`と距離0.156まで接近するが、並べて実物確認し、胴体の太さ・ラベル位置で見分けが付くことを確認して採用。core 0.275／距離0.156 |
| `squid` | いか | 0.535 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。core 0.604／距離0.250 |
| `cabbage` | キャベツ | 0.536 | **traced:2026-09-09** | 参考画像（利用者提供、断面）をなぞった。渦の線2本・根の葉脈も輪郭抽出が検出済みのものをそのまま使用。core 0.368（基準を僅かに割るが実物で確認して採用）／距離0.269 |
| `water` | 水 | 0.541 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。本体・肩・キャップの3輪郭をそのまま使用。core 0.457／距離0.194 |
| `sausage` | そーせーじ | 0.545 | **traced:2026-09-09** | 参考画像（利用者提供、2本交差・グリル跡）をなぞった。胴2本とグリル跡の白抜き10本を、輪郭抽出が最初から個別の輪郭として検出していたものをそのまま使った。core 0.535／距離0.283 |
| `onigiri` | おにぎり | 0.545 | **traced:2026-09-09** | 参考画像（利用者提供、線画）をなぞった。外側の輪郭を塗りつぶしとして使用。のりの帯は、当初旧手描きアイコンの座標を借りて実装したところ利用者から「借用のほうが不自然」との指摘を受け、輪郭データから実際ののりの四隅座標を計算し直して差し替えた。core 0.652／距離0.309 |
| `mayo` | まよねーず | 0.552 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。core 0.638／距離0.132——`wine`（瓶）と基準（0.18）を割って接近するが、並べて実物確認したうえで利用者の判断で採用（キャップの角ばり・胴の丸みで見分けはつく） |
| `peas` | 豆 | 0.559 | **traced:2026-09-09** | 参考画像（利用者提供、開いた鞘に豆2粒）をなぞった。奥の鞘・手前の鞘・豆1粒・ハイライト2つ、輪郭抽出が検出した5輪郭をそのまま使った（もう1粒は鞘の縁の切れ込みとして表現）。core 0.453／距離0.327 |
| `cheese` | ちーず | 0.561 | **set:ph:cheese-fill** | core 0.460／距離0.289。2026-09-09 |
| `roe` | 魚卵 | 0.562 | **traced:2026-09-09** | 参考画像（利用者提供、粒を敷き詰めた盛り＋台）をなぞった。外形と粒のハイライト13個を、輪郭抽出が最初から個別の輪郭として検出していたものをそのまま使った。core 0.542／距離0.224 |
| `bacon` | べーこん | 0.568 | **traced:2026-09-09** | 参考画像（利用者提供、2枚重なり・縞4本）をなぞった。外形と縞の白抜き4本を輪郭抽出が検出したままの形で使った。core 0.195は過去の採用例の最低（springOnion 0.303）をさらに下回る危険域だが、実物（32px丸薬）で縞が判読できることを確認したうえで利用者の判断で採用。距離0.211 |
| `bento` | 弁当 | 0.581 | **traced:2026-09-09** | 参考画像（利用者提供、二区画・ご飯とおかず）をなぞった。外箱・区画・ご飯側のごま6粒・おかず側の食材の抜きを輪郭抽出が検出したまま使った。core 0.443は基準にごく僅かに届かないが、実物で二区画の弁当として読めることを確認したうえで利用者の判断で採用。距離0.297 |
| `frozenGyoza` | ぎょうざ | 0.588 | | セット候補（tabler:dumpling-filled、core 0.799）は門1は通るが、ひだ・半月形の識別特徴が無いただの塊で不採用 → Phase 3 |
| `butter` | ばたー | 0.598 | **traced:2026-09-09** | 参考画像（利用者提供、線画・塊＋ナイフ＋湯気）をなぞった。線画は塗りが無いのでdilate→floodfill→erodeで実体化したが、強く閉じると塊とナイフが一体化して読めなくなったため閉じる強さを2pxに抑え、2つを別の輪郭のまま残した。湯気の点8個は塗りつぶし前の生の輪郭をそのまま使った。core 0.444は基準にごく僅かに届かないが、実物で塊とナイフの分離・湯気の点が読めることを確認したうえで利用者の判断で採用。距離0.279 |
| `amazake` | 甘酒 | 0.599 | **traced:2026-09-09** | 参考画像（利用者提供、線画・瓶＋リボン結び）をなぞった。線画は塗りが無いのでdilate→floodfill→erodeで実体化した（リボンの結び目2つの抜きは開いた曲線のため塗りつぶしで消えた）。core 0.699は良好だが、既存69型との最小距離が`mayo`と0.122まで接近し基準を大きく下回る——実物では肩の形・キャップの角ばりで見分けがつくことを確認したうえで利用者の判断で採用 |
| `pickles` | つけもの | 0.600 | **traced:2026-09-09** | 参考画像（利用者提供、シルエット・保存瓶）をなぞった。core 0.761は良好だが、既存69型との最小距離が`nori`と0.103までさらに接近し基準を大きく下回る——実物では蓋のねじ山・肩の丸みで見分けがつくことを確認したうえで利用者の判断で採用。参考画像にShutterstockの透かしが写っていたが、抽出したのは輪郭データのみ |
| `wasabi` | わさび | 0.605 | **traced:2026-09-09** | 参考画像（利用者提供、線画・チューブ）を単純抽出した後、一部だけを選んで塗った——蛇腹の3節目は抜くと本体がほぼ帯だけになるため抜かず、上端の継ぎ目線とグリップの溝3本だけを穴として残した。core 0.582／距離0.235 |
| `sushi` | 寿司 | 0.611 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。外形（しゃり＋のり帯）と切り身の縞の白抜き3本を輪郭抽出が検出したまま使った。core 0.190は既存69型の最低（bacon 0.195）に並ぶ危険域だが、実物で縞模様が判読できることを確認したうえで利用者の判断で採用。距離0.260 |
| `teaBag` | お茶 | 0.612 | **set:ph:tea-bag-fill** | core 0.492／距離0.327。カップ型候補（mingcute:teacup-fill、core 0.720）もあったが牛乳等と紛れるため見送り。2026-09-09 |
| `watermelon` | すいか | 0.617 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。実の外形・二重の皮の間の白抜き・種13個を輪郭抽出が検出したまま使った（種は利用者の指示で1.4倍に拡大）。core 0.658／距離0.177（`tofu`と基準0.18をわずかに下回るが、形の違いが明白なため利用者の判断で採用） |
| `coffee` | こーひー | 0.618 | **set:ph:coffee-bean-fill** | core 0.655／距離0.248。カップ型候補（fluent:drink-coffee-24-filled、core 0.630）もあったがteaBagのカップ候補と紛れるため豆を採った。2026-09-09 |
| `cucumber` | きゅうり | 0.631 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。外形といぼの粒7つを輪郭抽出が検出したまま使った（粒は利用者の指示で1.4倍に拡大）。core 0.614／距離0.223 |
| `cookie` | くっきー | 0.632 | **set:fluent:cookies-24-filled** | core 0.708／距離0.248。2026-09-09 |
| `cake` | けーき | 0.636 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。3つの層といちご、下段のチョコチップ3つを輪郭抽出が検出したまま使った。core 0.231は危険域だが、実物で層といちごが判読できることを確認したうえで利用者の判断で採用。距離0.308 |
| `miso` | みそ | 0.639 | **folded:gratin** | 利用者の判断で「味噌汁」として`gratin`（お椀もの）へ畳んだ。道連れだった瓶もの6つ（どうばんじゃん・コチュジャン・ピーナッツバター・マーマレード・はちみつ・塩麹）は新しい代表`honey`へ移した——形はまだ想像描きのまま（Phase 3未着手）。`blackPepper`のOF固定表も`honey`へ更新済み |
| `pastaSauce` | ぱすたそーす | 0.646 | **folded:pasta** | 利用者の判断で`pasta`へ畳んだ。`ramenSoup`も道連れ |
| `honey` | はちみつ（新代表） | 0.639 | **traced:2026-09-09** | `miso`が`gratin`へ畳まれた際、道連れの瓶もの5つ（どうばんじゃん・コチュジャン・ピーナッツバター・マーマレード・塩麹）と`blackPepper`の代表として新設。参考画像（利用者提供）をなぞった。瓶の外形・蓋の線2本・波線・ピーナッツ形の窓・下部の線を輪郭抽出が検出したまま使った。core 0.635は良好だが、既存69型との最小距離が`pickles`（つけもの瓶）と0.160まで接近し「溶け合う」域（0.14〜0.16）に入る——瓶の形として実物では見分けがつくと判断し、利用者の判断で採用 |
| `iceCream` | あいす | 0.646 | **set:ph:ice-cream-fill** | core 0.616／距離0.198。棒アイス型の候補（iconoir:ice-cream-solid／mingcute:ice-cream-2-fill）は`icePop`と紛れるため見送り、コーン型を採った。2026-09-09 |
| `croissant` | くろわっさん | 0.657 | **traced:2026-09-09** | 参考画像（利用者提供）をなぞった。中央の折り目・左右の中折り目2つ・両端の小さな折り目2つを輪郭抽出が検出したまま使った。core 0.442は基準にごく僅かに届かないが実物で採用。距離0.258 |
| `flour` | 粉 | 0.662 | **traced:2026-09-09** | `rice` `blackPepper` ほかも背負う。参考画像（利用者提供、麦穂2本＋容器）をなぞった。容器の外形・蓋の線・ラベルの丸窓・麦穂2本（葉18枚）を輪郭抽出が検出したまま使った。core 0.311は危険域（葉が細かい）だが実物で読めることを確認し利用者の判断で採用。距離0.321 |
| `mochi` | もち | 0.663 | **traced:2026-09-09** | 参考画像（利用者提供、線画）を単純抽出した——利用者の指示で内部を塗りつぶさず段の輪郭を残した。外形と下段・上段・橙の内側の抜き3つを輪郭抽出が検出したまま使った。core 0.126は既存全型で最低（bacon 0.195をさらに下回る）だが、実物で2段の輪と橙が判読できることを確認し利用者の判断で採用。距離0.292 |
| `furikake` | ふりかけ | 0.664 | | セット候補ゼロ |
| `tin` | 缶詰 | 0.666 | | セット候補ゼロ（"can"が"scan"等に部分一致する誤爆のみ） |
| `milk` | 牛乳 | 0.667 | | セット候補（tabler:milk-filled）は門1未達（core 0.425）→ Phase 3 |
| `meat` | 肉 | 0.669 | | セット候補ゼロ（"meat"が"champagne""hammer""hamburger"に部分一致する誤爆のみ） |
| `beer` | びーる | 0.678 | | セット候補（ph:beer-bottle-fill、core 0.437）は門1未達 → Phase 3 |
| `banana` | ばなな | 0.680 | | セット候補は絵文字系のみ |
| `riceCracker` | せんべい | 0.682 | | セット候補ゼロ（"cracker"が"firecracker"に部分一致する誤爆のみ） |
| `turnip` | かぶ | 0.691 | | セット候補ゼロ |
| `curry` | かれー | 0.695 | **traced:2026-09-09** | 参考画像（利用者提供、皿・盛り・飛沫3点）をなぞった。二重の縁線は細すぎて25pxで消えるので外側の縁だけを残し、盛りは中心から0.8倍に縮小して縁の余白を広げた。1枚目の参考画像にはスプーンも写っていたが、依頼はスプーン抜きだったので2枚目（スプーンなし）を再提供してもらい、その輪郭だけを採用。core 0.667／距離0.234 |
| `broccoli` | ぶろっこりー | 0.708 | | セット候補は絵文字系のみ |
| `fish` | 魚 | 0.710 | **set:fluent:food-fish-24-filled** | core 0.679／距離0.329。2026-09-09 |
| `potato` | いも | 0.712 | | セット候補ゼロ |
| `tofu` | とうふ | 0.718 | | セット候補ゼロ |
| `mushroom` | きのこ | 0.718 | **set:tabler:mushroom-filled** | core 0.696／距離0.223。2026-09-09 |
| `icePop` | あいすばー | 0.720 | | セット候補（ph:popsicle-fill、core 0.383）は門1未達 → Phase 3 |
| `tomato` | とまと | 0.722 | | セット候補ゼロ |
| `nori` | のり | 0.732 | | セット候補ゼロ |
| `apple` | りんご | 0.737 | | セット候補（mingcute:apple-fruit-fill、core 0.791）は門1は通るが、距離0.146で `tempura` と紛れ門2不合格 → Phase 3 |
| `yogurt` | よーぐると | 0.765 | | セット候補ゼロ |
| `chestnut` | くり | 0.773 | | セット候補は絵文字系のみ |
| `chips` | すなっく | 0.787 | | セット候補（mingcute:fries-fill＝フライドポテト）は門1は通るが、この型は「袋菓子（両端しぼり）」で `nuts` `gummy` `marshmallow` `popcorn` も背負う広い型なので意味が合わない → Phase 3 |
| `kamaboko` | かまぼこ | 0.788 | | セット候補は絵文字系のみ |
| `noodles` | めん | 0.789 | | セット候補は絵文字系のみ |
| `bread` | ぱん | 0.790 | | セット候補（tabler:bread-filled、core 0.791）は門1は通るが、距離0.093で `chips`（袋菓子）と紛れ門2不合格 → Phase 3 |
| `egg` | 卵 | 0.794 | | セット候補（mingcute:egg-fill、core 0.796）は形が既存とほぼ同じ（自己距離0.059）で置き換える意義が薄いうえ、次点の `milk` との距離0.185も基準線ギリギリなので見送り → Phase 3 |
| `tempura` | てんぷら | 0.819 | | 最強。セット候補ゼロ |

### `js/icons-goods.js` — 4型

| 型 | | core | 済 | 覚え書き |
|---|---|---|---|---|
| `plate` | 皿 | 0.643 | | 椀（深い）と浅さで分けている。セット候補ゼロ（「平たい皿」の絵が無い。試した：dish-cover 0.644／flying-saucer 0.322／fork-and-knife-with-plate 0.066）→ Phase 3 |
| `tube` | チューブ | 0.650 | | 口が上・潰した底が下という上下差が要点。セット候補ゼロ（試した：endotracheal-tube 0.615／test-tube 0.520／lotion-bottle 0.179、いずれも意味違い）→ Phase 3 |
| `bag` | 袋 | 0.775 | | セット候補ゼロ（shopping-bag系は core 通るが手提げ袋で骨格が別物）→ Phase 3 |
| `bar` | 石けん | 0.794 | | 角丸の強さで電池・箱と分けている。セット候補ゼロ（hand-soap-fillは「手洗い」の絵、soapはcore 0.000）→ Phase 3 |

### `js/icons-todo.js` — 1型

| 型 | | core | 済 | 覚え書き |
|---|---|---|---|---|
| `cityHall` | 役所 | 0.479 | **set:fluent:building-government-24-filled** | core 0.498／既存108枚との最小距離 0.231。2026-09-09 |

---

## この作業で壊してはいけないもの

- **`t.icon` に保存済みのキー**。キー文字列は一文字も変えない
  （変えると利用者が手で選んだ絵が外れる）
- **引く順番**。`markOf()` の iconsGoods → iconsFood → 色つき、
  `iconMarkHtml` の やること＝こと優先／買うもの＝品物優先
- **器（viewBox）は `-8 -8 272 272`、長辺216・中心128,128**。
  絵の座標は動かさない（動かすと、これから描くものが揃わなくなる）
- **色は `currentColor`**。丸薬の上では白抜き、詳細の紙の頭では白になる
- **`fill-rule`**：Phosphor 由来と Fluent 由来は付けない（nonzero）。
  手で描いたもの・なぞったものは `S(...)` の evenodd
