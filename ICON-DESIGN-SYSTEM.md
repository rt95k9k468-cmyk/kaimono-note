# くらしノート Icon Design System v1

対象: アプリ全体（買うもの・価格・やること・ダイエット・設定）のすべての絵
実装: `js/icon-system.js`（語彙）／`js/icons-v2.js`（試作30個）／`icon-preview.html`（確認用）

---

## 0. この文書の位置づけ

| 段階 | 内容 | 状態 |
|---|---|---|
| ① 現状調査 | `ICON-AUDIT.md` | **完了** |
| ② Design System 設計 | この文書＋`js/icon-system.js` | **完了** |
| ③ 代表30個の試作 | `js/icons-v2.js` | **完了** |
| ④ 品質確認 | 遵守検査14項目・実寸/暗所/実行速度 | **完了** |
| ⑤ 857個へ展開 | — | 未着手（確認後） |

**アプリ本体はまだ 1 バイトも変えていません。** `index.html` も `sw.js` も
`product-icons.js` も無変更で、新しい2ファイルはどこからも読まれていません。

---

## 1. 設計の芯 — 統一は「気をつける」では作れない

857個を一人で一日で描くことはありません。数週間かけ、何度も中断し、
そのあいだに描き手の手癖は必ず動きます。だから**画風を意思ではなく構造で縛ります**。

次の5つが同じなら、シルエットがどれだけ違っても一族に見えます。
逆にこの5つを外すと、どれだけ丁寧に描いても混ざりません。

| # | 規約 | 値 | なぜ |
|---|---|---|---|
| 1 | **輪郭線** | `var(--ic-line)` 1色・太さ 1.0・角丸結合 | 一族に見せる最大の要因。白い物がカードに溶ける問題もこれで解決 |
| 2 | **陰と艶** | 陰＝黒 13%／艶＝白 55%・光源は左上固定 | 色ごとに影を作ると絵ごとに光源がずれる。黒一枚なら 857個で光が揃う |
| 3 | **色** | 名前のある 41 色だけ。生の16進は禁止 | 旧版が「なんとなく揃わない」原因がこれ（**480色**のリテラル） |
| 4 | **丸み** | `xs .6 / sm 1 / md 1.6 / lg 2.4 / xl 3.4 / full` | 中間値を使い始めた時点で丸みは揃わなくなる |
| 5 | **版面** | インクは 3〜21（線込みで 2.5〜21.5）、viewBox は `0 0 24 24` | 行に並べたとき頭が揃う |

旧版との違いを一言でいうと、**輪郭線が付き、色に名前が付いた**ことです。

---

## 2. 輪郭線

```
stroke = var(--ic-line, #4a423b)   太さ 1.0   linejoin/linecap = round
```

* 純黒ではなく**茶を含んだ暗色**。食べものと日用品が並ぶ画面で、純黒は硬く、
  すべてがプラスチックに見えます。
* 24グリッドの 1.0 は、25px表示で約1.04px、2倍画面で実線2px。
  0.8 まで細めると 34px のタイルで頼りなく、1.2 まで太らせると 25px の行で絵が線に埋まります。
* **CSS変数**なので暗い画面で差し替えられます。旧版は色が固定で、
  ダークテーマでは牛乳・卵・ティッシュがカードに溶けていました
  （そのために `.p-icon` に `drop-shadow` が一段かかっています）。
  **線があれば影は要りません** — ⑤で `drop-shadow` を外せます（描画コストも下がります）。

```css
:root                          { --ic-line: #4a423b; }
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) { --ic-line: #37312c; }
}
:root[data-theme="dark"]       { --ic-line: #37312c; }
```

**内側の線は必ず輪郭より細く**（0.9 以下、または不透明度を落とす）。
同じ太さで引くと内と外の区別がつかず、24pxで絵が黒く潰れます。

---

## 3. 陰と艶

| 用途 | 値 | 使いどころ |
|---|---|---|
| `sh()` 面の陰 | `#000` @ **.13** | 形の右下。すべての立体物 |
| 浅い陰 | `#000` @ **.08** | 白いもの（卵・牛乳・おにぎり） |
| `gloss()` 艶 | `#fff` @ **.55** | 丸いもの。位置は左上固定 |
| `glow()` 白おき | `#fff` @ **.30** | ラベル面・ガラス面 |

**この4つ以外の陰影を作らないこと。** 「ここだけ少し濃く」を許した時点で、
857個の光源はばらばらになります。

---

## 4. パレット（41色）

系統ごとに `base` と `D`（深い方）の対。`D` は縁・蓋・へた・口金など
**造形上の暗部**に使います。面の陰は `sh()` が持つので、`D` を影として使わないこと。

| 系統 | base | D | 主な用途 |
|---|---|---|---|
| red | `#e8594b` | `#c13d33` | トマト・りんご・肉・危険 |
| coral | `#f0776a` | `#cf5245` | 持ち手・小物 |
| orange | `#f2903f` | `#d16d1c` | にんじん・みかん・注意 |
| amber | `#f5c04a` | `#dc9a1e` | 卵黄・チーズ・バナナ・穂 |
| lime | `#96c85a` | `#6fa338` | 葉物・新しいもの |
| green | `#4fa863` | `#33854a` | 野菜・植物 |
| forest | `#2f8f5b` | `#1f6f44` | アプリ主色に連なる緑 |
| teal | `#3fb3a6` | `#22907f` | 家電・機械 |
| sky | `#63b0e8` | `#3684c6` | 水・空・冷たいもの・画面 |
| blue | `#4d80d6` | `#2f5cb0` | 洗濯・青いもの |
| indigo | `#6a6fc9` | `#494ea3` | 夜・睡眠 |
| violet | `#a377d8` | `#7d4fb5` | 化粧品・香り |
| pink | `#ef8fb0` | `#d1648c` | 肉・桃・赤ちゃん |
| brown | `#b5793f` | `#8c5a2b` | 木・パン・段ボール |
| tan | `#dba869` | `#b7823f` | 焼き色・耳 |
| cream | `#f4e6c8` | `#dcc79a` | 生地・紙袋・ごはん |
| paper | `#eff3f6` | `#ccd8e1` | 白いもの（牛乳・卵・紙） |
| grey | `#b9c3c9` | `#8d989f` | 中間の無彩色 |
| steel | `#8e9ba4` | `#65727b` | 金属・刃物 |
| ink | `#4a423b` | `#332e29` | 画面・黒いもの |
| white | `#ffffff` | — | 艶・ラベル |

**1つの絵で使う色相は3つまで。** 変化はシルエットで作り、統一は色で作ります。

---

## 5. 部品 — テンプレートは作らない

`icon-system.js` が出すのは「ボトルの絵」ではなく**角丸・円・楕円・自由曲線**の4つだけです。

```js
r(x,y,w,h,rad,fill)   c(cx,cy,r,fill)   e(cx,cy,rx,ry,fill)   p(d,fill)   // 線あり
rn/cn/en/pn(...)                                                          // 線なし（内側の模様）
ln(d,color,w,opacity)                                                     // 内側の細線
sh(d) / shR(x,y,w,h,rad)   gloss(cx,cy,rx,ry)   glow(d)                   // 陰と艶
icon(...parts)                                                            // 器に収める
```

> **共通部品を「絵」の単位まで太らせないこと。**
> 旧版は `bottle` `jug` `box` `bag` など30種の**完成した silhouette** を持っており、
> 133個がその色違いでした。洗剤と柔軟剤とシャンプーが同じ形の色違いになるのは、
> 部品の粒度が大きすぎたからです。

### 実際にそうなっているかの検証

試作では、**同じ「洗剤の容器」4種を形で描き分け**ています。

| key | 実物の特徴 | 胴体パスの長さ | 要素数 |
|---|---|---|---|
| `dishSoap` | 細い注ぎ口・背が高い・肩が立つ | 104字 | 6 |
| `laundry` | 取っ手つきの角ジャグ | 119字 | 6 |
| `softener` | 肩が丸く落ちた寸胴・計量カップ蓋 | 92字 | 5 |
| `shampoo` | ポンプ・首の折れ | 95字 | 6 |

自動検査で「色を外しても同一形状ではない」「胴体の形そのものが違う」を毎回確認します。

---

## 6. 24pxで読めるということ

実機での表示は **25px（買うものの行）・28px（価格の行）・34px（タイル）**です。

| サイズ | 判定 |
|---|---|
| 34px | すべて明瞭 |
| 28px | すべて明瞭 |
| 25px | すべて明瞭。白い物も輪郭で分離する |
| 24px | 明瞭（設計の基準） |
| 20px | 単純な物（りんご・卵・魚・ボトル）は可 |
| **16px** | **建物・乗り物など要素の多い絵は潰れる。商品アイコンをこのサイズで出さないこと** |

**作画の掟**
* 図形要素は **1個あたり 12 個まで**（試作の平均は 5.9 個）
* 意味は**輪郭**で伝える。色は仲間を伝えるだけ
* 細部は「あるように見える」程度に留める。24pxに描き込みは入らない
* 一物一形。同じ形を色だけ変えて使い回さない

---

## 7. 自動検査（14項目）

857個を「気をつけて」保つことはできないので、機械で保ちます。
`scratchpad/iconsys.js` が毎回これを見ます。

| # | 検査 | 現状 |
|---|---|---|
| 1 | 個数 | 30 |
| 2 | すべて `viewBox="0 0 24 24"` | ✓ |
| 3 | すべて `class="p-icon"` | ✓ |
| 4 | すべて輪郭線グループを持つ | ✓ |
| 5 | グラデーションを使っていない | ✓ |
| 6 | **パレット外の生の色がない** | ✓ |
| 7 | 角丸・円が版面 2〜22 に収まる | ✓ |
| 8 | 内側の線が輪郭と同じ太さで不透明でない | ✓ |
| 9 | ボトル4種が色を外しても同一形状でない | ✓ |
| 10 | ボトル4種の胴体の形が違う | ✓ |
| 11 | 全体で同じ形の使い回しがない | ✓ |
| 12 | 平均 1,100B 以下 | **819B** |
| 13 | 最大 1,800B 以下 | **1,092B** (`fish`) |
| 14 | 図形要素 12 個以下 | **最大 8** (`broccoli`) |

---

## 8. 容量の見込み

| | 現行557 | 試作30（実測） | 857個の見込み |
|---|---|---|---|
| ソース 1個あたり | 306 B | **489 B** | — |
| 展開後SVG 1個あたり | 413 B | **819 B** | — |
| 描画ソース合計 | 171 KB | 14.7 KB | **約 410 KB** |
| ＋キーワード表 | 42 KB | — | 約 63 KB |
| ＋システム本体 | 12 KB | 10.5 KB | 10.5 KB |
| **合計（ソース）** | **227 KB** | — | **約 482〜544 KB** |
| **合計（gzip）** | **60 KB** | — | **約 128〜144 KB** |

単一ファイル版 `dist/kaimono-note.html` は 1.84 MB → **約 2.1 MB** の見込み。
うちフォントが 612 KB を占めているので、比率としては妥当な範囲です。

---

## 9. ⑤へ進む前に必ず片づけること

`ICON-AUDIT.md` で挙げたリスクのうち、**857個に増える前でないと直しにくいもの**。

| # | やること | 理由 |
|---|---|---|
| **A** | `sortProductsInCategory` の `iconKeyOf` をメモ化 | 比較関数の中で全キーワードを線形走査している。1,667→約3,000語で倍になる |
| **B** | アイコンピッカーの分割描画（仮想スクロール） | 857個で HTML 735 KB・DOM 8,452ノード・**76 ms**（実機で 230〜380 ms）を実測 |
| **C** | キーワード重複検出テストの追加 | 既に18語が死語。3,000語になれば確実に増える |
| **D** | `--ic-line` を `base.css` に追加 | 暗所対応。あわせて `.p-icon` の `drop-shadow` を撤去 |
| **E** | `ALIAS = { 旧key: 新key }` の用意 | 統合するキーがある場合。ユーザーの手動選択を失わないため |

---

## 10. CSV（ICON-MASTER-v2）についての指摘

いただいた 857 行の CSV を検査した結果、**そのままでは⑤に入れません**。

| # | 問題 | 件数 | 対処 |
|---|---|---|---|
| 1 | 新規キーが `NEW_001`〜`NEW_300` の**仮名のまま** | 300 | 意味のある英字キーを確定させる必要あり（`hospital` `superMarket` …） |
| 2 | **既存アイコンと完全に重複**する新規候補 | **24** | 削除。`アイロン`=`iron`、`洗濯ネット`=`laundryNet`、`エコバッグ`=`ecoBag`、`テレビ`=`tv`、`ゴミ箱`=`trashCan` など |
| 3 | 新規候補の名前が既存アイコンに解決してしまう | 125 | うち24が真の重複（上記）。残り101は**キーワード衝突の予告**（`クリーニング店`→栗、`タクシー`→くし、`おもちゃ`→餅） |
| 4 | `action_final=要確認` の27件 | 27 | **すべて既存キー**（ベビー4・文具17・カトラリー5・その他1）。判断は不要で、単に描き直す対象 |
| 5 | 参考画像の「やること・行動 150個」が CSV に無い | — | 画像と CSV が食い違っている。`ゴミ出し` `支払い` のような**行動**の絵をどうするか要決定 |

重複24件を除くと、**実際の新規は約276個・総数は約833個**になります。

---

## 付録 — 試作30個の内訳

| 分類 | key |
|---|---|
| 果物・野菜 | `apple` `tomato` `carrot` `banana` `broccoli` `strawberry` |
| 主食・たんぱく | `bread` `egg` `milk` `rice` `onigiri` `fish` |
| ボトル4種 | `dishSoap` `laundry` `softener` `shampoo` |
| 紙もの | `toiletRoll` `tissue` |
| 道具・機械 | `scissors` `knife` `smartphone` `tv` `broom` `vacuum` |
| **新規（場所・乗り物・行動）** | `hospital` `superMarket` `bank` `train` `trashOut` `payment` |

新規6個は、**買うもの以外の領域（場所・乗り物・行動）へこの画風が伸びるか**を
確かめるために入れてあります。伸びました。
